# План разработки

Предмет ведения: будущие этапы, границы выпусков, бэклог и критерии 1.0.0. Игровые правила, формулы, контракты и макеты не входят.

---

## 1. Правила ведения

Путь к 1.0.0 делится на этапы и подэтапы. Подэтап даёт отдельную версию `0.N.0` и проверяется без чтения исходного кода. Правки внутри подэтапа увеличивают `0.N.x` и не открывают нового объёма.

Инварианты комплекта обязательны для всех выпусков. Алгоритм противника формирует те же команды, что и человек.

Если подэтап не укладывается в объём, граница не расширяется за счёт следующего. Незавершённое остаётся дефектом либо явно переносится. Содержание, которого нет в комплекте к началу подэтапа, в выпуск не входит.

---

## 2. Пройденное

| Версии | Результат | Статус |
|---|---|---|
| 0.1.0–0.5.0 | Этап 1: каркас, сетка, атака, тактический экран, «Быстрый матч» | готово |
| 0.6.0–0.9.0 | Этап 2: обзор, дозор, среда, умения и состояния | готово |
| 0.10.0–0.13.0 | Этап 3: корабль, дружина, хозяйство, нефинальные миссии, сохранения | готово |
| 0.14.0–0.17.0 | Этап 4: хотсит, локальная сеть, набор/наблюдатель, публичная сеть, повторы | готово |
| 0.18.0 | Этап 5.1: генералы (Яга, Соловей) | готово |
| 0.19.0 | Этап 5.2: режим обучения (3 миссии) | готово |
| 0.20.0–0.20.19 | Этап 5.3: онбординг кампании и доводки входа/обучения/продолжения былины | готово |

Текущее приложение: **0.20.68**. Выпуски 0.20.20–0.20.68 — визуальный
план (этапы 1–5, правки по ревью в `doc/ui-design.md`, разделы 10–16) и
пролог кампании (подэтап 5.4, нормативный сценарий — `doc/campaign.md`).
Следующий подэтап — **0.21.0**: трек укрепления по экспертному ревью
(см. §3.1); содержательные подэтапы 5.5–5.8 сдвигаются на 0.22.0 и далее.

---

## 3. Дальше

### 3.1. Подэтап 0.21.0 — трек укрепления: тихие отказы и машинно-проверяемые инварианты

Подэтап инициирован экспертным ревью (`REVIEW_EXPERT.md`, раздел 7).
Порядок дней задан ревью и не произволен: сначала внешняя поверхность и
потеря данных, затем страховочная сеть тестов, и только потом рефакторинг
состояния. Один день — один коммит; ветка остаётся рабочей после каждого
дня. Трек ведётся в рабочей ветке `hardening/0.21`.

Черновой чеклист P0/P1 (статус на 0.21.0, день 1):

- [x] **День 1 — разведка и CI.** CI установлен: `.github/workflows/ci.yml`
  (три задания, `concurrency`, `--frozen-lockfile`) и
  `deploy-pages.yml` отслеживаются веткой; образец
  `doc/examples/ci.yml` поддерживается в соответствии с боевым файлом.
  Шаг `pnpm audit --audit-level=high` добавлен в задание `quality`
  образца; в боевой файл его переносит пользователь с правом `workflows`
  (приложение Arena этого права не имеет — см. `doc/operations.md`,
  раздел CI). Для зелёного порога обновлены dev-инструменты: vitest
  2.1 → 3.2 (критическая находка в dev-сервере Vitest UI), vite 5.4 →
  6.4 (high, обход `server.fs.deny`), esbuild 0.21 → 0.25 (moderate,
  dev-сервер ретранслятора). Локально аудит чист полностью.
- [x] **День 2 — ретранслятор (P0-1).** `maxPayload` в `WebSocketServer`
  (65 KiB; кадр крупнее закрывается транспортом кодом `1009` до
  буферизации), пределы комнат и соединений (`RELAY_MAX_ROOMS`=200,
  `RELAY_MAX_SOCKETS`=400 — переменные окружения и опции `createRelayServer`),
  отказ `CAPACITY` и `1013 OVERLOADED`, `peerId()` на
  `crypto.randomUUID()`; три новых теста в
  `apps/signaling-server/tests/relay-server.test.ts` (падали бы до правки);
  документация — `doc/network-protocol.md` §8.1 (владелец сетевого
  формата) и `apps/signaling-server/README.md`.
- [x] **День 3 — граница команд (P0-4) и мелкие правки сети.** Предел
  длины `path` (256), `isCommandPayload` как type-guard (`value is Command`,
  net зависит от core — слой 3→2) без `as Command` в сессии; пределы
  `match.entities` (256) / `grid.tiles` и `visible`/`explored` (10 000) в
  `isSyncPayload`; команда пишется в `replayDraft` только после
  `applied.ok` во всех трёх путях (локальный поочерёдный, команды
  ведомого у ведущего, собственные команды ведущего). Попутно
  исправлен скрытый дефект: черновик журнала затирался открытием боя
  (`openBattle({...idle})`) и до первой записи не доживал — теперь он
  несётся боевым стейтом. Из пакета P2: удалён осиротевший комментарий в
  `content/src/schemas/modes.ts`; общее localStorage-замыкание вынесено в
  `createLocalStorageBackend()`; повторам добавлены `onQuotaExceeded`,
  а `deleteReplay`/`clearReplays` возвращают результат записи.
  Тесты: net (+2), session (+2), storage (+1).
- [x] **День 4 — живучесть автосохранения (P0-3).** В
  `createSaveSerializer` добавлен флаг `workerAlive`: ошибка `onerror` и
  таймаут молчания (4 с) помечают поток мёртвым, после чего сериализация
  выполняется синхронно в главном потоке (`serializeSaveDraft`) с
  `console.warn` — автосохранение не замолкает навсегда и карта ожидания не
  растёт; у каждого промиса есть таймер, очищаемый на ответе, ошибке и
  `dispose()`. Тесты в `storage/tests/save-serializer.test.ts`: второй вызов
  после ошибки воркера разрешается откатом (не виснет), молчащий воркер
  разрешается по таймауту, `dispose()` отклоняет ожидание и гасит таймеры.
  Проверено глушением: без пометки воркера мёртвым тест падает.
- [x] **День 5 — формат повтора, часть 1 (P0-2).** В `@bylina/replay`
  введены независимые от версии приложения `REPLAY_FORMAT_VERSION = 1`
  (поля журнала) и `RULES_VERSION = 1` (боевые алгоритмы/порядок
  обращений к ГПСЧ/влияющий на исход контент); номер приложения хранится
  как диагностический `appVersion`. Добавлена `replayCompatibility()`
  (`"ok"` / `"otherRules"` / `"unsupported"`) — задел для интерфейса
  дня 6. `isReplayJournal` валидирует новую форму; журналы прежнего
  формата (поле `version: string`, без `formatVersion`) отсекаются и
  больше не воспроизводятся молча. Старая константа `REPLAY_VERSION`,
  приравнивавшая формат к версии приложения, удалена (обновлены доки
  README/operations и текст `set-version`). Тесты replay: штамп версий,
  отказ старого формата, классификация совместимости, воспроизведение
  (5 проверок).
- [x] **День 6 — формат повтора, часть 2: интерфейс.** Экран повторов
  показывает записи прежнего и будущего форматов, а не прячет их: журнал
  другой версии правил помечается бейджем «Другие правила», а просмотр
  открывается только после подтверждения в модальном окне (бой не
  запускается молча); журналы неподдерживаемого формата (старый строковый
  `version` или будущий `formatVersion`) не имеют кнопки просмотра, но
  доступны для удаления, с поясняющей подписью. Добавлены ключи
  `replay.otherRules`/`otherRulesNote`/`otherRulesWatch`/`unsupported`/
  `unsupportedNote` в ru и en (647 ключей, `check:i18n` зелёный).
  `check-version-consistency.mjs` проверяет единственное место объявления
  `REPLAY_FORMAT_VERSION`/`RULES_VERSION` и наличие напоминания о ручном
  подъёме `RULES_VERSION` (модуль replay + `doc/operations.md`); заметка в
  `doc/debug-mode.md`. UI-тесты: подтверждение просмотра при других
  правилах, отказ+удаление неподдерживаемого формата, будущий формат
  (3 проверки).
- [x] **День 7 — границы модулей в CI (P1-5, часть 1).**
  `app/.dependency-cruiser.cjs` переносит направление зависимостей
  `architecture.md` §2 в машиночитаемые правила: `no-cycles` (ацикличный
  граф), `layers-point-down` (пять слоёв: 1 — content/i18n/settings,
  2 — core/campaign/replay, 3 — storage/net/signaling, 4 — session,
  5 — ui/render; импорт допустим только в нижележащий либо свой слой) и
  `core-remains-pure` (`core`/`campaign` не импортируют React/PixiJS и
  модули связи, хранения, сессии, отображения и интерфейса; DOM-глобали
  остаются за ESLint). Кросс-пакетные импорты `@bylina/*` в
  pnpm-монорепозитории матчатся по спецификатору импорта (симлинки лежат
  в node_modules каждого пакета). Скрипт `pnpm check:boundaries` встроен в
  `pnpm lint` и отдельным шагом «Check module boundaries» в задаче lint CI.
  На текущем графе — 0 нарушений (260 модулей, 728 зависимостей);
  искусственные нарушения (импорт `@bylina/ui` и `react` в `core`, импорт
  вверх по слоям из content/net/session, `campaign → storage`) роняют
  проверку с ненулевым кодом — критерий «импорт `@bylina/ui` в `core/src`
  роняет CI» выполнен.
- [x] **День 8 — типизированный линтинг (P1-5, часть 2).**
  `eslint.config.mjs` поднимает набор `recommendedTypeChecked` на
  `projectService` (фаза 1, по принятой логике предупреждений): правила
  применяются только к типизированным файлам в `src`/`tests` — каталогам,
  покрытым package-tsconfig; `projectService` сам находит ближайший
  tsconfig, а корневые `*.config.ts` (вне tsconfig) остаются на базовом
  наборе. Все находки типизированного набора — `warn`, сборку не роняют:
  `no-floating-promises` и `no-misused-promises` в их числе (проверено
  искусственным плавающим промисом — срабатывает как warn). Дополнительно
  `no-undef` отключён для TS-файлов (видимость проверяет сам TypeScript).
  Типизированный проход заметно медленнее (~28 с против ~1,5 с), поэтому
  линт остаётся отдельной задачей CI; правила подключены внутри одного
  `pnpm lint` (eslint && check:boundaries). Список зафиксирован: 0
  ошибок, 333 предупреждения (289 в тестах, 44 в исходниках). По
  правилам: `require-await` 116 (почти всё в тестах),
  `no-unnecessary-type-assertion` 67, `unbound-method` 13,
  `no-await-in-loop` 12 (осознанный проигрыватель событий),
  `no-unsafe-assignment` 5, единичные `no-redundant-type-constituents`,
  `await-thenable`, `no-base-to-string`. `no-floating-promises` и
  `no-misused-promises` на текущем коде срабатываний не дали — правила
  активны и готовы ловить регрессии.
- [x] **День 9 — тест исчерпывающности `cloneState` (P1-4, часть 1).**
  `packages/core/tests/clone-state.test.ts` (6 проверок): состояние партии
  строится со всеми необязательными полями — `poison`, `panic`,
  `skillCooldowns`, `skillUses`, `weaponIds`, `skillIds`, `immobileTurns`,
  `timedLife`, `extracted`, `apple` (с `pos`/`carrierId`), `objective`,
  граневое `edge`, плюс метки клетки (`extract`/`homeOwner`/`blockLOS`/
  `pit`). Значения сверяются рекурсивным обходом (со `toEqual` на каждом
  узле, эталон значений — `structuredClone` в проверке повторной загрузки),
  отсутствие общих ссылок доказывается на каждом уровне: между снимком и
  исходным состоянием, между двумя последовательными снимками (пересечение
  множеств ссылок пусто), а также мутациями выданного снимка и исходного
  состояния — ни то ни другое не влияет на состояние ядра. Покрыт и путь
  повторной загрузки (`initial` из снимка другого ядра). Проверка
  глушением: удаление любой из 11 строк глубокого копирования (objective,
  extracted, apple.pos, grid.tiles, массив entities и шесть вложенных
  полей сущности) роняет тест — проверено поочерёдной мутацией с откатом.
- [x] **День 10 — тесты хранения и детерминизм (P1-4, часть 2).**
  Граница хранения (`packages/storage`): добавлена структурная валидация
  снимка партии `isMatchSnapshot` (Major-5) — `SaveData.match` проверяется
  на целостность сетки/клеток/сущностей и числовой строки состояния ГПСЧ;
  испорченный снимок отклоняется чисто (`load()` возвращает null, а не
  роняет ядро). Zod сознательно не вводился в storage: форма `MatchState`
  живёт в ядре, зеркалить её в Zod значило бы дублировать контракт;
  структурная проверка границы достаточна (Zod остаётся на границе
  контента). Тесты `save-boundaries.test.ts` (17): миграция 1 → 2, отказ
  на будущий/нулевой/отрицательный формат, валидация `match` (без сетки,
  не-массивы клеток/сущностей, клетка без координат, сущность без числовых
  полей, битая строка ГПСЧ, загрузка испорченной записи — null без
  исключения), `QuotaExceededError` (имя и коды 22/1014) возвращает false и
  зовёт колбэк, прочие ошибки записи не путаются с переполнением.
  Property-based детерминизм ядра (`core/tests/determinism.test.ts`, 3,
  fast-check): случайная политика ходов (MOVE/ATTACK/DEFEND/END_TURN)
  пишет принятые команды, затем воспроизводит их на свежем ядре с тем же
  seed — снимок идентичен после каждой команды; два независимых прогона
  одной политики идентичны; контрольный фиксированный прогон. Проверка
  глушением: внедрение недетерминированного мутажа в `apply` (случайное
  изменение `defense`) роняет все три теста, чистый код — зелёный.
- [x] **День 11 — ревизия в ядре и сессии (P1-1, часть 1).**
  `core/kernel.ts`: монотонный счётчик `revision`, поднимается ровно в
  `emit()` (единая точка зафиксированного изменения: успешный `apply`,
  `spawnScripted`, `restoreMatch`, `debugAutoWin`); внутренние `refresh()`
  посреди разбора команды ревизию не двигают. В интерфейс `TacticsKernel`
  добавлен `getRevision()`. `session`: `getBattleRevision()` (0 вне боя,
  иначе ревизия ведущего ядра); `subscribeBattle` уже уведомлял об
  изменениях. Тесты: core `revision.test.ts` (6) — старт с нуля, рост на
  успешном `apply`/атаке, отсутствие роста на отклонённой команде и на
  запросах предпросмотра (`getHitPreview`/`getReachable`/`getPath`/
  `getVisibleCells`/`getSnapshotFor` — architecture §3.7), одно уведомление
  на изменение; session `battle-revision.test.ts` (4) — 0 вне боя, рост на
  команде, нет роста на предпросмотре, подписка уведомляет и отписывается.
  UI не тронут (день 12).
- [x] **День 12 — экран боя на ревизии (P1-1, часть 2).**
  Хук `useBattleRevision(session)` (hooks.ts) подписывает экран на изменения
  боя через `useSyncExternalStore`: у локального ведущего признак —
  ревизия ядра, у сетевого ведомого/наблюдателя — счётчик приходящих
  снимков (в сессии `netBattleRevision` растёт только на `SYNC_PAYLOAD`,
  а ответы предпросмотра и восстановление чекпоинта его не двигают).
  `BattleScreenView` больше не подписывается на `subscribeBattle` ради
  тика: снимок, `directiveView`, видимость и разведка поля вычисляются
  `useMemo` по `battleRevision` — снимок клонируется один раз на
  зафиксированное изменение боя, а не на каждый рендер. Пустой снимок —
  модульная константа `EMPTY_SNAPSHOT`; перерисовка после асинхронного
  монтажа средства отображения вынесена в отдельное состояние
  `rendererReady`. Подавления `exhaustive-deps`, ставшие ненужными
  (снимок, указание, видимость/разведка), сняты; остальные оставлены на
  поштучный разбор дня 13. Все 36 файлов UI-тестов (222 проверки, включая
  `prologue-chain`, `boot-saved`, `prologue-m2`) зелёные без правки тестов.
- [x] **День 13 — проверка эффекта и остаток подавлений (P1-1, часть 3).**
  Поштучный разбор оставшихся подавлений `exhaustive-deps`: мемоизации
  достижимых клеток, предпросмотра пути, попадания/навыка и зоны поражения
  переведены с полей снимка на `battleRevision` (в теле мемо — `void
  battleRevision;`, подавления сняты), эффект авто-завершения хода тоже
  ключ ревизией вместо `snapshot.turnNumber`. Подавлений в `ui/src` стало
  **9 → 5**, и все пять — только эффекты в `BattleScreenView` (таймер
  повтора, подписка на pvp-события, очередь подсказок кампании,
  одноразовый запуск фазы врага при монтаже, авто-завершение хода), у
  каждого в комментарии указана конкретная причина, а не общий маркер
  устаревания. Эффект проверен прямым замером клонирований снимка
  (счётчик в `cloneState` + DOM-тест «монтирование → пять наведений на
  клетки → завершение хода», инструментарий снят после замера):
  старый экран — 8 клонирований на монтирование, 9 на пять наведений,
  4 на завершение хода (**итого 21**); новый — 4 / 1 / 3 (**итого 8**);
  наведение на клетки больше не клонирует снимок и не пересчитывает
  путь. Все 36 файлов UI-тестов (222 проверки) зелёные без правки тестов.
- [x] **День 14 — таймер повтора (P1-3) и остаток P2.** Таймер повтора
  переведён на курсор в `useRef` (`replayIndexRef`): интервал создаётся
  один раз на журнал, темп воспроизведения ровный, зависимости эффекта
  полны — подавление `exhaustive-deps` снято (в `ui/src` их осталось
  **4**, все эффекты с поштучным обоснованием). Добавлен контрактный
  тест схемы против типа ядра (`session/tests/content-contract.test.ts`,
  живёт в тестах сессии — им разрешены импорты всех слоёв): двусторонняя
  присваиваемость `TrainingEnemyAction`/`TrainingEnemyScript` и
  сверка перечислений `kind`/`onlyIf` между zod-схемой контента и типом
  ядра; расхождение стало бы ошибкой компиляции (Major-9). Четыре прохода
  начала хода в блоке `END_TURN` ядра (яд, истечение срока жизни,
  перезарядки, снятие приказов/пополнение ОД, паника) сведены к одному
  id-сортированному списку `upcomingEntities` с сохранением порядка фаз и
  событий — правку прикрывает property-based тест детерминизма дня 10
  (227 проверок ядра зелёные). Проверка результата `write()` и
  `onQuotaExceeded` для повторов, лимиты `isSyncPayload`, запись журнала
  после `applied.ok` и дедупликация backend хранилища были закрыты ранее
  (дни 3–10) и перепроверены. UI: 222 проверки в 36 файлах зелёные.
- [ ] **День 15 — выпуск 0.21.0.** Прогон сборки и ручная проверка
  пролога, сохранений, повторов и сетевого боя; обновление этого раздела;
  повышение `no-floating-promises` до error, если список дня 8 закрыт.
- [ ] **Дни 16–18 — намерение игрока одним объектом (P1-2).** Тип
  `Intent` как размеченное объединение, чистая `nextIntent()` в
  `battle-intent.ts` с тестами до интеграции; производные значения как
  шаг миграции; семь `useState` заменены одним.
- [ ] **День 19 — первый слой из `field-renderer.ts`.** Вынос всплывающего
  текста в `render/src/float-text.ts` с тестом на вытеснение при
  переполнении — по рецепту уже вынесенных `camera.ts`/`fringe.ts`/
  `token-art.ts`.
- [ ] **День 20 — решение по числу участников (Major-7).** Честное
  ограничение двумя участниками: снять обещание N в комментарии ядра,
  зафиксировать в `network-protocol.md`; бэклог на следующий месяц.

Критерий «готово» подэтапа: все P0 закрыты, версии форматов (сохранения и
повторы) гарантируют совместимость, границы модулей и чистота ядра
проверяются машиной, тихие отказы ретранслятора, автосейва и повторов
устранены.

### 3.1-бис. Подэтап 5.4 — пролог кампании (выпущен в 0.20.x)

Нормативный сценарий — `doc/campaign.md` (зафиксирован в атласе документации).

- Восемь миссий пролога и два экрана базы; Акт I — без корабля, карты кампании и счётчика Тьмы, Акт II — с борта корабля.
- Новые виды противников: лесная крыса и слизень (записи в изолированном бестиарии пролога).
- Подкрепления противника во всех типах миссий, кроме зачистки (используются в М6, М7, М8) — единый сервис, декларативная конфигурация.
- Тип «разведка» получает цель-объект: победа — вскрыть цель и только после этого эвакуироваться.
- Система триггеров миссий, скриптовый канал forceHit/forceMiss (в том числе для сущности стороны игрока), менеджер одноразовых подсказок, чекпоинты и мгновенный рестарт, режиссёр камеры.
- Общий интерфейс объектов-целей (идол, сундук, клеть): «осмотреть / вскрыть / разрушить».
- Состояние кампании `chapter`: «prologue» → «open»; в прологе гибель бойца не окончательна (откат к чекпоинту), ранения и усталость не применяются, службы корабля и счётчик Тьмы скрыты до конца пролога.

Не входит: полноценная экономика усталости и расход трав (сразу после пролога), свободная карта кампании и службы корабля (после М8), звук, модификации.

### 3.2. Подэтап 5.5 — 0.22.0. Удобство и инструменты анализа

- Повторы: пауза, шаг команды, перемотка, выбор стороны, индикатор хода.
- Разбивка вероятности попадания в панели без лишних кликов (укрытие, высота, дальность).
- Предупреждение о дружественном огне area-умений.
- Предпросмотр высадки с учётом ранений и снаряжения.
- Повторный матч с тем же составом и зерном.
- Статистика после миссии: урон, попадания, криты.

Не входит: звук, модификации, «Игла».

### 3.3. Подэтап 5.6 — 0.23.0. Финал и исчерпание содержания

- Миссия «Игла», условия открытия, Змей Горыныч (одна сущность, три умения голов).
- Состязательные записи оставшихся противников; числа только в JSON5.
- Волхв в дружине на общих правилах рекрутов.
- Прохождение «от нуля до Иглы» и оба поражения — внутренняя проверка.
- Переформулировать туториал «Тьма и Игла», чтобы не намекать на уже доступную цель.

Не входит: косметика и модификации.

### 3.4. Подэтап 5.7 — 0.24.0. Доступность, звук, модификации

- Пониженная цветовая различимость, дублирование цвета формой.
- Полнота словарей содержания на ru/en; проверка добавления третьего языка.
- Звуковые отклики и настройки громкости.
- Подмена JSON5 пользователем с отказом по схеме.
- Установка PWA, автономная кампания, кэш оболочки.
- Сеть: адрес ретранслятора по умолчанию, проверка доступности, внятные ошибки.
- Наблюдатель в публичной сети (роль через ретранслятор).
- Инвентарь Кузни: разбор, экипировка из списка.
- Мини-карта/компас в бою.
- Случайный старт «Потешных боёв»: переключатель «случайный / выбирает Игрок 1».

Не входит: объявление 1.0.0.

### 3.5. Подэтап 5.8 — 1.0.0. Выпуск

Сквозная проверка по разделу 5; только блокирующие дефекты; журнал 1.0.0 и публикация адреса.

---

## 4. Бэклог (из обзоров, ещё не сделано)

Приоритеты: P1 ясность боя, P2 удобство кампании, P3 повторы и сеть, P4 полировка.

| Идея | P | Выпуск |
|---|---|---|
| Разбивка вероятности попадания без кликов | P1 | 0.22.0 |
| Подсветка союзников при дружественном огне area | P1 | 0.22.0 |
| Управление повтором (пауза, шаг, перемотка, сторона) | P3 | 0.22.0 |
| Предпросмотр высадки | P2 | 0.22.0 |
| Повторный матч с тем же составом и зерном | P2 | 0.22.0 |
| Статистика после миссии | P4 | 0.22.0 |
| Сортировка дружины и подсказка уровня класса | P2 | 0.22.0 |
| Текст туториала «Тьма и Игла» без ложного намёка | — | 0.23.0 |
| Адрес ретранслятора по умолчанию и внятные ошибки | P3 | 0.24.0 |
| Наблюдатель через ретранслятор | P3 | 0.24.0 |
| Звук и раздельная громкость | P4 | 0.24.0 |
| Разбор/экипировка в Кузне | P4 | 0.24.0 |
| Мини-карта/компас | P4 | 0.24.0 |
| Случайный старт «Потешных боёв» | P2 | 0.24.0 |

Принятые решения, уже отражённые в нормативе (не бэклог): «Быстрый матч» без туториала; отдельный режим обучения; онбординг кампании.

---

## 5. Критерии готовности 1.0.0

Выпуск разрешён только при всех пунктах сразу.

1. «Быстрый матч»: три роли, три типа рядовых, случайная карта, три трудности только по численности; туториала в режиме нет.
2. Обучение: три миссии (сетка, бой, умения); каждая доступна отдельно и доводится до итога.
3. Кампания от первой точки до «Иглы»; победа или одно из двух поражений; туториалы «первого раза» один раз и отключаются.
4. Дружина, окончательная гибель, ранения, Горница, Кузня, запасы, пять типов миссий, оба рядовых генерала и Горыныч.
5. Состязание: набор, уничтожение и вынос предмета.
6. Хотсит, локальная сеть, сеть общего пользования.
7. Наблюдатель подключается и не отдаёт команд.
8. Кампания, обучение и «Быстрый матч» без сети после установки.
9. Живая сущность — одна клетка.
10. Подмена конфигурации ограничена схемой.
11. Автопроверки ядер проходят; канал проверен на двух экземплярах.
