# Отчёт: замечания и предложения по исправлению (Bylina Tactics, 0.20.0)

## 1. Несоответствие версии в корневом манифесте

**Проблема**  
В `/app/package.json` указана версия `0.19.0`, хотя все остальные пакеты (`@bylina/*`) имеют версию `0.20.1`, а текущий этап разработки по `doc/roadmap.md` — **0.20.0** (подэтап 5.3 + исправления 0.20.1 / 0.20.2).

**Последствия**  
- Несогласованность при сборке, публикации и проверке CI.  
- `changelog-1.1.md` уже описывает правки 0.20.1 и 0.20.2, а корневой манифест отстаёт.

**Предложение**  
Обновить версию в `/app/package.json` до `0.20.2` (или `0.20.1`).

```json
"version": "0.20.2"
```

## 2. Отсутствие пакета `replay` в описании структуры

**Проблема**  
В `doc/tech-stack.md` и `doc/roadmap.md` (этап 5.4) упоминается функциональность повторов, однако в списке пакетов `doc/tech-stack.md` пакет `replay` не перечислен явно.

**Предложение**  
Добавить в `doc/tech-stack.md` строку:
> `replay` — хранение и воспроизведение логов команд (для повторов и отладки).

## 3. Устаревшая ссылка на improvements.md

**Проблема**  
В `doc/README.md` в таблице назначений указан документ `improvements.md`, однако в актуальной структуре репозитория он отсутствует (или не обновлён после 0.18.0).

**Предложение**  
Либо удалить строку из таблицы, либо создать/обновить документ `doc/improvements.md` с замечаниями по версии 0.20.0.

## 4. Несоответствие в описании этапа 5.3 (онбординг)

**Проблема**  
В `roadmap.md` (раздел 7.3) указано, что 0.20.0 — «Онбординг кампании (выполнен)». При этом в `changelog-1.1.md` подробно описаны правки 0.20.1 и 0.20.2 как часть того же подэтапа.

**Предложение**  
В `roadmap.md` явно указать:
> Подэтап 5.3 завершён в версии **0.20.2** (включая доводки 0.20.1 и 0.20.2).

## 5. Потенциальное отсутствие автоматической проверки словарей

**Проблема**  
В `changelog-1.1.md` (п. 60) говорится об «автоматической проверке полноты словарей». В структуре проекта (`packages/i18n`) такая проверка не обнаружена в скриптах `package.json`.

**Предложение**  
Добавить в `packages/i18n/package.json` скрипт:
```json
"check:i18n": "node scripts/check-dictionaries.js"
```
и интегрировать его в `typecheck` / CI.

## 6. Отсутствие явного признака версии в документации

**Проблема**  
Ни один документ в `doc/` не содержит явного поля «Текущая версия комплекта документации: 0.20.2».

**Предложение**  
Добавить в начало `doc/README.md` после заголовка:
```markdown
**Текущая версия комплекта:** 0.20.2  
**Соответствует версии приложения:** 0.20.2
```

## 7. Риск расхождения между `content` и `campaign`

**Проблема**  
Пакет `campaign` зависит от `content`, но в `campaign/package.json` отсутствует явная зависимость от `@bylina/content` (проверено косвенно через структуру).

**Предложение**  
Убедиться, что в `packages/campaign/package.json` присутствует:
```json
"dependencies": {
  "@bylina/content": "workspace:*"
}
```

## 8. Рекомендация по усилению проверок перед следующим этапом

**Предложения (приоритизация):**

1. Добавить в CI job `version-consistency` (сравнение версий всех `package.json`).
2. Создать smoke-тест запуска приложения с сохранённой кампанией (уже частично сделано в 0.20.2).
3. Добавить проверку наличия всех ключей словарей, используемых в обучении и онбординге.
4. Обновить `doc/tech-stack.md` с актуальным списком пакетов (добавить `replay`, `storage`, `settings`).

---

**Итог:**  
Основная проблема — **рассинхронизация версии** в корневом манифесте. Остальные замечания носит организационный и документационный характер и легко исправимы.

---

## 9. Анализ замечаний независимых экспертов

Все поступившие отчёты были проанализированы. Ниже перечислены **только подтверждённые проблемы**, которые действительно присутствуют в коде и документации проекта.

### 9.1. Подтверждённые расхождения документации и кода

| № | Проблема | Подтверждение | Предложение по исправлению |
|---|----------|---------------|----------------------------|
| 9.1.1 | Версия проекта рассинхронизирована | `app/package.json` = `0.19.0`, все `@bylina/*` = `0.20.1`, `APP_VERSION` = `0.20.1`, `changelog-1.1.md` описывает 0.20.2 | Привести все версии к единому значению **0.20.2** |
| 9.1.2 | Хранилище использует `localStorage` вместо `IndexedDB` | `packages/storage/src/index.ts` + `App.tsx` используют `localStorage`. `Dexie.js` отсутствует в зависимостях | Обновить `doc/tech-stack.md` или мигрировать на IndexedDB |
| 9.1.3 | Упоминание `bitECS` в документации | `doc/tech-core.md`, `doc/tech-stack.md` упоминают bitECS, но ядро (`kernel.ts`, `types.ts`) построено на POJO | Удалить упоминания bitECS из `tech-core.md` и `tech-stack.md` |
| 9.1.4 | Запрос `VISIBLE` описан, но не реализован | `network-protocol.md` описывает `QUERY VISIBLE`, код обрабатывает только `REACHABLE` и `HIT` | Удалить `VISIBLE` из документации |
| 9.1.5 | `debugMode` доступен через UI, но не описан | `debug-mode.md` упоминает только `?debug=1`, код имеет `settings.debugMode` | Дополнить `debug-mode.md` описанием настройки в UI |
| 9.1.6 | Устаревший заголовок документа | `doc/run-windows.md` содержит заголовок версии `0.12.0` | Обновить заголовки всех документов до `0.20.2` / `1.1` |
| 9.1.7 | `SyncPayload` не соответствует документации | `network-protocol.md` описывает `matchMeta`, `grid`, `entities`, `seen`; код отправляет `{ match, visible, explored }` | Привести `SyncPayload` к единому формату |

### 9.2. Подтверждённые проблемы производительности и надёжности

| № | Проблема | Подтверждение | Предложение по исправлению |
|---|----------|---------------|----------------------------|
| 9.2.1 | Полная перерисовка рельефа при каждом обновлении | `field-renderer.ts`: `paintStatic()` вызывается при `staticDirty = true` на каждое действие | Кэшировать рельеф в отдельный `Container`, обновлять только при смене карты |
| 9.2.2 | Автосохранение блокирует главный поток | `storage/src/index.ts` + `App.tsx`: `JSON.stringify` + `localStorage.setItem` каждые 400 мс | Перенести сериализацию в Web Worker |
| 9.2.3 | Отсутствует Heartbeat в signaling-сервере | `signaling-server/src/server.mjs` не использует `ping/pong` | Добавить периодический `ping` и очистку неактивных сокетов |
| 9.2.4 | Софт-лок в режиме обучения при нехватке ОД | Логика `shouldAutoEndTurn` может завершать ход на шаге, требующем действия | Запретить авто-завершение хода, если шаг требует конкретного действия |

### 9.3. Подтверждённые архитектурные и сетевые проблемы

| № | Проблема | Подтверждение | Предложение по исправлению |
|---|----------|---------------|----------------------------|
| 9.3.1 | Отсутствует обработчик `ROLE_CHANGED` | Сервер отправляет `ROLE_CHANGED`, клиент (`createSignalingSession`) его не обрабатывает | Реализовать host migration или удалить `ROLE_CHANGED` из протокола |
| 9.3.2 | Отсутствует реальное переподключение | UI показывает «Reconnecting», но `onClose` только уведомляет, новый WebSocket не создаётся | Реализовать машину состояний reconnect или изменить текст UI |
| 9.3.3 | `SIGNAL` рассылается всем участникам комнаты | Сервер ретранслирует `SIGNAL` broadcast'ом вместо адресации `to` | Сделать `SIGNAL` адресным (`to`, `from`) |
| 9.3.4 | `BattleScreen.tsx` — God Component (>1500 строк) | Смешаны рендер, ввод, обучение, сеть, replay, кампания | Вынести логику в кастомные хуки (`useBattleNetwork`, `useTrainingFlow`) |
| 9.3.5 | Весь контент бандлится в главный чанк | `content-files.ts` использует `import.meta.glob(..., { eager: true })` | Перейти на динамическую загрузку через `fetch` / lazy loading |

### 9.4. Подтверждённые проблемы валидации и API

| № | Проблема | Подтверждение | Предложение по исправлению |
|---|----------|---------------|----------------------------|
| 9.4.1 | `confirmDeployment` допускает дублирование `fighterId` | Проверка уникальности отсутствует в Session API | Добавить `new Set(fighterIds).size === fighterIds.length` |
| 9.4.2 | Дублирование валидации действий | Часть проверок дублируется в UI (`trainingActionAllowed`) и ядре | UI должен полагаться только на `getHitPreview` / `getReachable` ядра |

### 9.5. Рекомендации по приоритизации исправлений (подтверждённые)

**🔴 Критический приоритет**
- Привести все версии к `0.20.2`
- Исправить `paintStatic` (кэширование рельефа)
- Исправить софт-лок в обучении
- Реализовать/удалить host migration (`ROLE_CHANGED`)

**🟡 Высокий приоритет**
- Мигрировать хранилище на Web Worker / IndexedDB
- Добавить Heartbeat в signaling server
- Сделать `SIGNAL` адресным
- Обновить `SyncPayload` до единого контракта
- Декомпозировать `BattleScreen.tsx`

**🟢 Средний приоритет**
- Удалить упоминания bitECS из документации
- Актуализировать `debug-mode.md` и `ui-design.md`
- Добавить проверку уникальности fighter ID
- Унифицировать валидацию действий

---

**Итоговый вывод по экспертным отчётам**  
Большинство замечаний экспертов подтверждены. Главные риски проекта находятся в трёх областях:

1. **Сетевой слой** (multi-peer, reconnect, host migration, signaling)
2. **Версионирование и документация**
3. **Производительность рендера и сохранения**

Рекомендуется сначала закрыть критические пункты из разделов 9.1–9.3 перед следующим этапом разработки.
