/**
 * Версия приложения (0.20.54): единственный источник — корневой манифест
 * `app/package.json`. Прежде номер был прописан в пятнадцати манифестах,
 * трёх константах и четырёх тестах: каждое увеличение требовало правки
 * двадцати трёх мест и легко расходилось. Теперь номер объявлен один раз,
 * а код читает его оттуда — ни один исходник не содержит литерала версии.
 */

import manifest from "../../../package.json";

/** Версия приложения из корневого манифеста. */
export const APP_VERSION: string = manifest.version;
