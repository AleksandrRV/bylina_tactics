From 2508dfdea70ca99f1a46149bfc2456971e5e21e6 Mon Sep 17 00:00:00 2001
From: Aleksandr Romanov <alex.romanov@bit.games>
Date: Wed, 26 Aug 2026 10:09:17 +0700
Subject: [PATCH] Visual Improvement AI AGENT

(cherry picked from commit 367e7968827021291cea086dd537a57fd434090d)
---
 app/README.md                                 |    2 +-
 app/apps/game-pwa/package.json                |    4 +-
 app/apps/game-pwa/src/App.tsx                 |    2 +-
 app/apps/game-pwa/src/main.tsx                |    4 +
 app/apps/game-pwa/src/styles.css              |   50 +-
 app/apps/signaling-server/README.md           |    2 +-
 app/apps/signaling-server/package.json        |    2 +-
 app/package.json                              |    9 +-
 app/packages/campaign/package.json            |    2 +-
 app/packages/campaign/src/index.ts            |   24 +-
 app/packages/campaign/tests/campaign.test.ts  |   16 +
 app/packages/content/data/campaign.json5      |    8 +
 app/packages/content/package.json             |    2 +-
 app/packages/content/src/schemas.ts           |    4 +
 app/packages/content/tests/content.test.ts    |   14 +
 app/packages/core/package.json                |    2 +-
 app/packages/core/src/index.ts                |    2 +-
 app/packages/core/src/kernel.ts               |   14 +-
 app/packages/core/src/mapgen.ts               |    4 +
 app/packages/core/src/types.ts                |    6 +-
 app/packages/core/tests/core.test.ts          |    4 +-
 app/packages/core/tests/grid.test.ts          |    4 +-
 app/packages/i18n/locales/en/ui.json          |    9 +-
 app/packages/i18n/locales/ru/ui.json          |    9 +-
 app/packages/i18n/package.json                |    2 +-
 app/packages/net/package.json                 |    2 +-
 app/packages/render/README.md                 |   18 +
 app/packages/render/package.json              |    2 +-
 app/packages/render/src/camera.ts             |   61 +-
 app/packages/render/src/colors.ts             |  173 +++
 app/packages/render/src/field-renderer.ts     | 1231 +++++++++++++----
 app/packages/render/src/index.ts              |    5 +
 app/packages/render/tests/camera.test.ts      |   23 +
 app/packages/render/tests/render.test.ts      |   18 +-
 app/packages/replay/package.json              |    2 +-
 app/packages/replay/src/index.ts              |    2 +-
 app/packages/replay/tests/replay.test.ts      |    4 +-
 app/packages/session/package.json             |    2 +-
 app/packages/session/src/index.ts             |    2 +-
 app/packages/session/tests/session.test.ts    |   12 +-
 app/packages/settings/package.json            |    2 +-
 app/packages/settings/src/index.ts            |    5 +
 app/packages/settings/tests/settings.test.ts  |    5 +
 app/packages/signaling/package.json           |    2 +-
 app/packages/storage/package.json             |    2 +-
 app/packages/ui/package.json                  |    2 +-
 app/packages/ui/src/BattleScreenView.tsx      |  284 +++-
 app/packages/ui/src/CampaignScreen.tsx        |  166 ++-
 app/packages/ui/src/SettingsScreen.tsx        |   17 +
 app/packages/ui/src/battle.css                |  454 +++++-
 app/packages/ui/src/campaign.css              |  332 ++++-
 app/packages/ui/src/hooks.ts                  |   23 +
 app/packages/ui/src/training-progress.ts      |    3 +
 app/packages/ui/tests/boot-saved.test.tsx     |    2 +
 .../ui/tests/training-battle-dom.test.tsx     |    4 +
 .../ui/tests/training-progress.test.ts        |    2 +
 .../ui/tests/training-ui-rules.test.ts        |    1 +
 app/pnpm-lock.yaml                            |    2 +-
 app/scripts/check-visual-regression.mjs       |  147 ++
 app/scripts/visual-audit.mjs                  |  118 ++
 app/visual-baselines/states.json              |  133 ++
 doc/README.md                                 |    9 +-
 doc/content-schema.md                         |    3 +
 doc/operations.md                             |    8 +-
 doc/roadmap.md                                |    4 +-
 doc/ui-design.md                              |   92 +-
 66 files changed, 3098 insertions(+), 482 deletions(-)
 create mode 100644 app/packages/render/src/colors.ts
 create mode 100644 app/scripts/check-visual-regression.mjs
 create mode 100644 app/scripts/visual-audit.mjs
 create mode 100644 app/visual-baselines/states.json

diff --git a/app/README.md b/app/README.md
index 1866c2d..76d6131 100644
--- a/app/README.md
+++ b/app/README.md
@@ -1,6 +1,6 @@
 # Былина: Тьма Кощея
 
-Пошаговая тактическая игра. Версия **0.20.19**: тактический слой (умения классов, состояния, призывы, туман войны, дозор, разрушаемая среда), режим обучения из трёх миссий со свободным выбором и строгим пошаговым сценарием (каждое действие игрока и ход Нави предопределены и подсвечены, камера сама подводит цель шага), онбординг кампании и генералы; продолжение былины — через акцентную кнопку «Продолжить» в главном меню.
+Пошаговая тактическая игра. Версия **0.21.2**: тактический слой (умения классов, состояния, призывы, туман войны, дозор, разрушаемая среда), режим обучения из трёх миссий со свободным выбором и строгим пошаговым сценарием (каждое действие игрока и ход Нави предопределены и подсвечены, камера сама подводит цель шага), онбординг кампании и генералы, четыре биома поля с атмосферным туманом и нарастающей Тьмой; продолжение былины — через акцентную кнопку «Продолжить» в главном меню.
 
 Исходный код и сборка живут в этом каталоге. Карта документации и репозитория — [`../doc/project-map.md`](../doc/project-map.md). Нормативный комплект — [`../doc/`](../doc/README.md).
 
diff --git a/app/apps/game-pwa/package.json b/app/apps/game-pwa/package.json
index a88bdaf..1042fa5 100644
--- a/app/apps/game-pwa/package.json
+++ b/app/apps/game-pwa/package.json
@@ -1,6 +1,6 @@
 {
   "name": "@bylina/game-pwa",
-  "version": "0.20.19",
+  "version": "0.21.2",
   "private": true,
   "type": "module",
   "scripts": {
@@ -33,6 +33,6 @@
     "@vitejs/plugin-react": "^4.3.4",
     "typescript": "^5.6.3",
     "vite": "^5.4.11",
-    "vite-plugin-pwa": "^0.21.1"
+    "vite-plugin-pwa": "^0.21.2"
   }
 }
diff --git a/app/apps/game-pwa/src/App.tsx b/app/apps/game-pwa/src/App.tsx
index ae90f71..bd3fe44 100644
--- a/app/apps/game-pwa/src/App.tsx
+++ b/app/apps/game-pwa/src/App.tsx
@@ -332,7 +332,7 @@ export function App() {
     let lastSave = 0;
     // Трейлинг-дебаунс: частые изменения боя не пишутся чаще раза в окно,
     // но изменение, попавшее в окно, НЕ отбрасывается, а откладывается до его
-    // закрытия — последний ход всегда будет сохранён (исправление 0.20.20).
+    // закрытия — последний ход всегда будет сохранён (исправление 0.21.2).
     const debouncedPersist = (): void => {
       const now = Date.now();
       if (now - lastSave >= 400) {
diff --git a/app/apps/game-pwa/src/main.tsx b/app/apps/game-pwa/src/main.tsx
index af7a9dc..b3914d9 100644
--- a/app/apps/game-pwa/src/main.tsx
+++ b/app/apps/game-pwa/src/main.tsx
@@ -1,10 +1,14 @@
 import { StrictMode } from "react";
 import { createRoot } from "react-dom/client";
+import { applyRenderColorVariables } from "@bylina/render";
 import { App } from "./App.js";
 import "../../../packages/ui/src/battle.css";
 import "../../../packages/ui/src/campaign.css";
 import "./styles.css";
 
+// CSS и PixiJS получают цветовые значения из одного справочника render.
+applyRenderColorVariables();
+
 const root = document.getElementById("root");
 if (!root) {
   throw new Error("root element is missing");
diff --git a/app/apps/game-pwa/src/styles.css b/app/apps/game-pwa/src/styles.css
index 82a8f10..c67429a 100644
--- a/app/apps/game-pwa/src/styles.css
+++ b/app/apps/game-pwa/src/styles.css
@@ -1,14 +1,11 @@
 :root {
   color-scheme: dark;
-  --ink: #14181c;
-  --ink-2: #1c232a;
-  --ink-3: #262f38;
-  --line: #3a4550;
-  --mist: #d5cfc0;
-  --mist-dim: #9a9384;
-  --amber: #e0b34a;
-  --amber-dim: #8a6a24;
+  /* Цветовые custom properties устанавливаются из @bylina/render до
+     первого рендера. PixiJS и CSS поэтому используют один справочник. */
   --shadow: 0 18px 50px rgba(0, 0, 0, 0.35);
+  /* Единый ритм появления модалок, плашек и баннеров (Этап 4.10). */
+  --ui-motion-duration: 300ms;
+  --ui-motion-ease: cubic-bezier(0.22, 1, 0.36, 1);
   font-family: "Segoe UI", "PT Sans", "Noto Sans", system-ui, sans-serif;
   font-size: 17px;
   line-height: 1.45;
@@ -42,7 +39,7 @@ body {
       transparent 0 27px,
       rgba(224, 179, 74, 0.035) 27px 28px
     ),
-    radial-gradient(1200px 600px at 50% -10%, #2a333c, var(--ink));
+    radial-gradient(1200px 600px at 50% -10%, var(--backdrop-highlight), var(--ink));
 }
 
 button,
@@ -77,11 +74,30 @@ input {
 .display-title {
   margin: 0;
   font-family: Palatino, "Palatino Linotype", "Iowan Old Style", "Times New Roman", serif;
-  font-weight: 600;
+  font-weight: 700;
   font-size: clamp(2.6rem, 10vw, 4rem);
-  letter-spacing: 0.04em;
+  letter-spacing: 0.055em;
   line-height: 0.95;
-  color: #f3ecdc;
+  color: var(--white);
+  text-wrap: balance;
+  font-kerning: normal;
+  text-shadow: 0 1px 0 rgba(8, 10, 12, 0.9), 0 0 1px rgba(255, 242, 196, 0.35);
+}
+
+/* 4.11: типографическая обработка системного serif вместо внешнего шрифта.
+   Так мы не встраиваем непроверенный файл, сохраняем кириллицу и применяем
+   display-акцент только к крупным заголовкам, не ухудшая плотность HUD. */
+.campaign-title-block h1,
+.settings-head h1,
+.mission-title,
+.pause-card h2,
+.modal h2 {
+  font-family: Palatino, "Palatino Linotype", "Iowan Old Style", "Times New Roman", serif;
+  font-weight: 700;
+  letter-spacing: 0.045em;
+  text-wrap: balance;
+  font-kerning: normal;
+  text-shadow: 0 1px 0 rgba(8, 10, 12, 0.9), 0 0 1px rgba(255, 242, 196, 0.3);
 }
 
 .muted {
@@ -157,7 +173,7 @@ input {
 }
 
 .btn-primary {
-  background: linear-gradient(180deg, #222a32, #1a2026);
+  background: linear-gradient(180deg, var(--primary-surface-top), var(--primary-surface-bottom));
 }
 
 .btn-ghost {
@@ -184,7 +200,7 @@ input {
   margin: 0;
   font-family: Palatino, "Palatino Linotype", "Iowan Old Style", serif;
   font-size: 2.2rem;
-  color: #f3ecdc;
+  color: var(--white);
 }
 
 .panel {
@@ -248,7 +264,7 @@ input {
   width: 44px;
   height: 24px;
   border-radius: 12px;
-  border: 1px solid #3a4550;
+  border: 1px solid var(--line);
   background: rgba(28, 35, 32, 0.9);
   position: relative;
   cursor: pointer;
@@ -275,7 +291,7 @@ input {
 
 .toggle-row input[type="checkbox"]:checked::after {
   transform: translateX(20px);
-  background: var(--amber, #e0b34a);
+  background: var(--amber);
 }
 
 .volume-value {
@@ -309,7 +325,7 @@ input[type="range"] {
 .modal h2 {
   margin: 0 0 10px;
   font-family: Palatino, "Palatino Linotype", "Iowan Old Style", serif;
-  color: #f3ecdc;
+  color: var(--white);
 }
 
 .modal .btn {
diff --git a/app/apps/signaling-server/README.md b/app/apps/signaling-server/README.md
index 9f63b10..dd968b1 100644
--- a/app/apps/signaling-server/README.md
+++ b/app/apps/signaling-server/README.md
@@ -1,6 +1,6 @@
 # Ретранслятор установления соединения
 
-Компонент добавлен в версии 0.17.0 (roadmap §6.4); текущая поставка — 0.20.19. Сервер знакомит участников состязательного
+Компонент добавлен в версии 0.17.0 (roadmap §6.4); текущая поставка — 0.21.2. Сервер знакомит участников состязательного
 режима в сети общего пользования: перечень комнат и обмен описаниями сессии
 WebRTC. **Правила боя сервер не исполняет.**
 
diff --git a/app/apps/signaling-server/package.json b/app/apps/signaling-server/package.json
index d53a129..8091871 100644
--- a/app/apps/signaling-server/package.json
+++ b/app/apps/signaling-server/package.json
@@ -1,6 +1,6 @@
 {
   "name": "@bylina/signaling-server",
-  "version": "0.20.19",
+  "version": "0.21.2",
   "private": true,
   "type": "module",
   "description": "Ретранслятор установления соединения (сеть общего пользования). Не исполняет игровые правила.",
diff --git a/app/package.json b/app/package.json
index 151da0d..a21cf58 100644
--- a/app/package.json
+++ b/app/package.json
@@ -1,15 +1,20 @@
 {
   "name": "bylina",
   "private": true,
-  "version": "0.20.19",
+  "version": "0.21.2",
   "packageManager": "pnpm@10.34.5",
   "scripts": {
     "dev": "pnpm --filter @bylina/game-pwa dev",
+    "visual:regression": "node scripts/check-visual-regression.mjs",
+    "visual:regression:update": "node scripts/check-visual-regression.mjs --update",
+    "audit:visual": "node scripts/visual-audit.mjs",
+    "prebuild": "pnpm visual:regression && pnpm audit:visual",
+    "pretest": "pnpm visual:regression && pnpm audit:visual",
     "build": "pnpm --filter @bylina/game-pwa build",
     "preview": "pnpm --filter @bylina/game-pwa preview --host 0.0.0.0",
     "test": "pnpm -r --if-present test",
     "typecheck": "pnpm -r --if-present typecheck",
-    "check:versions": "node scripts/check-version-consistency.mjs"
+    "check:versions": "node scripts/check-version-consistency.mjs && pnpm visual:regression && pnpm audit:visual"
   },
   "engines": {
     "node": ">=20"
diff --git a/app/packages/campaign/package.json b/app/packages/campaign/package.json
index 0ebeb08..e4bac37 100644
--- a/app/packages/campaign/package.json
+++ b/app/packages/campaign/package.json
@@ -1,6 +1,6 @@
 {
   "name": "@bylina/campaign",
-  "version": "0.20.19",
+  "version": "0.21.2",
   "type": "module",
   "exports": {
     ".": "./src/index.ts"
diff --git a/app/packages/campaign/src/index.ts b/app/packages/campaign/src/index.ts
index 02aee1f..847cdb1 100644
--- a/app/packages/campaign/src/index.ts
+++ b/app/packages/campaign/src/index.ts
@@ -77,6 +77,14 @@ export interface ScanResult {
   opened: string[];
 }
 
+/** Визуальное событие перелёта корабля, не влияющее на правила кампании. */
+export interface ShipFlight {
+  /** Уникальный номер события в жизни автомата. */
+  id: number;
+  from: { x: number; y: number };
+  to: { x: number; y: number };
+}
+
 export interface CampaignState {
   darkness: number;
   darknessMax: number;
@@ -134,6 +142,8 @@ export interface CampaignApi {
   abandonMission(): void;
   /** Сканирование окрестности корабля: открывает точки в радиусе за стоимость. */
   scan(): ScanResult | null;
+  /** Последний перелёт корабля для анимации карты; это не часть сохранения. */
+  getLastFlight(): ShipFlight | null;
   /** Изготовить предмет в Кузне (один экземпляр каждой записи). */
   craftItem(itemId: string): boolean;
   /** Надеть предмет на бойца; `null` снимает снаряжение. */
@@ -257,6 +267,11 @@ export function createCampaign(config: CampaignConfig, options: CampaignOptions
       }
     : freshState;
   const listeners = new Set<() => void>();
+  // Полёт — одноразовое событие представления. Оно намеренно не входит в
+  // CampaignState и не сериализуется: загрузка сохранения не должна повторно
+  // запускать старую анимацию и не меняет правила кампании.
+  let flightSequence = 0;
+  let lastFlight: ShipFlight | null = null;
 
   // Снимок является публичным значением API: вложенные объекты тоже должны
   // быть независимыми от автомата. Иначе вызывающий код мог изменить
@@ -309,6 +324,9 @@ export function createCampaign(config: CampaignConfig, options: CampaignOptions
       lastResult: cloneLastResult(state.lastResult),
     }),
     getMissions: () => missions.map((mission) => ({ ...mission })),
+    getLastFlight: () => lastFlight
+      ? { id: lastFlight.id, from: { ...lastFlight.from }, to: { ...lastFlight.to } }
+      : null,
     getMission: (id) => missions.find((mission) => mission.id === id),
     getItems: () => items.map((item) => ({ ...item, cost: { ...item.cost } })),
     getDeployLimits: () => ({ min: config.deployMin, max: config.deployMax }),
@@ -366,7 +384,11 @@ export function createCampaign(config: CampaignConfig, options: CampaignOptions
         if (!state.deadGenerals.includes(generalId)) state.deadGenerals.push(generalId);
       }
       // Корабль перелетает к завершённой точке; дальнейшие точки открываются сканированием.
-      state.shipPosition = { x: mission.x, y: mission.y };
+      // Координаты перелёта — часть представления, а не новая механика кампании.
+      const from = { ...state.shipPosition };
+      const to = { x: mission.x, y: mission.y };
+      state.shipPosition = to;
+      lastFlight = { id: flightSequence++, from, to };
 
       let newRecruit: string | null = null;
       if (outcome === "victory" && livingCount() > 0 && state.fighters.length < config.rosterCap) {
diff --git a/app/packages/campaign/tests/campaign.test.ts b/app/packages/campaign/tests/campaign.test.ts
index d970fe8..9493da7 100644
--- a/app/packages/campaign/tests/campaign.test.ts
+++ b/app/packages/campaign/tests/campaign.test.ts
@@ -119,6 +119,22 @@ describe("createCampaign: points and darkness", () => {
     ]);
   });
 
+  it("exposes the presentation-only flight from the previous ship position", () => {
+    const automaton = campaign();
+    const fighters = automaton.getState().fighters.map((fighter) => fighter.id);
+    expect(automaton.getLastFlight()).toBeNull();
+    automaton.startMission("clearing_1");
+    automaton.finishMission("clearing_1", "victory", fighters.map((fighterId) => ({ fighterId, survived: true, hp: 10 })));
+    expect(automaton.getLastFlight()).toEqual({
+      id: 0,
+      from: { x: 20, y: 50 },
+      to: { x: 20, y: 50 },
+    });
+    const flight = automaton.getLastFlight();
+    if (flight) flight.from.x = 99;
+    expect(automaton.getLastFlight()?.from.x).toBe(20);
+  });
+
   it("defeat adds more darkness but does not end the campaign", () => {
     const automaton = campaign();
     const fighters = automaton.getState().fighters.map((fighter) => fighter.id);
diff --git a/app/packages/content/data/campaign.json5 b/app/packages/content/data/campaign.json5
index 47519fd..5c1d23d 100644
--- a/app/packages/content/data/campaign.json5
+++ b/app/packages/content/data/campaign.json5
@@ -46,6 +46,7 @@
         edgeCoverChance: 0.4,
         halfCoverChance: 0.55,
         heightMix: { z0: 0.1, z1: 0.8, z2: 0.1 },
+        biome: "meadow",
       },
       enemies: [{ unitId: "upyr", count: 3 }],
     },
@@ -67,6 +68,7 @@
         edgeCoverChance: 0.45,
         halfCoverChance: 0.5,
         heightMix: { z0: 0.15, z1: 0.7, z2: 0.15 },
+        biome: "thicket",
       },
       enemies: [
         { unitId: "upyr", count: 2 },
@@ -91,6 +93,7 @@
         edgeCoverChance: 0.45,
         halfCoverChance: 0.5,
         heightMix: { z0: 0.15, z1: 0.7, z2: 0.15 },
+        biome: "swamp",
       },
       enemies: [
         { unitId: "upyr", count: 3 },
@@ -115,6 +118,7 @@
         edgeCoverChance: 0.45,
         halfCoverChance: 0.5,
         heightMix: { z0: 0.15, z1: 0.7, z2: 0.15 },
+        biome: "scorched",
       },
       objectiveUnitId: "idol",
       enemies: [
@@ -140,6 +144,7 @@
         edgeCoverChance: 0.4,
         halfCoverChance: 0.5,
         heightMix: { z0: 0.1, z1: 0.8, z2: 0.1 },
+        biome: "meadow",
         extract: true,
       },
       escorteeUnitId: "captive",
@@ -167,6 +172,7 @@
         edgeCoverChance: 0.45,
         halfCoverChance: 0.5,
         heightMix: { z0: 0.15, z1: 0.7, z2: 0.15 },
+        biome: "thicket",
         extract: true,
       },
       enemies: [
@@ -193,6 +199,7 @@
         edgeCoverChance: 0.5,
         halfCoverChance: 0.5,
         heightMix: { z0: 0.2, z1: 0.6, z2: 0.2 },
+        biome: "scorched",
       },
       enemies: [
         { unitId: "upyr", count: 2 },
@@ -220,6 +227,7 @@
         edgeCoverChance: 0.5,
         halfCoverChance: 0.45,
         heightMix: { z0: 0.2, z1: 0.6, z2: 0.2 },
+        biome: "thicket",
       },
       enemies: [
         { unitId: "upyr", count: 4 },
diff --git a/app/packages/content/package.json b/app/packages/content/package.json
index e633366..e329c00 100644
--- a/app/packages/content/package.json
+++ b/app/packages/content/package.json
@@ -1,6 +1,6 @@
 {
   "name": "@bylina/content",
-  "version": "0.20.19",
+  "version": "0.21.2",
   "type": "module",
   "exports": {
     ".": "./src/index.ts"
diff --git a/app/packages/content/src/schemas.ts b/app/packages/content/src/schemas.ts
index b426aef..763e7e9 100644
--- a/app/packages/content/src/schemas.ts
+++ b/app/packages/content/src/schemas.ts
@@ -129,7 +129,11 @@ export const skillConfigSchema = z.object({
   });
 });
 
+export const biomeIdSchema = z.enum(["meadow", "swamp", "thicket", "scorched"]);
+
 export const mapGenConfigSchema = z.object({
+  /** Визуальный биом; отсутствует в старых конфигурациях и означает meadow. */
+  biome: biomeIdSchema.optional(),
   width: z.number().int().min(8).max(64),
   height: z.number().int().min(8).max(64),
   pitChance: z.number().min(0).max(1),
diff --git a/app/packages/content/tests/content.test.ts b/app/packages/content/tests/content.test.ts
index c35263c..9308e33 100644
--- a/app/packages/content/tests/content.test.ts
+++ b/app/packages/content/tests/content.test.ts
@@ -122,6 +122,20 @@ describe("parseContent", () => {
     expect(result.data.quickMatch.playerSlots).toEqual(["bogatyr", "strelets", "znaharka"]);
     expect(result.data.quickMatch.difficulties).toHaveLength(3);
     expect(result.data.campaign.needleMissionId).toBe("needle");
+    expect(result.data.campaign.missions.map((mission) => mission.map.biome)).toEqual([
+      "meadow", "thicket", "swamp", "scorched", "meadow", "thicket", "scorched", "thicket",
+    ]);
+  });
+
+  it("accepts legacy maps without a biome", () => {
+    const files = readDataTree();
+    const campaignKey = Object.keys(files).find((key) => key.endsWith("campaign.json5"));
+    expect(campaignKey).toBeDefined();
+    if (!campaignKey) return;
+    files[campaignKey] = files[campaignKey]!.replace(/^\s*biome:.*\n/gm, "");
+    const result = parseContent(files);
+    expect(result.ok).toBe(true);
+    if (result.ok) expect(result.data.campaign.missions.every((mission) => mission.map.biome === undefined)).toBe(true);
   });
 
   it("rejects unknown fields and broken references", () => {
diff --git a/app/packages/core/package.json b/app/packages/core/package.json
index b770f15..0252338 100644
--- a/app/packages/core/package.json
+++ b/app/packages/core/package.json
@@ -1,6 +1,6 @@
 {
   "name": "@bylina/core",
-  "version": "0.20.19",
+  "version": "0.21.2",
   "type": "module",
   "exports": {
     ".": "./src/index.ts"
diff --git a/app/packages/core/src/index.ts b/app/packages/core/src/index.ts
index 0e38e9a..42b0161 100644
--- a/app/packages/core/src/index.ts
+++ b/app/packages/core/src/index.ts
@@ -22,7 +22,7 @@ export {
 } from "./defaults.js";
 export type { SpawnUnitConfig } from "./defaults.js";
 export { QUICK_MATCH_MAP, enemySpawns, generateBattlefield, playerSpawns } from "./mapgen.js";
-export type { MapGenConfig, SpawnPoint } from "./mapgen.js";
+export type { BiomeId, MapGenConfig, SpawnPoint } from "./mapgen.js";
 export { createMissionMatch, createPvpMatch, createQuickMatch } from "./match.js";
 export type { MissionMatchOptions, PvpMatchOptions, QuickMatchOptions, RosterMods, RosterSlot } from "./match.js";
 export { isCombatant, livingOf, matchOutcome } from "./outcome.js";
diff --git a/app/packages/core/src/kernel.ts b/app/packages/core/src/kernel.ts
index 84b35b4..506a203 100644
--- a/app/packages/core/src/kernel.ts
+++ b/app/packages/core/src/kernel.ts
@@ -22,7 +22,13 @@ import type {
 } from "./types.js";
 import { defaultWeapons, type WeaponStats } from "./weapons.js";
 
-export const CORE_VERSION = "0.20.19";
+export const CORE_VERSION = "0.21.2";
+
+/** Presentation data is derived by the rules layer; the renderer never invents a global shake force. */
+function impactShake(result: string, actionType: string, damage: number): number {
+  const base = result === "MISS" ? 0.8 : 1 + damage * 0.65 + (result === "CRIT" ? 1.5 : 0);
+  return Math.min(7, base + (actionType === "MELEE" ? 1 : 0));
+}
 
 export interface KernelOptions {
   initial?: MatchState;
@@ -326,6 +332,7 @@ export function createTacticsKernel(options: KernelOptions = {}): TacticsKernel
   };
 
   const damageCover = (cover: EntityState, events: GameEvent[]): void => {
+    const previousCoverType = cover.coverType;
     cover.coverType = Math.max(0, cover.coverType - 1) as 0 | 1 | 2;
     if (cover.coverType === 0) {
       cover.obstacle = false;
@@ -335,6 +342,7 @@ export function createTacticsKernel(options: KernelOptions = {}): TacticsKernel
       type: "COVER_DESTROYED",
       gridPos: cellPos(cover),
       newStatus: cover.coverType === 0 ? "NONE" : "HALF",
+      shake: previousCoverType === 2 ? 5 : 3,
     });
   };
 
@@ -647,6 +655,7 @@ export function createTacticsKernel(options: KernelOptions = {}): TacticsKernel
         damageDealt: resolved.damage,
         isFlanked: resolved.flanked,
         heightMod: resolved.heightMod,
+        shake: resolved.result === "MISS" ? 0 : impactShake(resolved.result, resolved.actionType, resolved.damage),
       });
       applyDamage(mover, resolved.damage, events);
     }
@@ -695,7 +704,7 @@ export function createTacticsKernel(options: KernelOptions = {}): TacticsKernel
     target.y = y;
     target.z = tile.z;
     const fall = tile.pit && !target.flying;
-    events.push({ type: "ENTITY_DISPLACED", entityId: target.id, from, to: cellPos(target), cause: fall ? "FALL" : "KNOCKBACK" });
+    events.push({ type: "ENTITY_DISPLACED", entityId: target.id, from, to: cellPos(target), cause: fall ? "FALL" : "KNOCKBACK", shake: fall ? 4 : 3 });
     refresh();
     revealAdjacent(events);
     if (fall) {
@@ -980,6 +989,7 @@ export function createTacticsKernel(options: KernelOptions = {}): TacticsKernel
       damageDealt: resolved.damage,
       isFlanked: resolved.flanked,
       heightMod: resolved.heightMod,
+      shake: resolved.result === "MISS" ? 0 : impactShake(resolved.result, resolved.actionType, resolved.damage),
     });
     applyDamage(target, resolved.damage, events);
     return resolved.result !== "MISS";
diff --git a/app/packages/core/src/mapgen.ts b/app/packages/core/src/mapgen.ts
index 2614cf5..1c52ad4 100644
--- a/app/packages/core/src/mapgen.ts
+++ b/app/packages/core/src/mapgen.ts
@@ -3,7 +3,11 @@ import { findPath } from "./pathfinding.js";
 import type { Rng } from "./rng.js";
 import type { EntityState, Grid, Tile } from "./types.js";
 
+export type BiomeId = "meadow" | "swamp" | "thicket" | "scorched";
+
 export interface MapGenConfig {
+  /** Visual-only biome; omitted by legacy maps and treated as meadow. */
+  biome?: BiomeId;
   width: number;
   height: number;
   pitChance: number;
diff --git a/app/packages/core/src/types.ts b/app/packages/core/src/types.ts
index 84c9109..2879655 100644
--- a/app/packages/core/src/types.ts
+++ b/app/packages/core/src/types.ts
@@ -150,7 +150,7 @@ export type GameEvent =
       isDash: boolean;
       apSpent: number;
     }
-  | { type: "ENTITY_DISPLACED"; entityId: number; from: CellPos; to: CellPos; cause: "KNOCKBACK" | "TELEPORT" | "FALL" }
+  | { type: "ENTITY_DISPLACED"; entityId: number; from: CellPos; to: CellPos; cause: "KNOCKBACK" | "TELEPORT" | "FALL"; shake?: number }
   | { type: "STAT_CHANGED"; entityId: number; stat: "AP" | "HP"; newValue: number; delta: number }
   | {
       type: "COMBAT_RESOLVED";
@@ -161,6 +161,8 @@ export type GameEvent =
       damageDealt: number;
       isFlanked: boolean;
       heightMod: -1 | 0 | 1;
+      /** Presentation-only impact amplitude; zero for a miss. */
+      shake?: number;
     }
   | { type: "SKILL_RESOLVED"; sourceId: number; skillId: string; targetId?: number; targetPos?: CellPos; success: boolean }
   | { type: "SKILL_RESOURCE_CHANGED"; entityId: number; skillId: string; cooldown: number; uses: number; usesLeft?: number }
@@ -174,7 +176,7 @@ export type GameEvent =
       sourceId?: number;
     }
   | { type: "ENTITY_SPAWNED"; entity: EntityState; cause: "SUMMON" | "ILLUSION" | "RESURRECTION" }
-  | { type: "COVER_DESTROYED"; gridPos: CellPos; newStatus: "HALF" | "NONE" }
+  | { type: "COVER_DESTROYED"; gridPos: CellPos; newStatus: "HALF" | "NONE"; shake?: number }
   | { type: "ENTITY_DIED"; entityId: number; causeOfDeath: "DAMAGE" | "FALL_INTO_PIT" | "POISON" }
   | { type: "ENTITY_REMOVED"; entityId: number; reason: "FLED" | "EXPIRED" | "EXTRACTED" }
   | { type: "OVERWATCH_FIRED"; watcherId: number; triggerId: number; at: CellPos }
diff --git a/app/packages/core/tests/core.test.ts b/app/packages/core/tests/core.test.ts
index 2f75e19..ff9a93f 100644
--- a/app/packages/core/tests/core.test.ts
+++ b/app/packages/core/tests/core.test.ts
@@ -4,8 +4,8 @@ import type { EntityState, MatchState } from "../src/types.js";
 import type { WeaponStats } from "../src/weapons.js";
 
 describe("package version", () => {
-  it("is 0.20.19", () => {
-    expect(CORE_VERSION).toBe("0.20.19");
+  it("is 0.21.2", () => {
+    expect(CORE_VERSION).toBe("0.21.2");
   });
 });
 
diff --git a/app/packages/core/tests/grid.test.ts b/app/packages/core/tests/grid.test.ts
index eab7881..c6c80cc 100644
--- a/app/packages/core/tests/grid.test.ts
+++ b/app/packages/core/tests/grid.test.ts
@@ -134,9 +134,9 @@ describe("path optimality", () => {
 });
 
 describe("createTacticsKernel", () => {
-  it("reports 0.20.19 and does not touch the document object", () => {
+  it("reports 0.21.2 and does not touch the document object", () => {
     const kernel = createTacticsKernel();
-    expect(kernel.version).toBe("0.20.19");
+    expect(kernel.version).toBe("0.21.2");
     expect(typeof globalThis.document).toBe("undefined");
   });
 
diff --git a/app/packages/i18n/locales/en/ui.json b/app/packages/i18n/locales/en/ui.json
index ad9b7da..f38b030 100644
--- a/app/packages/i18n/locales/en/ui.json
+++ b/app/packages/i18n/locales/en/ui.json
@@ -75,6 +75,9 @@
     "objectiveQuick": "Destroy all enemies",
     "enemyTurn": "The Nav advances",
     "pause": "Pause",
+    "speedFast": "Fast mode",
+    "speedNormal": "Normal speed",
+    "speedHint": "Toggle double speed for battle and replay.",
     "resume": "Resume",
     "toMenu": "Return to menu",
     "hp": "Health {current} / {max}",
@@ -116,7 +119,8 @@
       "NO_LOS": "The target is hidden behind an obstacle.",
       "OUT_OF_RANGE": "Out of range."
     },
-    "controlsTitle": "Controls"
+    "controlsTitle": "Controls",
+    "skillScrollHint": "Scroll weapons and skills →"
   },
   "unit": {
     "bogatyr": {
@@ -247,6 +251,8 @@
     "sfxVolume": "Actions",
     "showHints": "Show hints",
     "showHintsHint": "First-time tutorials in the campaign. Turning them off does not block progress.",
+    "autoEndTurn": "Auto-end turn",
+    "autoEndTurnHint": "Pass to the Nav when all living fighters have spent their AP.",
     "debugMode": "Debug mode",
     "debugModeHint": "Inspection tools (movement-cost overlay, auto-win) in battle. For developers and QA.",
     "back": "Back to menu"
@@ -651,6 +657,7 @@
     "allDoneHint": "All three missions are done. The Chronicler is pleased — the Quick Match awaits you.",
     "toQuickMatch": "Go to Quick Match",
     "continue": "Continue",
+    "offscreenHint": "Target is beyond the screen edge",
     "reset": "Reset progress",
     "resetHint": "Completed mission marks will be cleared.",
     "battleLabel": "Training",
diff --git a/app/packages/i18n/locales/ru/ui.json b/app/packages/i18n/locales/ru/ui.json
index 8f2e809..e52c121 100644
--- a/app/packages/i18n/locales/ru/ui.json
+++ b/app/packages/i18n/locales/ru/ui.json
@@ -75,6 +75,9 @@
     "objectiveQuick": "Уничтожьте всех противников",
     "enemyTurn": "Ход Нави",
     "pause": "Пауза",
+    "speedFast": "Ускорение",
+    "speedNormal": "Обычная скорость",
+    "speedHint": "Переключить двойную скорость боя и повтора.",
     "resume": "Продолжить",
     "toMenu": "Выйти в меню",
     "hp": "Здоровье {current} / {max}",
@@ -116,7 +119,8 @@
       "NO_LOS": "Цель не видна: мешает препятствие.",
       "OUT_OF_RANGE": "Слишком далеко."
     },
-    "controlsTitle": "Управление"
+    "controlsTitle": "Управление",
+    "skillScrollHint": "Прокрутите слоты оружия и умений →"
   },
   "unit": {
     "bogatyr": {
@@ -247,6 +251,8 @@
     "sfxVolume": "Действия",
     "showHints": "Показывать подсказки",
     "showHintsHint": "Туториалы «первого раза» в кампании. Отключение не мешает прохождению.",
+    "autoEndTurn": "Автоматически завершать ход",
+    "autoEndTurnHint": "Передать ход Нави, когда у всех живых бойцов закончились ОД.",
     "debugMode": "Отладочный режим",
     "debugModeHint": "Средства проверки (оверлей стоимости, автопобеда) в бою. Для разработчиков и QA.",
     "back": "Назад в меню"
@@ -651,6 +657,7 @@
     "allDoneHint": "Все три миссии завершены. Летописец доволен — теперь вас ждёт быстрый матч.",
     "toQuickMatch": "В быстрый матч",
     "continue": "Далее",
+    "offscreenHint": "Цель за краем экрана",
     "reset": "Сбросить прогресс",
     "resetHint": "Отметки пройденных миссий будут очищены.",
     "battleLabel": "Обучение",
diff --git a/app/packages/i18n/package.json b/app/packages/i18n/package.json
index 0aaa825..a5710d3 100644
--- a/app/packages/i18n/package.json
+++ b/app/packages/i18n/package.json
@@ -1,6 +1,6 @@
 {
   "name": "@bylina/i18n",
-  "version": "0.20.19",
+  "version": "0.21.2",
   "type": "module",
   "exports": {
     ".": "./src/index.ts",
diff --git a/app/packages/net/package.json b/app/packages/net/package.json
index 9e4ddfe..14ceb0a 100644
--- a/app/packages/net/package.json
+++ b/app/packages/net/package.json
@@ -1,6 +1,6 @@
 {
   "name": "@bylina/net",
-  "version": "0.20.19",
+  "version": "0.21.2",
   "type": "module",
   "exports": {
     ".": "./src/index.ts"
diff --git a/app/packages/render/README.md b/app/packages/render/README.md
index 0fd24ec..e522888 100644
--- a/app/packages/render/README.md
+++ b/app/packages/render/README.md
@@ -84,3 +84,21 @@ pnpm dev
 ## 5. Граница ответственности
 
 Ядро не импортирует PixiJS. Смена версии PixiJS — правка двух `package.json`, затем `pnpm install` и таблица раздела 3. Правила боя не меняются.
+
+## 6. Визуальный фундамент (этап 1)
+
+`src/colors.ts` — единый источник семантических цветов: PixiJS использует
+числовые значения `RENDER_COLORS`, а `applyRenderColorVariables()` публикует
+те же значения для CSS интерфейса до первого рендера. Новые цвета поля и
+боевых состояний добавляются в этот модуль, а не в CSS или `field-renderer.ts`.
+
+Туман войны разделён на неподвижный `fogBaseLayer` и редкий
+`fogDriftLayer`: первый пересобирается только при смене `matchSeed`, видимых
+или разведанных клеток; второй обновляется не чаще 15 раз в секунду.
+Декоративные фазы и рисунок травы вычисляются детерминированным `hashCell`.
+
+Колесо и pinch масштабируют поле вокруг якорной точки через
+`zoomAroundPoint`; `centerOn` используется двойным щелчком/касанием портрета
+бойца. Рендер получает `reducedMotion` из системного
+`prefers-reduced-motion`, поэтому событийные анимации, дыхание, тряска и
+дрейф тумана отключаются без изменения игрового состояния.
diff --git a/app/packages/render/package.json b/app/packages/render/package.json
index 50057a8..fbd668e 100644
--- a/app/packages/render/package.json
+++ b/app/packages/render/package.json
@@ -1,6 +1,6 @@
 {
   "name": "@bylina/render",
-  "version": "0.20.19",
+  "version": "0.21.2",
   "type": "module",
   "exports": {
     ".": "./src/index.ts"
diff --git a/app/packages/render/src/camera.ts b/app/packages/render/src/camera.ts
index a4f2bfa..6023b5a 100644
--- a/app/packages/render/src/camera.ts
+++ b/app/packages/render/src/camera.ts
@@ -52,6 +52,59 @@ export function worldToScreen(point: Point, plane: CameraPlane): Point {
   return { x: plane.offset.x + point.x * plane.scale, y: plane.offset.y + point.y * plane.scale };
 }
 
+/**
+ * Меняет масштаб вокруг экранной точки. Мировая точка под курсором остаётся
+ * на том же пикселе — в отличие от простого изменения `scale`, которое
+ * выглядит как сдвиг поля. Функция чистая, поэтому правило одинаково для
+ * колеса мыши и pinch-жеста и легко проверяется без PixiJS.
+ */
+export function zoomAroundPoint(
+  screenPoint: Point,
+  plane: CameraPlane,
+  factor: number,
+  minScale = 0.55,
+  maxScale = 1.8,
+): CameraPlane {
+  if (!Number.isFinite(factor) || factor <= 0 || plane.scale <= 0) return plane;
+  const scale = Math.min(maxScale, Math.max(minScale, plane.scale * factor));
+  const worldPoint = {
+    x: (screenPoint.x - plane.offset.x) / plane.scale,
+    y: (screenPoint.y - plane.offset.y) / plane.scale,
+  };
+  return {
+    scale,
+    offset: {
+      x: screenPoint.x - worldPoint.x * scale,
+      y: screenPoint.y - worldPoint.y * scale,
+    },
+  };
+}
+
+/** Ограничение камеры по одной оси: поле не уводится за край экрана. */
+function clampAxis(target: number, mapSpan: number, screenSpan: number): number {
+  // Поле уже вмещается в экран — выровнять по центру оси.
+  if (mapSpan <= screenSpan) return (screenSpan - mapSpan) / 2;
+  // Иначе окно камеры лежит в пределах поля: [screenSpan − mapSpan, 0].
+  return Math.min(0, Math.max(screenSpan - mapSpan, target));
+}
+
+/** Центрирует мировую точку, не показывая пустоту за границами карты. */
+export function centerCameraOn(
+  point: Point,
+  plane: CameraPlane,
+  screen: ScreenSize,
+  map: MapPlane,
+): CameraPlane {
+  if (screen.width <= 0 || screen.height <= 0 || plane.scale <= 0) return plane;
+  return {
+    scale: plane.scale,
+    offset: {
+      x: clampAxis(screen.width * 0.5 - point.x * plane.scale, map.width * plane.scale, screen.width),
+      y: clampAxis(screen.height * 0.5 - point.y * plane.scale, map.height * plane.scale, screen.height),
+    },
+  };
+}
+
 /**
  * Нужна ли подводка: цель за пределами «зоны комфорта» — то есть за краем
  * экрана или вплотную к нему (в пределах полос перекрытия интерфейсом).
@@ -67,14 +120,6 @@ export function needsTrainingFocus(point: Point, plane: CameraPlane, screen: Scr
   );
 }
 
-/** Ограничение камеры по одной оси: поле не уводится за край экрана. */
-function clampAxis(target: number, mapSpan: number, screenSpan: number): number {
-  // Поле уже вмещается в экран — выровнять по центру оси.
-  if (mapSpan <= screenSpan) return (screenSpan - mapSpan) / 2;
-  // Иначе окно камеры лежит в пределах поля: [screenSpan − mapSpan, 0].
-  return Math.min(0, Math.max(screenSpan - mapSpan, target));
-}
-
 /**
  * Целевое положение камеры подводки: цель приводится к горизонтали центра
  * и чуть выше середины высоты (под плашку наставника, над панелью
diff --git a/app/packages/render/src/colors.ts b/app/packages/render/src/colors.ts
new file mode 100644
index 0000000..bad960f
--- /dev/null
+++ b/app/packages/render/src/colors.ts
@@ -0,0 +1,173 @@
+/**
+ * Единый справочник цветов представления (Этап 1.1).
+ *
+ * Числа используются PixiJS, а те же значения публикуются как CSS custom
+ * properties для оболочки и интерфейса. Справочник намеренно не содержит
+ * цветов игрового баланса: это только семантика изображения.
+ */
+export const RENDER_COLORS = {
+  ui: {
+    ink: 0x14181c,
+    ink2: 0x1c232a,
+    ink3: 0x262f38,
+    line: 0x3a4550,
+    mist: 0xd5cfc0,
+    // Поднят с #9a9384: вторичный текст теперь заметно контрастнее тёмного фона.
+    mistDim: 0xb8b3a5,
+    white: 0xf3ecdc,
+    amber: 0xe0b34a,
+    amberBright: 0xe8b64c,
+    amberDim: 0x8a6a24,
+    danger: 0xc45c5c,
+    dangerBright: 0xd84a3a,
+    movement: 0x388cdc,
+    movementBright: 0x60c8ff,
+    success: 0x74e071,
+    warning: 0xffd268,
+    forge: 0xc2764c,
+    forgeBright: 0xe0a16b,
+    chamber: 0x9dbb72,
+    chamberBright: 0xbfd98f,
+    injury: 0xa03a4e,
+    fieldBackground: 0x101410,
+    // Поверхности интерфейса: варианты исходной палитры, а не новые оттенки.
+    buttonSurface: 0x1c2320,
+    primarySurfaceTop: 0x222a32,
+    primarySurfaceBottom: 0x1a2026,
+    rosterSurface: 0x161c18,
+    rosterSelected: 0x1d231b,
+    navSurface: 0x131b10,
+    backdropHighlight: 0x2a333c,
+    brightText: 0xfff2c4,
+  },
+  terrain: {
+    face: [0x2c3a2c, 0x43603f, 0x74925f] as const,
+    riser: [0x171a12, 0x23291a, 0x38432a] as const,
+    shadow: 0x081008,
+    outline: 0x0c120c,
+    edgeLight: 0xe8f0d0,
+    pit: 0x070907,
+    wall: 0x7a6a56,
+  },
+  faction: {
+    druzhinaRing: 0xe8b64c,
+    druzhinaRingDark: 0x57431a,
+    druzhinaDisc: 0x241c12,
+    navRing: 0x8bc34a,
+    navRingDark: 0x1e3311,
+    navDisc: 0x131b10,
+    navFallback: 0x6d9a3a,
+  },
+  targeting: {
+    selected: 0xffffff,
+    ready: 0xe0b34a,
+    blocked: 0xc45c5c,
+    line: 0xe8b64c,
+  },
+  overlay: {
+    fogHidden: 0x080a0c,
+    fogExplored: 0x0c1218,
+    fogMist: 0x8a9aaa,
+    trainingDim: 0x060a08,
+    shadow: 0x000000,
+  },
+  status: {
+    poison: 0x78d83d,
+    panic: 0xb94cff,
+    immobile: 0x6f8f3d,
+    hidden: 0x8fd3bc,
+    flying: 0xbfe8ff,
+    timed: 0x5fd6e8,
+    defending: 0x68aee8,
+  },
+} as const;
+
+/**
+ * Визуальные палитры биомов: это только цвета представления, они не
+ * участвуют в генерации и правилах боя. У каждого яруса ровно три оттенка.
+ */
+export const BIOME_PALETTES = {
+  meadow: {
+    face: [[0x293829, 0x2c3a2c, 0x30402f], [0x3e5a3b, 0x43603f, 0x486743], [0x6c8958, 0x74925f, 0x7c9964]],
+    riser: [0x171a12, 0x23291a, 0x38432a],
+    scree: [0x65714e, 0x7d855c, 0x9a956c],
+    bush: 0x315035,
+    bushLight: 0x5e7949,
+    rare: [0xb6a65e, 0xd5c57b, 0x7d9250],
+  },
+  swamp: {
+    face: [[0x263934, 0x2b433b, 0x304a40], [0x3b574b, 0x426052, 0x4a6a58], [0x5c7256, 0x657d5d, 0x6d8762]],
+    riser: [0x14211e, 0x23362e, 0x344a38],
+    scree: [0x4d6558, 0x687a62, 0x8a8c6b],
+    bush: 0x294d3d,
+    bushLight: 0x66815c,
+    rare: [0x9fae78, 0xd2c66e, 0x7e9a8b],
+  },
+  thicket: {
+    face: [[0x203229, 0x263b2e, 0x2c4332], [0x334e38, 0x3b5940, 0x426347], [0x526e43, 0x5e7948, 0x688250]],
+    riser: [0x121f19, 0x1f3023, 0x30432c],
+    scree: [0x4b5b3f, 0x66704a, 0x7c7a51],
+    bush: 0x1f422f,
+    bushLight: 0x527447,
+    rare: [0x80613e, 0xa17a49, 0x6b8b55],
+  },
+  scorched: {
+    face: [[0x352927, 0x3d2e2a, 0x46332d], [0x554033, 0x5f4939, 0x6a5140], [0x795b42, 0x836447, 0x8e6d4c]],
+    riser: [0x211817, 0x34241c, 0x4d3324],
+    scree: [0x5c4b3d, 0x77604a, 0x937654],
+    bush: 0x4a392d,
+    bushLight: 0x796044,
+    rare: [0x9a8063, 0xb29a73, 0x6f6860],
+  },
+} as const;
+
+export type RenderColors = typeof RENDER_COLORS;
+
+function colorToCss(color: number): string {
+  return `#${color.toString(16).padStart(6, "0")}`;
+}
+
+/** CSS-переменные интерфейса, производимые из RENDER_COLORS, а не наоборот. */
+export const RENDER_CSS_VARIABLES = {
+  "--ink": colorToCss(RENDER_COLORS.ui.ink),
+  "--ink-2": colorToCss(RENDER_COLORS.ui.ink2),
+  "--ink-3": colorToCss(RENDER_COLORS.ui.ink3),
+  "--line": colorToCss(RENDER_COLORS.ui.line),
+  "--mist": colorToCss(RENDER_COLORS.ui.mist),
+  "--mist-dim": colorToCss(RENDER_COLORS.ui.mistDim),
+  "--white": colorToCss(RENDER_COLORS.ui.white),
+  "--amber": colorToCss(RENDER_COLORS.ui.amber),
+  "--amber-bright": colorToCss(RENDER_COLORS.ui.amberBright),
+  "--amber-dim": colorToCss(RENDER_COLORS.ui.amberDim),
+  "--danger": colorToCss(RENDER_COLORS.ui.danger),
+  "--danger-bright": colorToCss(RENDER_COLORS.ui.dangerBright),
+  "--movement": colorToCss(RENDER_COLORS.ui.movement),
+  "--movement-bright": colorToCss(RENDER_COLORS.ui.movementBright),
+  "--success": colorToCss(RENDER_COLORS.ui.success),
+  "--warning": colorToCss(RENDER_COLORS.ui.warning),
+  "--forge": colorToCss(RENDER_COLORS.ui.forge),
+  "--forge-bright": colorToCss(RENDER_COLORS.ui.forgeBright),
+  "--chamber": colorToCss(RENDER_COLORS.ui.chamber),
+  "--chamber-bright": colorToCss(RENDER_COLORS.ui.chamberBright),
+  "--injury": colorToCss(RENDER_COLORS.ui.injury),
+  "--field-background": colorToCss(RENDER_COLORS.ui.fieldBackground),
+  "--button-surface": colorToCss(RENDER_COLORS.ui.buttonSurface),
+  "--primary-surface-top": colorToCss(RENDER_COLORS.ui.primarySurfaceTop),
+  "--primary-surface-bottom": colorToCss(RENDER_COLORS.ui.primarySurfaceBottom),
+  "--roster-surface": colorToCss(RENDER_COLORS.ui.rosterSurface),
+  "--roster-selected": colorToCss(RENDER_COLORS.ui.rosterSelected),
+  "--nav-surface": colorToCss(RENDER_COLORS.ui.navSurface),
+  "--backdrop-highlight": colorToCss(RENDER_COLORS.ui.backdropHighlight),
+  "--bright-text": colorToCss(RENDER_COLORS.ui.brightText),
+  "--nav": colorToCss(RENDER_COLORS.faction.navRing),
+} as const;
+
+/**
+ * Устанавливает переменные до первого рендера React. Это связывает CSS
+ * оболочки с тем же источником, из которого PixiJS получает цвета.
+ */
+export function applyRenderColorVariables(target?: HTMLElement): void {
+  if (typeof document === "undefined" && !target) return;
+  const root = target ?? document.documentElement;
+  for (const [name, value] of Object.entries(RENDER_CSS_VARIABLES)) root.style.setProperty(name, value);
+}
diff --git a/app/packages/render/src/field-renderer.ts b/app/packages/render/src/field-renderer.ts
index 39f371a..cc61bfa 100644
--- a/app/packages/render/src/field-renderer.ts
+++ b/app/packages/render/src/field-renderer.ts
@@ -2,15 +2,18 @@ import {
   effectiveCoverTier,
   terrainCoverTier,
   tileAt,
+  distH,
   type CellPos,
   type EntityState,
   type GameEvent,
   type MatchState,
   type ReachableCell,
+  type BiomeId,
   type Tile,
 } from "@bylina/core";
 import { Application, Container, Graphics, Rectangle, Text, type FederatedPointerEvent } from "pixi.js";
-import { needsTrainingFocus, trainingGlideOffset, TRAINING_COMFORT, type Point } from "./camera.js";
+import { centerCameraOn, needsTrainingFocus, trainingGlideOffset, TRAINING_COMFORT, zoomAroundPoint, type Point } from "./camera.js";
+import { BIOME_PALETTES, RENDER_COLORS as COLORS } from "./colors.js";
 
 export const RENDER_STATUS = "pixi" as const;
 export const CELL_SIZE = 52;
@@ -20,13 +23,21 @@ const PAD = 26;
 export interface FieldView {
   /** Seed identifies the generated terrain; it changes only when a new map is created. */
   matchSeed: number;
+  /** Visual-only map biome. Legacy maps omit it and use meadow. */
+  biome?: BiomeId;
+  /** Campaign darkness, kept out of the combat snapshot and rules engine. */
+  darkness?: { current: number; max: number };
   snapshot: MatchState;
   selectedId: number | null;
   aimId: number | null;
   reachable: ReachableCell[];
   path: CellPos[];
   aimOk: boolean;
+  /** Состояние кольца выбранной цели: до предпросмотра, готово, либо заблокировано. */
+  aimStatus?: "selected" | "ready" | "blocked";
   heightMod: -1 | 0 | 1;
+  /** Уважение к системной prefers-reduced-motion для Pixi-анимаций. */
+  reducedMotion?: boolean;
   debugMovement?: boolean;
   /** Клетки, которые сторона наблюдает сейчас (ключи «x,y»). Пустое множество = без тумана. */
   visibleCells?: Set<string>;
@@ -44,6 +55,12 @@ export interface FieldView {
    * прежде золотистая рамка терялась на жёлтой подсветке клеток рывка.
    */
   trainingFocus?: boolean;
+  /** Cells affected by the currently aimed area skill. */
+  areaPreview?: { center: CellPos; radius: number } | null;
+  /** A flank attack is being aimed at this entity. */
+  flanked?: boolean;
+  /** Localized labels supplied by the view for combat feedback. */
+  combatLabels?: { miss: string };
 }
 
 export interface FieldRenderer {
@@ -51,9 +68,15 @@ export interface FieldRenderer {
   update(view: FieldView): void;
   play(events: GameEvent[]): Promise<void>;
   pan(dx: number, dy: number): void;
+  /** Центрирует камеру на бойце после двойного клика/касания его портрета. */
+  centerOn(x: number, y: number, z: number): void;
+  /** Экранная позиция клетки для привязки DOM-подсказок; отсутствует до mount. */
+  screenPosition?(x: number, y: number, z: number): Point | null;
   destroy(): void;
   setOnActivate(handler: (x: number, y: number) => void): void;
   setOnHover(handler: (x: number, y: number) => void): void;
+  /** Scale choreography and replay timing without changing game state. */
+  setPlaybackSpeed(multiplier: 1 | 2): void;
 }
 
 /* ---------- палитра и примитивы ---------- */
@@ -93,25 +116,42 @@ function easeInOut(t: number): number {
   return t < 0.5 ? 2 * t * t : 1 - (-2 * t + 2) ** 2 / 2;
 }
 
-/**
- * Локальный шум для визуальных эффектов (тряска камеры). Начальное значение
- * зависит от времени запуска, поэтому эффект не повторяется кадр в кадр,
- * но не использует стандартный генератор среды (§1 math: источник случайности
- * тактического слоя — только Mulberry32; средство отображения состояния не меняет).
- */
-let shakeSeed = (Date.now() >>> 0) || 0x9e3779b9;
+/* ---------- рельеф ---------- */
 
-function shakeNoise(): number {
-  shakeSeed = (Math.imul(shakeSeed, 1664525) + 1013904223) >>> 0;
-  return shakeSeed / 4294967296;
+/** Визуальная палитра поля. У каждого яруса три близких оттенка:
+ * хеш выбирает один из них, поэтому поверхность не выглядит плоской,
+ * но и не превращается в шумную текстуру. */
+const DEFAULT_BIOME: BiomeId = "meadow";
+
+/** Небольшой куст состоит только из замкнутых эллипсов — без «шумных»
+ * линий, которые раньше спорили с сеткой и фишками. */
+function drawBush(g: Graphics, x: number, y: number, scale: number, base: number, light: number): void {
+  g.ellipse(x, y + 3 * scale, 6 * scale, 2 * scale).fill({ color: COLORS.overlay.shadow, alpha: 0.16 });
+  g.ellipse(x - 4 * scale, y, 4.5 * scale, 4 * scale).fill(base);
+  g.ellipse(x + 1 * scale, y - 1.5 * scale, 5 * scale, 4.5 * scale).fill(light);
+  g.ellipse(x + 5 * scale, y + 1 * scale, 3.8 * scale, 3.5 * scale).fill(base);
 }
 
-/* ---------- рельеф ---------- */
-
-/** Верхние грани по уровням: низ — холодный мох, земля — луг, верх — светлая выжженная трава. */
-const Z_FACE = [0x2c3a2c, 0x43603f, 0x74925f] as const;
-/** Откос (южная стена скалы) по уровням: тёмная земляная огранка. */
-const Z_RISER = [0x171a12, 0x23291a, 0x38432a] as const;
+function drawRareDecoration(g: Graphics, x: number, y: number, biome: BiomeId, colors: readonly [number, number, number], scale: number): void {
+  if (biome === "swamp") {
+    // Гриб: три закрытые формы, редкий и сразу отличимый силуэт.
+    g.ellipse(x, y + 4 * scale, 2.2 * scale, 3.5 * scale).fill(colors[2]);
+    g.ellipse(x, y - 0.5 * scale, 5 * scale, 2.8 * scale).fill(colors[0]);
+    g.ellipse(x - 1.5 * scale, y - 1.2 * scale, 1 * scale, 0.7 * scale).fill(colors[1]);
+    return;
+  }
+  if (biome === "thicket") {
+    // Поваленная ветка — короткая закрытая овальная форма, а не линия.
+    g.ellipse(x, y, 8 * scale, 2.5 * scale).fill(colors[0]);
+    g.ellipse(x - 3 * scale, y - 0.5 * scale, 2 * scale, 1.5 * scale).fill(colors[1]);
+    g.ellipse(x + 5 * scale, y + 0.5 * scale, 1.8 * scale, 1.4 * scale).fill(colors[2]);
+    return;
+  }
+  // Камешек на выжженной земле или маленький цветок на лугу.
+  g.ellipse(x, y + 1.5 * scale, 4 * scale, 2.5 * scale).fill(colors[0]);
+  g.ellipse(x - 1.2 * scale, y, 2 * scale, 1.5 * scale).fill(colors[1]);
+  g.ellipse(x + 2 * scale, y + 0.6 * scale, 1.8 * scale, 1.3 * scale).fill(colors[2]);
+}
 
 function visualLevel(tile: Tile): number {
   return tile.pit ? 0 : tile.z;
@@ -131,8 +171,16 @@ interface FactionLook {
   disc: number;
 }
 
-const DRUZHINA: FactionLook = { ring: 0xe8b64c, ringDark: 0x57431a, disc: 0x241c12 };
-const NAV: FactionLook = { ring: 0x8bc34a, ringDark: 0x1e3311, disc: 0x131b10 };
+const DRUZHINA: FactionLook = {
+  ring: COLORS.faction.druzhinaRing,
+  ringDark: COLORS.faction.druzhinaRingDark,
+  disc: COLORS.faction.druzhinaDisc,
+};
+const NAV: FactionLook = {
+  ring: COLORS.faction.navRing,
+  ringDark: COLORS.faction.navRingDark,
+  disc: COLORS.faction.navDisc,
+};
 
 interface TokenCtx {
   g: Graphics;
@@ -140,10 +188,106 @@ interface TokenCtx {
   cy: number;
 }
 
-/** Богатырь: стальной шлем с наносником, алые плечи, щит с коловратом. */
+type TokenStatus =
+  | "panic"
+  | "poison"
+  | "immobile"
+  | "hidden"
+  | "flying"
+  | "timed"
+  | "camouflage"
+  | "defending"
+  | "overwatch";
+
+/**
+ * Фиксированные позиции орбитальных индикаторов. Порядок соответствует
+ * приоритету чтения: сначала боевые помехи, затем маскировка/срок жизни,
+ * потом режимы действия. Поэтому три и более состояния не слипаются в один
+ * случайный венок и сохраняют одинаковую геометрию между кадрами.
+ */
+const TOKEN_STATUS_SLOTS: readonly { x: number; y: number }[] = [
+  { x: 0, y: -30 },
+  { x: 21, y: -21 },
+  { x: 30, y: 0 },
+  { x: 21, y: 21 },
+  { x: 0, y: 30 },
+  { x: -21, y: 21 },
+  { x: -30, y: 0 },
+  { x: -21, y: -21 },
+  { x: 0, y: -39 },
+];
+
+function statusBadgeColor(status: TokenStatus): number {
+  switch (status) {
+    case "panic":
+      return COLORS.status.panic;
+    case "poison":
+      return COLORS.status.poison;
+    case "immobile":
+      return COLORS.status.immobile;
+    case "hidden":
+    case "camouflage":
+      return COLORS.status.hidden;
+    case "flying":
+      return COLORS.status.flying;
+    case "timed":
+      return COLORS.status.timed;
+    case "defending":
+      return COLORS.status.defending;
+    case "overwatch":
+      return COLORS.ui.amberBright;
+  }
+}
+
+/** Маленький форменный знак состояния внутри непрозрачной ячейки-слота. */
+function drawStatusBadge(g: Graphics, cx: number, cy: number, status: TokenStatus, phase: number, timedLife?: number): void {
+  const color = statusBadgeColor(status);
+  g.circle(cx, cy, 6.8).fill({ color: COLORS.ui.ink, alpha: 0.94 });
+  g.circle(cx, cy, 6.8).stroke({ width: 2, color, alpha: 0.96 });
+  if (status === "panic") {
+    g.roundRect(cx - 1, cy - 4, 2, 5.5, 0.8).fill(color);
+    g.circle(cx, cy + 3.5, 1.1).fill(color);
+  } else if (status === "poison") {
+    g.circle(cx - 2.2, cy + 1, 2.1).fill(color);
+    g.circle(cx + 2.2, cy - 1.5, 2.2).fill(color);
+    g.circle(cx - 0.5, cy - 3.5, 1.3).fill({ color: 0xd7ff8a, alpha: 0.95 });
+  } else if (status === "immobile") {
+    for (const dx of [-3.5, 0, 3.5]) {
+      g.moveTo(cx + dx, cy + 3.8)
+        .quadraticCurveTo(cx + dx * 0.6, cy - 0.8, cx + dx * 0.85, cy - 3.6)
+        .stroke({ width: 2, color, alpha: 0.95 });
+    }
+  } else if (status === "hidden") {
+    g.ellipse(cx, cy, 4.2, 2.8).stroke({ width: 2, color });
+    g.circle(cx, cy, 1.2).fill(color);
+  } else if (status === "flying") {
+    const flap = Math.sin(phase) * 1.2;
+    g.moveTo(cx - 1, cy + 2).quadraticCurveTo(cx - 5.5, cy - 3 - flap, cx - 5.5, cy + 1).stroke({ width: 2, color });
+    g.moveTo(cx + 1, cy + 2).quadraticCurveTo(cx + 5.5, cy - 3 - flap, cx + 5.5, cy + 1).stroke({ width: 2, color });
+  } else if (status === "timed") {
+    const dots = Math.max(1, Math.min(3, timedLife ?? 1));
+    for (let i = 0; i < dots; i += 1) g.circle(cx - 3 + i * 3, cy, 1.1).fill(color);
+  } else if (status === "camouflage") {
+    g.ellipse(cx - 1.8, cy, 3.2, 1.8).fill({ color, alpha: 0.9 });
+    g.ellipse(cx + 2.2, cy - 1.2, 2.7, 1.5).fill({ color, alpha: 0.72 });
+    g.moveTo(cx - 4, cy + 3).lineTo(cx + 4, cy - 3).stroke({ width: 2, color });
+  } else if (status === "defending") {
+    g.poly([cx, cy - 4.5, cx + 4, cy - 2.5, cx + 3, cy + 3, cx, cy + 5, cx - 3, cy + 3, cx - 4, cy - 2.5]).fill({ color, alpha: 0.82 });
+    g.moveTo(cx, cy - 2.5).lineTo(cx, cy + 2.5).stroke({ width: 2, color: COLORS.ui.white });
+    g.moveTo(cx - 2, cy).lineTo(cx + 2, cy).stroke({ width: 2, color: COLORS.ui.white });
+  } else {
+    g.ellipse(cx, cy, 4.3, 2.8).stroke({ width: 2, color });
+    g.circle(cx, cy, 1.1).fill(COLORS.ui.ink);
+    g.circle(cx + 0.5, cy - 0.4, 1).fill(COLORS.ui.white);
+  }
+}
+
+/** Богатырь: стальной шлем с наносником, широкие алые плечи и щит. */
 function drawBogatyr({ g, cx, cy }: TokenCtx): void {
+  // Широкая линия плеча — силуэт класса читается ещё до мелких деталей.
+  g.poly([cx - 15, cy + 1, cx - 10, cy - 3, cx, cy, cx + 10, cy - 3, cx + 15, cy + 1, cx + 11, cy + 11, cx - 11, cy + 11]).fill(0x8e2f22);
   g.ellipse(cx, cy + 6, 13, 8).fill(0x8e2f22);
-  g.poly([cx - 6, cy + 9, cx, cy + 3, cx + 6, cy + 9, cx + 6, cy + 12, cx - 6, cy + 12]).fill(0x6f2118);
+  g.poly([cx - 8, cy + 9, cx, cy + 3, cx + 8, cy + 9, cx + 7, cy + 12, cx - 7, cy + 12]).fill(0x6f2118);
   g.circle(cx, cy - 3, 8.5).fill(0xb8c0c8);
   g.circle(cx, cy - 3, 8.5).stroke({ width: 1, color: 0x545c64 });
   g.poly([cx - 8, cy - 3, cx, cy - 12.5, cx + 8, cy - 3]).fill(0x9aa4ad);
@@ -175,16 +319,18 @@ function drawStrelets({ g, cx, cy }: TokenCtx): void {
       .lineTo(cx + dx + 1.1, cy - 5)
       .stroke({ width: 1, color: 0x6b543a, alpha: 0.8 });
   }
-  g.moveTo(cx + 9.5, cy - 7)
-    .quadraticCurveTo(cx + 14.5, cy, cx + 9.5, cy + 8)
+  // Лук и тетива выходят за круг подставки: отличительный профиль стрельца.
+  g.moveTo(cx + 9.5, cy - 8)
+    .quadraticCurveTo(cx + 17, cy, cx + 9.5, cy + 9)
     .stroke({ width: 2, color: 0x5f4126 });
-  g.moveTo(cx + 9.5, cy - 7)
-    .lineTo(cx + 9.5, cy + 8)
-    .stroke({ width: 0.8, color: 0xd8d2c2, alpha: 0.9 });
+  g.moveTo(cx + 9.5, cy - 8)
+    .lineTo(cx + 9.5, cy + 9)
+    .stroke({ width: 2, color: 0xd8d2c2, alpha: 0.9 });
   g.moveTo(cx + 3, cy - 9)
     .lineTo(cx + 7.5, cy - 12.5)
     .stroke({ width: 1.4, color: 0x8a5a20 });
   g.poly([cx + 7.5, cy - 12.5, cx + 6, cy - 11, cx + 8.6, cy - 10.6]).fill(0x3a4550);
+  g.moveTo(cx + 10, cy - 0.8).lineTo(cx + 18, cy - 0.8).stroke({ width: 2, color: 0xe4d8bc });
 }
 
 /** Знахарка: бирюзовый капюшон, светлое лицо, коса, зелье. */
@@ -203,6 +349,9 @@ function drawZnaharka({ g, cx, cy }: TokenCtx): void {
   g.circle(cx - 7.5, cy + 8.5, 2.4).fill(0x7fe07a);
   g.circle(cx - 7.5, cy + 8.5, 2.4).stroke({ width: 0.8, color: 0x2e5f55 });
   g.circle(cx - 7.5, cy + 8, 1).fill(0xd6ffd2);
+  // Острый край капюшона и коса закрепляют силуэт знахарки.
+  g.moveTo(cx - 9, cy - 2).quadraticCurveTo(cx, cy - 14, cx + 9, cy - 2).stroke({ width: 2, color: 0x9ed8c8 });
+  g.moveTo(cx + 7, cy + 4).quadraticCurveTo(cx + 12, cy + 8, cx + 8.5, cy + 13).stroke({ width: 2, color: 0xd8c4a5 });
 }
 
 /** Упырь: чёрный плащ-крыло с алым подбоем, бледное лицо, угольные глаза. */
@@ -292,11 +441,14 @@ function drawVolkhv({ g, cx, cy }: TokenCtx): void {
 }
 
 function drawForestBeast({ g, cx, cy }: TokenCtx): void {
+  // Низкий корпус и крупная голова дают зверю отдельный силуэт среди фигур.
   g.ellipse(cx, cy + 3, 12, 8).fill(0x6f8d43);
   g.circle(cx + 8, cy - 3, 6).fill(0x86a957);
   g.poly([cx + 5, cy - 8, cx + 7, cy - 14, cx + 10, cy - 8]).fill(0x526d32);
   g.poly([cx + 10, cy - 8, cx + 14, cy - 13, cx + 14, cy - 6]).fill(0x526d32);
   g.circle(cx + 10, cy - 3, 0.9).fill(0xe9f5a1);
+  g.circle(cx + 5.5, cy + 7, 2.5).fill(0x587538);
+  g.circle(cx + 12, cy + 6, 2.2).fill(0x587538);
   g.moveTo(cx - 10, cy + 1).quadraticCurveTo(cx - 18, cy - 7, cx - 13, cy - 12)
     .stroke({ width: 2.5, color: 0x759a49 });
 }
@@ -393,7 +545,32 @@ const CLASS_ART: Partial<Record<string, (ctx: TokenCtx) => void>> = {
   solovey: drawSolovey,
 };
 
-const FALLBACK_ART: Record<"druzhina" | "nav", number> = { druzhina: 0xc9a24b, nav: 0x6d9a3a };
+const FALLBACK_ART: Record<"druzhina" | "nav", number> = { druzhina: 0xc9a24b, nav: COLORS.faction.navFallback };
+
+/**
+ * Подставка стороны: круг дружины и шестиугольник Нави. Форма повторяет
+ * смысл цвета и остаётся различимой даже когда фишки уменьшены камерой.
+ */
+function drawTokenBase(g: Graphics, cx: number, cy: number, faction: FactionLook, hexagonal: boolean): void {
+  if (!hexagonal) {
+    g.circle(cx, cy, 17.5).fill(faction.ring);
+    g.circle(cx, cy, 17.5).stroke({ width: 1, color: faction.ringDark });
+    g.circle(cx, cy, 14.6).fill(faction.disc);
+    return;
+  }
+  const points: number[] = [];
+  for (let i = 0; i < 6; i += 1) {
+    const angle = -Math.PI / 2 + (i * Math.PI) / 3;
+    points.push(cx + Math.cos(angle) * 18.5, cy + Math.sin(angle) * 18.5);
+  }
+  g.poly(points).fill(faction.ring).stroke({ width: 1, color: faction.ringDark });
+  const inner: number[] = [];
+  for (let i = 0; i < 6; i += 1) {
+    const angle = -Math.PI / 2 + (i * Math.PI) / 3;
+    inner.push(cx + Math.cos(angle) * 15.2, cy + Math.sin(angle) * 15.2);
+  }
+  g.poly(inner).fill(faction.disc);
+}
 
 /** Деревянная завала-укрытие: полубрус (1) или высокий сруб (2). */
 /** Смещение для граневых укрытий: N=0, E=1, S=2, W=3. */
@@ -405,13 +582,59 @@ const HALF = C / 2;
  * Граневое укрытие: бревна вдоль всей грани клетки.
  * N/S — горизонтальные, E/W — вертикальные.
  */
+function drawLowPalisade(g: Graphics, cx: number, cy: number, coverType: 1 | 2, edge: 0 | 1 | 2 | 3, color: number, stroke: number): void {
+  // Низкий частокол поверх линии грани: короткие стойки читаются даже в
+  // полуукрытии, а иконка щита по-прежнему остаётся главным знаком защиты.
+  const count = coverType === 2 ? 4 : 3;
+  const span = C - 12;
+  for (let i = 0; i < count; i += 1) {
+    const along = -span / 2 + ((span * i) / (count - 1));
+    const height = coverType === 2 ? 8 : 6;
+    if (edge === 0 || edge === 2) {
+      const x = cx + along;
+      const y = edge === 0 ? cy - HALF - height / 2 : cy + HALF - height / 2;
+      g.roundRect(x - 1.7, y - height, 3.4, height + 4, 1.2).fill(color);
+      g.poly([x - 1.7, y - height, x, y - height - 3, x + 1.7, y - height]).fill(color);
+      g.roundRect(x - 1.7, y - height, 3.4, height + 4, 1.2).stroke({ width: 0.5, color: stroke });
+    } else {
+      const y = cy + along;
+      const x = edge === 1 ? cx + HALF - height / 2 : cx - HALF - height / 2;
+      g.roundRect(x, y - 1.7, height + 4, 3.4, 1.2).fill(color);
+      g.poly([x + height, y - 1.7, x + height + 3, y, x + height, y + 1.7]).fill(color);
+      g.roundRect(x, y - 1.7, height + 4, 3.4, 1.2).stroke({ width: 0.5, color: stroke });
+    }
+  }
+}
+
 function drawEdgeCover(
   g: Graphics,
   cx: number,
   cy: number,
   coverType: 1 | 2,
   edge: 0 | 1 | 2 | 3,
+  biome: BiomeId = DEFAULT_BIOME,
 ): void {
+  if (biome !== "thicket") {
+    const horizontal = edge === 0 || edge === 2;
+    const span = C - 12;
+    const base = biome === "scorched" ? 0x70604e : biome === "swamp" ? 0x365846 : 0x3f6a3c;
+    const light = biome === "scorched" ? 0xa08c6b : biome === "swamp" ? 0x6b8761 : 0x6d914f;
+    const count = coverType === 2 ? 3 : 2;
+    for (let i = 0; i < count; i += 1) {
+      const along = -span / 2 + (span * i / (count - 1));
+      const x = horizontal ? cx + along : cx + (edge === 1 ? HALF - 8 : -HALF + 8);
+      const y = horizontal ? cy + (edge === 0 ? -HALF + 5 : HALF - 5) : cy + along;
+      if (biome === "scorched") {
+        g.ellipse(x, y, 7 + coverType, 5 + coverType * 0.4).fill(base);
+        g.ellipse(x - 1.5, y - 1.5, 3.5, 2).fill(light);
+        g.ellipse(x, y, 7 + coverType, 5 + coverType * 0.4).stroke({ width: 0.8, color: 0x35291f });
+      } else {
+        drawBush(g, x, y, 0.65 + coverType * 0.08, base, light);
+      }
+    }
+    drawLowPalisade(g, cx, cy, coverType, edge, shade(base, -15), shade(base, -35));
+    return;
+  }
   const isHorizontal = edge === 0 || edge === 2;
   const length = C - 4; // длина бревна (чуть меньше клетки)
   const thick = coverType === 2 ? 10 : 6; // толщина
@@ -471,6 +694,7 @@ function drawEdgeCover(
       g.circle(bx2 + thick / 2, by + bh - er - 0.5, er).stroke({ width: 0.6, color: endStroke });
     }
   }
+  drawLowPalisade(g, cx, cy, coverType, edge, coverType === 2 ? 0x6b4b2a : 0x8c6a3d, 0x2e2419);
 }
 
 function drawCover(
@@ -479,15 +703,53 @@ function drawCover(
   cy: number,
   coverType: 1 | 2,
   edge?: 0 | 1 | 2 | 3,
+  seedX = 0,
+  seedY = 0,
+  biome: BiomeId = DEFAULT_BIOME,
 ): void {
   if (edge !== undefined) {
-    drawEdgeCover(g, cx, cy, coverType, edge);
+    drawEdgeCover(g, cx, cy, coverType, edge, biome);
+    if (coverType === 1) {
+      const side = hashCell(seedX, seedY, 157) > 0.5 ? 1 : -1;
+      if (edge === 0 || edge === 2) {
+        const y = edge === 0 ? cy - HALF - 5 : cy + HALF - 1;
+        const x = cx + side * (hashCell(seedX, seedY, 163) * 10);
+        g.moveTo(x, y).lineTo(x + side * 3, y + 4).lineTo(x - side * 1, y + 8)
+          .stroke({ width: 1.15, color: 0x2e2419, alpha: 0.85 });
+      } else {
+        const x = edge === 1 ? cx + HALF - 1 : cx - HALF + 5;
+        const y = cy + side * (hashCell(seedX, seedY, 163) * 10);
+        g.moveTo(x, y).lineTo(x + 4, y + side * 3).lineTo(x + 8, y - side)
+          .stroke({ width: 1.15, color: 0x2e2419, alpha: 0.85 });
+      }
+    }
     return;
   }
   // Целоклеточное укрытие.
   const px = cx;
   const py = cy;
-  g.ellipse(px, py + 11, 15, 4.5).fill({ color: 0x000000, alpha: 0.3 });
+  g.ellipse(px, py + 11, 15, 4.5).fill({ color: COLORS.overlay.shadow, alpha: 0.3 });
+
+  if (biome !== "thicket") {
+    if (biome === "scorched") {
+      const stoneColors = [0x665647, 0x86715b, 0x4e4238];
+      for (let i = 0; i < (coverType === 2 ? 4 : 3); i += 1) {
+        const ox = (i - 1.5) * 7 + (hashCell(seedX, seedY, 301 + i) - 0.5) * 3;
+        const oy = (hashCell(seedX, seedY, 307 + i) - 0.5) * 5;
+        g.ellipse(px + ox, py + oy, 7 + (i % 2), 5 + coverType).fill(stoneColors[i % stoneColors.length]);
+        g.ellipse(px + ox - 1.5, py + oy - 1.5, 2.8, 1.8).fill(0xa08b6c);
+      }
+    } else {
+      drawBush(g, px - 6, py + 2, 0.9, biome === "swamp" ? 0x2b503e : 0x3f6b3b, biome === "swamp" ? 0x63815a : 0x73954f);
+      drawBush(g, px + 6, py + 1, 0.82, biome === "swamp" ? 0x244638 : 0x355e35, biome === "swamp" ? 0x58765a : 0x628648);
+    }
+    // Повреждение остаётся контрастным, но не меняет силу укрытия.
+    if (coverType === 1) {
+      const side = hashCell(seedX, seedY, 131) > 0.5 ? 1 : -1;
+      g.ellipse(px + side * 5, py - 2, 1.2, 3.5).fill({ color: 0x2e2419, alpha: 0.8 });
+    }
+    return;
+  }
 
   if (coverType === 1) {
     const ly = py + 4;
@@ -512,6 +774,21 @@ function drawCover(
       .stroke({ width: 2.2, color: 0x6b4f2a });
     g.poly([px + 11, py - 14, px + 9, py - 11.6, px + 12.4, py - 11.4]).fill(0x3a2a18);
   }
+  // Полуукрытие получает устойчивый, координатный рисунок повреждений:
+  // это читаемый знак состояния, а не случайная текстура материала.
+  if (coverType === 1) {
+    const side = hashCell(seedX, seedY, 131) > 0.5 ? 1 : -1;
+    const crackX = px + side * (4 + hashCell(seedX, seedY, 137) * 4);
+    const crackY = py - 2 - hashCell(seedX, seedY, 139) * 4;
+    g.moveTo(crackX - side * 1, crackY - 5)
+      .lineTo(crackX, crackY)
+      .lineTo(crackX - side * 3, crackY + 4)
+      .lineTo(crackX + side * 1, crackY + 7)
+      .stroke({ width: 1.2, color: 0x2e2419, alpha: 0.85 });
+    // Сколотый верхний угол — тот же seed, чтобы не дрожал между кадрами.
+    const chip = hashCell(seedX, seedY, 149) > 0.45 ? 1 : -1;
+    g.poly([px + chip * 8, py - 8, px + chip * 13, py - 11, px + chip * 10, py - 4]).fill(0x3f2e1c);
+  }
 }
 
 /**
@@ -540,7 +817,7 @@ function drawShieldIcon(
 
   const hw = shieldW / 2;
   const hh = shieldH / 2;
-  const color = coverType === 2 ? 0xe8b64c : 0x60c8ff;
+  const color = coverType === 2 ? COLORS.ui.amberBright : COLORS.ui.movementBright;
   const darkColor = coverType === 2 ? 0x8a6a24 : 0x3080b0;
 
   // Форма щита: верх — прямоугольник, низ — треугольник (заострение).
@@ -585,7 +862,7 @@ function drawShieldIcon(
 
 /** Могильная отметина павшего: камешки и череп. */
 function drawFallen(g: Graphics, cx: number, cy: number): void {
-  g.ellipse(cx, cy + 7, 12, 4).fill({ color: 0x000000, alpha: 0.3 });
+  g.ellipse(cx, cy + 7, 12, 4).fill({ color: COLORS.overlay.shadow, alpha: 0.3 });
   g.ellipse(cx - 5, cy + 5, 5, 3.2).fill(0x4a4d48);
   g.ellipse(cx + 5.5, cy + 6, 4, 2.6).fill(0x3c3f3a);
   g.ellipse(cx + 1, cy + 3, 3.4, 2.2).fill(0x555850);
@@ -603,9 +880,12 @@ type Fx =
   | { kind: "flash"; x: number; y: number; start: number; crit: boolean; miss: boolean; angle: number }
   | { kind: "bolt"; x0: number; y0: number; x1: number; y1: number; start: number; dur: number; warm: boolean }
   | { kind: "poof"; x: number; y: number; start: number }
+  | { kind: "death"; x: number; y: number; start: number; entityId: number; pit: boolean }
+  | { kind: "coverBreak"; x: number; y: number; start: number; seed: number }
   | { kind: "extract"; x: number; y: number; start: number }
   | { kind: "skill"; x0: number; y0: number; x1: number; y1: number; start: number; dur: number; style: string; success: boolean }
-  | { kind: "status"; x: number; y: number; start: number; status: string; applied: boolean };
+  | { kind: "status"; x: number; y: number; start: number; status: string; applied: boolean }
+  | { kind: "combatText"; x: number; y: number; start: number; text: string; color: number; critical: boolean; label: Text };
 
 interface DisplayState {
   x: number;
@@ -623,9 +903,23 @@ export function createFieldRenderer(): FieldRenderer {
   const world = new Container();
   // Terrain is immutable for a generated map and remains outside dynamic overlays.
   const terrain = new Container();
+  // Туман не смешан с частицами: базовая пелена перестраивается только при
+  // смене видимости, а медленные пятна — отдельным слоем не чаще 15 Гц.
+  const fogBaseLayer = new Graphics();
+  // Переходная полоса и короткое «схлопывание» открытия клетки — отдельные
+  // динамические слои поверх кэшируемой основы тумана.
+  const fogTransitionLayer = new Graphics();
+  const fogDriftLayer = new Graphics();
+  const fogRevealLayer = new Graphics();
   const fxLayer = new Graphics();
+  // Magic is deliberately isolated so additive effects do not muddy physical FX.
+  const magicLayer = new Graphics();
+  magicLayer.blendMode = "add";
+  const floatingTextLayer = new Container();
   const debugLayer = new Container();
-  world.addChild(terrain, fxLayer, debugLayer);
+  const atmosphereLayer = new Graphics();
+  atmosphereLayer.eventMode = "none";
+  world.addChild(terrain, fogBaseLayer, fogTransitionLayer, fogDriftLayer, fogRevealLayer, fxLayer, magicLayer, floatingTextLayer, debugLayer);
   world.eventMode = "static";
   world.hitArea = new Rectangle(-4000, -4000, 12000, 12000);
 
@@ -641,12 +935,26 @@ export function createFieldRenderer(): FieldRenderer {
   const lunges = new Map<number, { dx: number; dy: number }>();
   const bumps = new Map<number, { dx: number; dy: number }>();
   const dying = new Map<number, number>();
+  const deathPit = new Map<number, boolean>();
   const flashes = new Map<number, number>();
   const fxs: Fx[] = [];
 
   let playing = false;
   let holdDisplay = false;
-  let terrainSeed: number | null = null;
+  let reducedMotion = false;
+  let playbackSpeed: 1 | 2 = 1;
+  let terrainSeed: string | null = null;
+  let fogSignature: string | null = null;
+  let lastVisibleCells: Set<string> | null = null;
+  let lastFogMatchSeed: number | null = null;
+  const fogReveals = new Map<string, number>();
+  let lastFogDriftPaint = -Infinity;
+  let lastDynamicPaint = -Infinity;
+  let atmosphereSignature: string | null = null;
+  const lowPowerDevice = typeof navigator !== "undefined"
+    && ((navigator.hardwareConcurrency ?? 8) <= 4
+      || (navigator as Navigator & { deviceMemory?: number }).deviceMemory !== undefined
+        && (navigator as Navigator & { deviceMemory?: number }).deviceMemory! <= 4);
   const jobs: Array<{ events: GameEvent[]; done: () => void }> = [];
 
   // Подводка камеры к цели обучающего указания (0.20.14): срабатывает при
@@ -785,8 +1093,8 @@ export function createFieldRenderer(): FieldRenderer {
       if (dist > CELL_SIZE * 0.8) continue; // слишком далеко от луча
 
       // Найти точку пересечения с гранью укрытия.
-      const color = cover.coverType === 2 ? 0xe8b64c : 0x60c8ff;
-      const markerSize = 4;
+      const color = cover.coverType === 2 ? COLORS.ui.danger : COLORS.ui.warning;
+      const markerSize = 8;
       // Рисуем ромб в точке проекции.
       g.moveTo(projX, projY - markerSize)
         .lineTo(projX + markerSize, projY)
@@ -831,6 +1139,19 @@ export function createFieldRenderer(): FieldRenderer {
     world.y = (h - bh * scale) / 2;
   };
 
+  /** Зум с якорем под курсором/между пальцами. */
+  const zoomAt = (screenX: number, screenY: number, factor: number): void => {
+    const next = zoomAroundPoint(
+      { x: screenX, y: screenY },
+      { scale: world.scale.x, offset: { x: world.x, y: world.y } },
+      factor,
+    );
+    world.scale.set(next.scale);
+    world.x = next.offset.x;
+    world.y = next.offset.y;
+    userMoved = true;
+  };
+
   /* ---------- статичный слой: рельеф ---------- */
 
   const drawTile = (tile: Tile): Graphics => {
@@ -840,40 +1161,48 @@ export function createFieldRenderer(): FieldRenderer {
     const { fy } = faceOf(tile.x, tile.y, z);
     const tiles = snapshot?.grid.tiles ?? [];
     const C = CELL_SIZE;
+    const biome = view?.biome ?? DEFAULT_BIOME;
+    const palette = BIOME_PALETTES[biome];
+    const mapSeed = view?.matchSeed ?? 0;
+    const seeded = (salt: number): number => hashCell(tile.x, tile.y, salt + mapSeed);
 
     // Откосы тянутся вниз от грани до уровня южного соседа (или до основания на краю карты).
     const southLevel = neighborLevel(tiles, tile.x, tile.y + 1);
     const dropSouth = southLevel === null ? z : Math.max(0, z - southLevel);
     if (dropSouth > 0 && !tile.pit) {
       const h = dropSouth * RISE;
-      const riser = Z_RISER[z] ?? 0x23291a;
+      const riser = palette.riser[z] ?? palette.riser[1];
       g.rect(0, C, C, h).fill(mix(riser, 0x1a140c, 0.35));
       g.rect(0, C, C, 2).fill(shade(riser, 26));
       for (let i = 0; i < dropSouth; i += 1) {
-        g.rect(0, C + (i + 1) * RISE - 1.5, C, 1.5).fill({ color: 0x000000, alpha: 0.22 });
+        g.rect(0, C + (i + 1) * RISE - 1.5, C, 1.5).fill({ color: COLORS.overlay.shadow, alpha: 0.22 });
       }
-      g.rect(0, C, 3, h).fill({ color: 0x000000, alpha: 0.16 });
-      g.rect(C - 3, C, 3, h).fill({ color: 0x000000, alpha: 0.16 });
-      // Камни-выступы на откосе.
-      const stones = 1 + Math.floor(hashCell(tile.x, tile.y, 5) * 2);
+      g.rect(0, C, 3, h).fill({ color: COLORS.overlay.shadow, alpha: 0.16 });
+      g.rect(C - 3, C, 3, h).fill({ color: COLORS.overlay.shadow, alpha: 0.16 });
+      // Нарисованная осыпь смягчает переход яруса: несколько закрытых
+      // камешков с координатным seed, без частиц и без обновления кадр за кадром.
+      const stones = 2 + Math.floor(seeded(5) * 3);
       for (let i = 0; i < stones; i += 1) {
-        const sx = 6 + hashCell(tile.x, tile.y, 11 + i) * (C - 14);
-        const sy = C + 3 + hashCell(tile.x, tile.y, 17 + i) * Math.max(1, h - 6);
-        g.circle(sx, sy, 1.3).fill(shade(riser, 30));
+        const sx = 5 + seeded(11 + i) * (C - 12);
+        const sy = C + 3 + seeded(17 + i) * Math.max(1, h - 6);
+        const radius = 1.2 + seeded(19 + i) * 1.2;
+        const stone = palette.scree[i % palette.scree.length] ?? palette.scree[0];
+        g.ellipse(sx, sy, radius * 1.45, radius).fill(stone);
+        g.ellipse(sx - radius * 0.25, sy - radius * 0.25, radius * 0.55, radius * 0.35).fill(shade(stone, 18));
       }
     }
 
     // Грань.
-    const jitter = (hashCell(tile.x, tile.y, 1) - 0.5) * 14;
-    const base = tile.pit ? 0x141a12 : tile.blockLOS ? 0x3c332a : (Z_FACE[z] ?? Z_FACE[1]);
-    const fill = tile.pit || tile.blockLOS ? base : shade(base, jitter);
+    const shadeIndex = Math.min(2, Math.floor(seeded(1) * 3));
+    const base = tile.pit ? 0x141a12 : tile.blockLOS ? 0x3c332a : (palette.face[z]?.[shadeIndex] ?? palette.face[1][1]);
+    const fill = base;
     g.rect(0, 0, C, C).fill(fill);
     if (!tile.pit && !tile.blockLOS) {
       // Мягкий перелив: светлее к северной кромке, темнее к южной.
       g.rect(0, 0, C, 8).fill({ color: 0xffffff, alpha: 0.05 });
-      g.rect(0, C - 9, C, 9).fill({ color: 0x000000, alpha: 0.08 });
+      g.rect(0, C - 9, C, 9).fill({ color: COLORS.overlay.shadow, alpha: 0.08 });
     }
-    g.rect(0, 0, C, C).stroke({ width: 1, color: 0x0c120c, alpha: 0.32 });
+    g.rect(0, 0, C, C).stroke({ width: 1, color: COLORS.terrain.outline, alpha: 0.32 });
 
     // Тени от более высоких соседей и световые канты над низкими — для всех четырёх сторон,
     // поэтому перепад высот читается и по горизонтали, и по вертикали.
@@ -894,21 +1223,21 @@ export function createFieldRenderer(): FieldRenderer {
         for (let i = 0; i < steps; i += 1) {
           const frac = i / steps;
           const alpha = (0.3 - frac * 0.22) * Math.min(1, diff * 0.75);
-          if (side.edge === "n") g.rect(0, frac * width, C, width / steps).fill({ color: 0x081008, alpha });
-          if (side.edge === "s") g.rect(0, C - width + frac * width, C, width / steps).fill({ color: 0x081008, alpha });
-          if (side.edge === "e") g.rect(C - width + frac * width, 0, width / steps, C).fill({ color: 0x081008, alpha });
-          if (side.edge === "w") g.rect(frac * width, 0, width / steps, C).fill({ color: 0x081008, alpha });
+          if (side.edge === "n") g.rect(0, frac * width, C, width / steps).fill({ color: COLORS.terrain.shadow, alpha });
+          if (side.edge === "s") g.rect(0, C - width + frac * width, C, width / steps).fill({ color: COLORS.terrain.shadow, alpha });
+          if (side.edge === "e") g.rect(C - width + frac * width, 0, width / steps, C).fill({ color: COLORS.terrain.shadow, alpha });
+          if (side.edge === "w") g.rect(frac * width, 0, width / steps, C).fill({ color: COLORS.terrain.shadow, alpha });
         }
       } else if (diff < 0 && side.edge !== "s") {
         // Световой кант по кромке над обрывом (южную кромку подчёркивает откос).
         const strength = Math.min(0.4, 0.22 + -diff * 0.1);
-        if (side.edge === "n") g.rect(0, 0, C, 2).fill({ color: 0xe8f0d0, alpha: strength });
-        if (side.edge === "e") g.rect(C - 2, 0, 2, C).fill({ color: 0xe8f0d0, alpha: strength });
-        if (side.edge === "w") g.rect(0, 0, 2, C).fill({ color: 0xe8f0d0, alpha: strength });
+        if (side.edge === "n") g.rect(0, 0, C, 2).fill({ color: COLORS.terrain.edgeLight, alpha: strength });
+        if (side.edge === "e") g.rect(C - 2, 0, 2, C).fill({ color: COLORS.terrain.edgeLight, alpha: strength });
+        if (side.edge === "w") g.rect(0, 0, 2, C).fill({ color: COLORS.terrain.edgeLight, alpha: strength });
       }
     }
     if (dropSouth > 0 && !tile.pit) {
-      g.rect(0, C - 2, C, 2).fill({ color: 0xe8f0d0, alpha: 0.3 });
+      g.rect(0, C - 2, C, 2).fill({ color: COLORS.terrain.edgeLight, alpha: 0.3 });
     }
 
     // Яма: глубокий овал с краями-крошкой.
@@ -927,7 +1256,7 @@ export function createFieldRenderer(): FieldRenderer {
 
     // Камень-глыба (блокирует обзор): валун с гранями.
     if (tile.blockLOS) {
-      g.ellipse(C / 2, C - 9, 16, 5).fill({ color: 0x000000, alpha: 0.28 });
+      g.ellipse(C / 2, C - 9, 16, 5).fill({ color: COLORS.overlay.shadow, alpha: 0.28 });
       g.poly([6, C - 12, 10, 16, 24, 8, 38, 10, 46, 18, 44, C - 10, 26, C - 6, 12, C - 8]).fill(0x7a6a56);
       g.poly([10, 16, 24, 8, 38, 10, 34, 22, 16, 24]).fill(0x94836b);
       g.poly([12, C - 8, 26, C - 6, 44, C - 10, 40, C - 16, 18, C - 14]).fill(0x584a3a);
@@ -941,30 +1270,20 @@ export function createFieldRenderer(): FieldRenderer {
       g.circle(33, 16, 1.2).fill(0x6b5b48);
     }
 
-    // Травяной декор: стабилен (хеш от координат), не портит читаемость.
+    // Биомный декор: кустик — 2–3 замкнутые формы, полностью детерминированные
+    // координатами и seed карты. Отдельная ветка > 97% даёт редкий декор (~3%).
     if (!tile.pit && !tile.blockLOS) {
-      const blades = 1 + Math.floor(hashCell(tile.x, tile.y, 29) * 3);
-      for (let i = 0; i < blades; i += 1) {
-        const bx = 5 + hashCell(tile.x, tile.y, 31 + i * 2) * (C - 10);
-        const by = 5 + hashCell(tile.x, tile.y, 32 + i * 2) * (C - 10);
-        const lean = (hashCell(tile.x, tile.y, 41 + i) - 0.5) * 4;
-        g.moveTo(bx, by)
-          .lineTo(bx - 1.6 + lean, by - 4.4)
-          .stroke({ width: 1.1, color: shade(fill, z === 2 ? -18 : 22) });
-        g.moveTo(bx, by)
-          .lineTo(bx + 1.8 + lean, by - 3.6)
-          .stroke({ width: 1, color: shade(fill, z === 2 ? -10 : 30) });
-      }
-      if (hashCell(tile.x, tile.y, 47) > 0.82) {
-        const fx0 = 8 + hashCell(tile.x, tile.y, 53) * (C - 16);
-        const fy0 = 8 + hashCell(tile.x, tile.y, 59) * (C - 16);
-        g.ellipse(fx0, fy0, 2.6, 1.5).fill(shade(fill, -24));
+      const bushRoll = seeded(29);
+      if (bushRoll > (biome === "thicket" ? 0.12 : 0.26)) {
+        const bx = 7 + seeded(31) * (C - 14);
+        const by = 9 + seeded(32) * (C - 18);
+        const scale = 0.55 + seeded(33) * 0.25;
+        drawBush(g, bx, by, scale, palette.bush, palette.bushLight);
       }
-      if (hashCell(tile.x, tile.y, 61) > 0.9 && z >= 1) {
-        const fx0 = 7 + hashCell(tile.x, tile.y, 67) * (C - 14);
-        const fy0 = 7 + hashCell(tile.x, tile.y, 71) * (C - 14);
-        g.circle(fx0, fy0, 1.3).fill(0xd8ce9a);
-        g.circle(fx0 + 2, fy0 + 1, 1).fill(0xc9b26a);
+      if (seeded(61) >= 0.97) {
+        const rx = 7 + seeded(67) * (C - 14);
+        const ry = 8 + seeded(71) * (C - 16);
+        drawRareDecoration(g, rx, ry, biome, palette.rare, 0.7 + seeded(79) * 0.25);
       }
     }
 
@@ -982,7 +1301,185 @@ export function createFieldRenderer(): FieldRenderer {
       terrain.addChild(drawTile(tile));
     }
     terrain.sortableChildren = true;
-    terrainSeed = view.matchSeed;
+    terrainSeed = `${view.matchSeed}:${view.biome ?? DEFAULT_BIOME}`;
+  };
+
+  const fogSignatureOf = (next: FieldView): string | null => {
+    if (!next.visibleCells) return null;
+    const visible = [...next.visibleCells].sort().join("|");
+    const explored = [...(next.exploredCells ?? [])].sort().join("|");
+    return `${next.matchSeed}:${visible}::${explored}`;
+  };
+
+  /** Неподвижная часть тумана: полное скрытие и затемнение разведанных клеток. */
+  const paintFogBase = (): void => {
+    fogBaseLayer.clear();
+    fogTransitionLayer.clear();
+    fogDriftLayer.clear();
+    fogRevealLayer.clear();
+    lastFogDriftPaint = -Infinity;
+    if (!view?.visibleCells) {
+      fogBaseLayer.visible = false;
+      fogTransitionLayer.visible = false;
+      fogDriftLayer.visible = false;
+      fogRevealLayer.visible = false;
+      return;
+    }
+    fogBaseLayer.visible = true;
+    fogTransitionLayer.visible = true;
+    fogDriftLayer.visible = true;
+    fogRevealLayer.visible = true;
+    for (const tile of view.snapshot.grid.tiles) {
+      const key = `${tile.x},${tile.y}`;
+      const isVisible = view.visibleCells.has(key);
+      const isExplored = view.exploredCells?.has(key) ?? false;
+      if (isVisible) continue;
+      const z = visualLevel(tile);
+      const { fx, fy } = faceOf(tile.x, tile.y, z);
+      if (!isExplored) {
+        fogBaseLayer.rect(fx, fy, CELL_SIZE, CELL_SIZE).fill(COLORS.overlay.fogHidden);
+      } else {
+        // Мягче прежнего прямоугольника: края остаются читаемой переходной зоной.
+        fogBaseLayer.rect(fx, fy, CELL_SIZE, CELL_SIZE).fill({ color: COLORS.overlay.fogExplored, alpha: 0.5 });
+        fogTransitionLayer.ellipse(
+          fx + CELL_SIZE / 2,
+          fy + CELL_SIZE / 2,
+          CELL_SIZE * 0.64,
+          CELL_SIZE * 0.58,
+        ).fill({ color: COLORS.overlay.fogMist, alpha: 0.085 });
+      }
+    }
+  };
+
+  /** Анимированная часть тумана. Перерисовывается отдельно и не чаще 15 Гц. */
+  const paintFogDrift = (now: number): void => {
+    fogDriftLayer.clear();
+    if (!view?.visibleCells || reducedMotion) return;
+    const slowT = now * 0.0003;
+    for (const tile of view.snapshot.grid.tiles) {
+      const key = `${tile.x},${tile.y}`;
+      if (view.visibleCells.has(key) || !(view.exploredCells?.has(key) ?? false)) continue;
+      const z = visualLevel(tile);
+      const { fx, fy } = faceOf(tile.x, tile.y, z);
+      for (let i = 0; i < 3; i += 1) {
+        const h1 = hashCell(tile.x + i * 17, tile.y - i * 11, 73 + i);
+        const h2 = hashCell(tile.x - i * 13, tile.y + i * 19, 89 + i);
+        const phase = slowT + h1 * Math.PI * 2;
+        const drift = Math.sin(phase) * 4;
+        const driftY = Math.cos(phase * 0.7 + i) * 3;
+        const cx = fx + h1 * CELL_SIZE + drift;
+        const cy = fy + h2 * CELL_SIZE + driftY;
+        const radius = 10 + h1 * 16;
+        const alpha = 0.035 + 0.022 * Math.sin(phase * 1.3 + i * 2.1);
+        // Перекрывающиеся эллипсы дают мягкую дымку без текстурной нагрузки.
+        fogDriftLayer.ellipse(cx, cy, radius * 1.35, radius * 0.62).fill({ color: COLORS.overlay.fogMist, alpha });
+        fogDriftLayer.ellipse(cx - radius * 0.22, cy + radius * 0.1, radius * 0.8, radius * 0.42).fill({ color: COLORS.overlay.fogMist, alpha: alpha * 0.7 });
+      }
+    }
+  };
+
+  /** Короткое схлопывание тумана над клеткой, ставшей видимой. */
+  const paintFogReveal = (now: number): void => {
+    fogRevealLayer.clear();
+    if (!view?.visibleCells || reducedMotion) {
+      fogReveals.clear();
+      return;
+    }
+    fogRevealLayer.visible = true;
+    for (const [key, started] of fogReveals) {
+      const t = Math.min(1, (now - started) / 420);
+      if (t >= 1) {
+        fogReveals.delete(key);
+        continue;
+      }
+      const [xs, ys] = key.split(",");
+      const tile = view.snapshot.grid.tiles.find((candidate) => candidate.x === Number(xs) && candidate.y === Number(ys));
+      if (!tile) continue;
+      const { fx, fy } = faceOf(tile.x, tile.y, visualLevel(tile));
+      const cx = fx + CELL_SIZE / 2;
+      const cy = fy + CELL_SIZE / 2;
+      const collapse = 1 - easeOut(t);
+      const alpha = 0.62 * collapse;
+      // Несколько эллипсов схлопываются к центру, а не просто исчезают.
+      fogRevealLayer.ellipse(cx, cy, (CELL_SIZE * 0.7) * collapse + 2, (CELL_SIZE * 0.55) * collapse + 2).fill({ color: COLORS.overlay.fogMist, alpha });
+      fogRevealLayer.ellipse(cx + 3 * collapse, cy - 2 * collapse, (CELL_SIZE * 0.43) * collapse + 1, (CELL_SIZE * 0.3) * collapse + 1).fill({ color: COLORS.overlay.fogHidden, alpha: alpha * 0.75 });
+    }
+  };
+
+  const paintFog = (next: FieldView): void => {
+    const signature = fogSignatureOf(next);
+    if (signature !== fogSignature) {
+      const previous = lastVisibleCells;
+      const sameMap = lastFogMatchSeed === next.matchSeed;
+      if (sameMap && previous && next.visibleCells) {
+        for (const key of next.visibleCells) {
+          if (!previous.has(key)) fogReveals.set(key, performance.now());
+        }
+      } else {
+        // Новая карта не должна анимировать сразу весь стартовый обзор.
+        fogReveals.clear();
+      }
+      lastVisibleCells = next.visibleCells ? new Set(next.visibleCells) : null;
+      lastFogMatchSeed = next.matchSeed;
+      fogSignature = signature;
+      paintFogBase();
+    }
+    const now = performance.now();
+    paintFogReveal(now);
+    if (now - lastFogDriftPaint >= 1000 / 15) {
+      paintFogDrift(now);
+      lastFogDriftPaint = now;
+    }
+  };
+
+  const paintFogDriftIfDue = (now: number): void => {
+    paintFogReveal(now);
+    if (!view?.visibleCells || reducedMotion || now - lastFogDriftPaint < 1000 / 15) return;
+    paintFogDrift(now);
+    lastFogDriftPaint = now;
+  };
+
+  /** Лёгкий общий тон сцены, виньетка и редкое матовое зерно. Этот слой
+   * находится над world, но не участвует в боевом snapshot. */
+  const paintAtmosphere = (): void => {
+    if (!mounted || !view) return;
+    const width = app.renderer.width;
+    const height = app.renderer.height;
+    const darkness = view.darkness;
+    const ratio = darkness ? Math.min(1, Math.max(0, darkness.current / Math.max(1, darkness.max))) : 0;
+    const signature = `${width}:${height}:${view.matchSeed}:${darkness?.current ?? "none"}:${darkness?.max ?? "none"}:${reducedMotion}:${lowPowerDevice}`;
+    if (signature === atmosphereSignature) return;
+    atmosphereSignature = signature;
+    atmosphereLayer.clear();
+    atmosphereLayer.visible = true;
+
+    if (darkness) {
+      const coldAlpha = (reducedMotion ? 0.025 : 0.04) + ratio * (reducedMotion ? 0.08 : 0.15);
+      atmosphereLayer.rect(0, 0, width, height).fill({ color: 0x829bb7, alpha: coldAlpha });
+      // Нейтральная вуаль снижает насыщенность по мере роста Тьмы,
+      // холодный слой сверху сохраняет ощущение зимнего воздуха.
+      atmosphereLayer.rect(0, 0, width, height).fill({ color: 0xa0a8aa, alpha: ratio * (reducedMotion ? 0.02 : 0.06) });
+      atmosphereLayer.rect(0, 0, width, height).fill({ color: 0x101a29, alpha: ratio * (reducedMotion ? 0.035 : 0.075) });
+    }
+
+    const vignetteAlpha = lowPowerDevice || reducedMotion ? 0.045 : 0.075;
+    for (let i = 0; i < 3; i += 1) {
+      const inset = i * 12;
+      atmosphereLayer.rect(inset, inset, Math.max(1, width - inset * 2), Math.max(1, height - inset * 2))
+        .stroke({ width: 18, color: 0x05080a, alpha: vignetteAlpha / (i + 1) });
+    }
+
+    // Зерно строится один раз на размер/режим и не перерисовывается каждый кадр.
+    if (!lowPowerDevice && !reducedMotion) {
+      const cell = 17;
+      for (let y = 0; y < height; y += cell) {
+        for (let x = 0; x < width; x += cell) {
+          const grain = hashCell(Math.floor(x / cell), Math.floor(y / cell), 701 + view.matchSeed);
+          if (grain < 0.48) continue;
+          atmosphereLayer.rect(x + grain * 5, y + (1 - grain) * 5, 1, 1).fill({ color: grain > 0.74 ? 0xffffff : 0x050708, alpha: 0.025 });
+        }
+      }
+    }
   };
 
   const paintDebug = (): void => {
@@ -998,7 +1495,7 @@ export function createFieldRenderer(): FieldRenderer {
           fontFamily: "monospace",
           fontSize: 9,
           fill: 0xaaaaaa,
-          stroke: { color: 0x000000, width: 2 },
+          stroke: { color: COLORS.overlay.shadow, width: 2 },
         },
       });
       coordLabel.position.set(fx + 2, fy + 1);
@@ -1013,8 +1510,8 @@ export function createFieldRenderer(): FieldRenderer {
             fontFamily: "monospace",
             fontSize: 12,
             fontWeight: "700",
-            fill: 0xf3ecdc,
-            stroke: { color: 0x0c120c, width: 3 },
+            fill: COLORS.ui.white,
+            stroke: { color: COLORS.terrain.outline, width: 3 },
           },
         });
         mpLabel.anchor.set(1, 1);
@@ -1032,12 +1529,24 @@ export function createFieldRenderer(): FieldRenderer {
     const shown = display.get(entity.id);
     const deadNow = shown?.dead ?? entity.dead;
     if (deadNow) return;
-    const { cx, cy } = entityPixel(entity);
+    const ground = entityPixel(entity);
+    const selected = entity.id === view?.selectedId;
+    // Небольшое «дыхание» живых фишек. Фаза вычисляется из id сущности,
+    // поэтому декор стабилен и не вмешивается в воспроизводимость партии.
+    const now = performance.now();
+    const breath = !reducedMotion && !lowPowerDevice && !playing
+      ? Math.sin(now * (Math.PI * 2 / 2000) + hashCell(entity.id, 0, 101) * Math.PI * 2) * 1.35
+      : 0;
+    const cx = ground.cx;
     const dieStart = dying.get(entity.id);
-    const fade = dieStart === undefined ? 1 : Math.max(0.25, 1 - ((performance.now() - dieStart) / 430) * 0.75);
+    const deathProgress = dieStart === undefined ? 0 : Math.min(1, Math.max(0, (now - dieStart) / 700));
+    const cy = ground.cy - (selected ? 3 : 0) + breath + (deathPit.get(entity.id) ? deathProgress * 25 : deathProgress * 7);
+    const fade = dieStart === undefined || reducedMotion
+      ? 1
+      : Math.max(0.25, 1 - ((now - dieStart) / 700) * 0.75);
 
     if (entity.coverType > 0) {
-      drawCover(g, cx, cy, entity.coverType as 1 | 2, entity.edge);
+      drawCover(g, cx, cy, entity.coverType as 1 | 2, entity.edge, entity.x, entity.y, view?.biome ?? DEFAULT_BIOME);
       return;
     }
 
@@ -1061,75 +1570,47 @@ export function createFieldRenderer(): FieldRenderer {
         cy + forwardY * depth - sideY * width,
         cx + forwardX * depth + sideX * width,
         cy + forwardY * depth + sideY * width,
-      ]).fill({ color: 0xe8b64c, alpha: 0.08 });
+      ]).fill({ color: COLORS.ui.amberBright, alpha: 0.08 });
     }
 
-    g.ellipse(cx, cy + 15, 15, 5).fill({ color: 0x000000, alpha: 0.32 * fade });
-    g.circle(cx, cy, 17.5).fill(faction.ring);
-    g.circle(cx, cy, 17.5).stroke({ width: 1, color: faction.ringDark });
-    g.circle(cx, cy, 14.6).fill(faction.disc);
+    // Чем выше ярус, тем длиннее и светлее тень. Выбранная фишка чуть
+    // приподнята, поэтому её тень становится заметнее и отделяет её от поля.
+    const shadowWidth = 15 + Math.min(2, Math.max(0, entity.z)) * 3 + (selected ? 2 : 0);
+    const shadowY = ground.cy + 15 + Math.min(2, Math.max(0, entity.z)) * 3 + (selected ? 2 : 0);
+    const shadowAlpha = Math.max(0.12, 0.32 - Math.min(2, Math.max(0, entity.z)) * 0.07) * fade + (selected ? 0.08 : 0);
+    g.ellipse(ground.cx, shadowY, shadowWidth, 5).fill({ color: COLORS.overlay.shadow, alpha: shadowAlpha });
+    drawTokenBase(g, cx, cy, faction, entity.owner === 2);
 
     const art = CLASS_ART[entity.configId];
     if (art) art({ g, cx, cy });
     else g.circle(cx, cy, 10).fill(FALLBACK_ART[entity.owner === 2 ? "nav" : "druzhina"]);
 
-    const statusTime = performance.now() * 0.004;
-    if (entity.poison) {
-      for (let i = 0; i < 4; i += 1) {
-        const angle = statusTime + (i * Math.PI * 2) / 4;
-        const radius = 20 + Math.sin(statusTime * 1.7 + i) * 2;
-        g.circle(cx + Math.cos(angle) * radius, cy + Math.sin(angle) * radius - 2, 2.2 + (i % 2) * 0.7)
-          .fill({ color: 0x78d83d, alpha: 0.8 });
-        g.circle(cx + Math.cos(angle) * radius - 0.6, cy + Math.sin(angle) * radius - 2.8, 0.7)
-          .fill({ color: 0xd7ff8a, alpha: 0.9 });
-      }
-    }
-    if (entity.panic) {
-      const pulse = 0.55 + Math.sin(statusTime * 2.4) * 0.25;
-      g.circle(cx, cy, 22 + pulse * 3).stroke({ width: 2, color: 0xb94cff, alpha: pulse });
-      g.poly([cx - 2, cy - 27, cx + 2, cy - 27, cx + 1, cy - 18, cx - 1, cy - 18]).fill(0xff5e7a);
-      g.circle(cx, cy - 14.5, 1.8).fill(0xff5e7a);
-    }
-    if (entity.immobileTurns) {
-      for (let i = 0; i < 5; i += 1) {
-        const angle = (i / 5) * Math.PI * 2;
-        const x0 = cx + Math.cos(angle) * 10;
-        const y0 = cy + Math.sin(angle) * 8 + 9;
-        g.moveTo(x0, y0)
-          .quadraticCurveTo(cx + Math.cos(angle + 0.5) * 19, cy + 14, cx + Math.cos(angle) * 24, cy + 12)
-          .stroke({ width: 2, color: 0x6f8f3d, alpha: 0.9 });
-      }
-    }
-    if (entity.hidden) {
-      g.circle(cx, cy, 24).stroke({ width: 1.5, color: 0x8fd3bc, alpha: 0.5 + Math.sin(statusTime) * 0.2 });
-    }
-    if (entity.flying) {
-      const wing = 7 + Math.sin(statusTime * 2) * 3;
-      g.moveTo(cx - 13, cy - 2).quadraticCurveTo(cx - 21, cy - wing, cx - 24, cy + 2)
-        .stroke({ width: 2, color: 0xbfe8ff, alpha: 0.8 });
-      g.moveTo(cx + 13, cy - 2).quadraticCurveTo(cx + 21, cy - wing, cx + 24, cy + 2)
-        .stroke({ width: 2, color: 0xbfe8ff, alpha: 0.8 });
-    }
-    if (entity.timedLife !== undefined) {
-      const dots = Math.max(1, Math.min(6, entity.timedLife));
-      for (let i = 0; i < dots; i += 1) {
-        const angle = -Math.PI / 2 + (i / Math.max(1, dots)) * Math.PI * 2;
-        g.circle(cx + Math.cos(angle) * 22, cy + Math.sin(angle) * 22, 1.8).fill(0x5fd6e8);
-      }
-    }
-    if (entity.camouflageMinCover && view?.snapshot.entities.some((other) =>
+    const statusTime = (reducedMotion ? 0 : now) * 0.004;
+    const hasCamouflage = Boolean(entity.camouflageMinCover && view?.snapshot.entities.some((other) =>
       !other.dead && other.owner === entity.owner && other.id !== entity.id && other.providesCamouflage &&
       Math.max(Math.abs(other.x - entity.x), Math.abs(other.y - entity.y)) <= 1
-    )) {
-      for (let i = 0; i < 3; i += 1) {
-        const angle = statusTime * 0.5 + (i * Math.PI * 2) / 3;
-        g.ellipse(cx + Math.cos(angle) * 21, cy + Math.sin(angle) * 14, 3.5, 1.8).fill({ color: 0x7fb84d, alpha: 0.75 });
-      }
-    }
+    ));
+    const statuses: TokenStatus[] = [];
+    // Фиксированный приоритет: отрицательные состояния не уступают место
+    // декоративным режимам, если на фишке одновременно много признаков.
+    if (entity.panic) statuses.push("panic");
+    if (entity.poison) statuses.push("poison");
+    if (entity.immobileTurns) statuses.push("immobile");
+    if (entity.hidden) statuses.push("hidden");
+    if (entity.flying) statuses.push("flying");
+    if (entity.timedLife !== undefined) statuses.push("timed");
+    if (hasCamouflage) statuses.push("camouflage");
+    if (entity.defending) statuses.push("defending");
+    if (entity.overwatch) statuses.push("overwatch");
+    statuses.forEach((status, index) => {
+      const slot = TOKEN_STATUS_SLOTS[index];
+      if (!slot) return;
+      drawStatusBadge(g, cx + slot.x, cy + slot.y, status, statusTime * 2.4 + index, entity.timedLife);
+    });
 
     if (fade < 1) {
       // Угасание павшего: фигура темнеет к моменту смены на могильную отметину.
-      g.circle(cx, cy, 17.5).fill({ color: 0x101410, alpha: (1 - fade) * 0.85 });
+      g.circle(cx, cy, 17.5).fill({ color: COLORS.ui.fieldBackground, alpha: (1 - fade) * 0.85 });
     }
 
     // Клин взгляда по направлению (0 — север, далее по часовой).
@@ -1144,15 +1625,24 @@ export function createFieldRenderer(): FieldRenderer {
     ]).fill(faction.ring);
 
     if (entity.id === view?.selectedId) {
-      g.circle(cx, cy, 22).stroke({ width: 2.6, color: 0xe8b64c });
-      g.circle(cx, cy, 25.2).stroke({ width: 1, color: 0xe8b64c, alpha: 0.5 });
+      g.circle(cx, cy, 22).stroke({ width: 2.6, color: COLORS.ui.amberBright });
+      g.circle(cx, cy, 25.2).stroke({ width: 1, color: COLORS.ui.amberBright, alpha: 0.5 });
     }
     if (entity.id === view?.aimId) {
-      g.circle(cx, cy, 22).stroke({ width: 2.4, color: 0xaed581 });
+      const aimStatus = view?.aimStatus ?? (view?.aimOk ? "ready" : "selected");
+      const aimColor = aimStatus === "ready"
+        ? COLORS.targeting.ready
+        : aimStatus === "blocked"
+          ? COLORS.targeting.blocked
+          : COLORS.targeting.selected;
+      // Белый — предварительный выбор, янтарный — подтверждаемая атака,
+      // красный — цель сейчас недоступна. Цвет не смешивается с кольцом Нави.
+      g.circle(cx, cy, 22).stroke({ width: 2.8, color: aimColor });
+      g.circle(cx, cy, 25.2).stroke({ width: 1, color: aimColor, alpha: 0.42 });
     }
 
     if (flash > 0) {
-      g.circle(cx, cy, 20 + (1 - flash) * 6).stroke({ width: 2.6, color: 0xf3ecdc, alpha: flash });
+      g.circle(cx, cy, 20 + (1 - flash) * 6).stroke({ width: 2.6, color: COLORS.ui.white, alpha: flash });
     }
 
     const hp = shown?.hp ?? entity.hp;
@@ -1167,37 +1657,36 @@ export function createFieldRenderer(): FieldRenderer {
     for (let i = 0; i < entity.maxAp; i += 1) {
       const px0 = cx - ((entity.maxAp - 1) * 8) / 2 + i * 8;
       g.poly([px0, cy + 21.5, px0 + 2.9, cy + 24.4, px0, cy + 27.3, px0 - 2.9, cy + 24.4]).fill(
-        i < pips ? 0xe8b64c : 0x3a382e,
+        i < pips ? COLORS.ui.amberBright : 0x3a382e,
       );
     }
 
-    // Защитная стойка: щит-индикатор.
-    if (entity.defending) {
-      const sx = cx + 16;
-      const sy = cy - 18;
-      g.roundRect(sx - 5, sy - 6, 10, 12, 2).fill(0x388cdc);
-      g.roundRect(sx - 5, sy - 6, 10, 12, 2).stroke({ width: 1.2, color: 0x8fd0ff });
-      g.moveTo(sx, sy - 3).lineTo(sx, sy + 3).stroke({ width: 1.4, color: 0xf3ecdc });
-      g.moveTo(sx - 2.5, sy).lineTo(sx + 2.5, sy).stroke({ width: 1.4, color: 0xf3ecdc });
-    }
+  };
 
-    // Дозор: глаз-индикатор.
-    if (entity.overwatch) {
-      const ox = cx - 16;
-      const oy = cy - 18;
-      g.circle(ox, oy, 5.5).fill({ color: 0xe8b64c, alpha: 0.9 });
-      g.circle(ox, oy, 5.5).stroke({ width: 1, color: 0x57431a });
-      g.circle(ox, oy, 2.2).fill(0x1c1a20);
-      g.circle(ox + 0.8, oy - 0.8, 0.7).fill(0xf3ecdc);
-    }
+  const isMagicFx = (fx: Fx): boolean => {
+    if (["windup", "flash", "bolt", "status", "extract"].includes(fx.kind)) return true;
+    return fx.kind === "skill" && !["breach", "shield_bash", "circular_sweep", "objective"].includes(fx.style);
   };
 
-  const drawFxList = (g: Graphics, now: number): void => {
+  const drawFxList = (g: Graphics, now: number, pass: "physical" | "magic"): void => {
     for (let i = fxs.length - 1; i >= 0; i -= 1) {
+      const candidate = fxs[i];
+      if (!candidate || (pass === "magic") !== isMagicFx(candidate)) continue;
       const fx = fxs[i];
       if (!fx) continue;
+      if (fx.kind === "combatText") {
+        const t = (now - fx.start) / (760 / playbackSpeed);
+        if (t >= 1) {
+          floatingTextLayer.removeChild(fx.label);
+          fx.label.destroy();
+          fxs.splice(i, 1);
+          continue;
+        }
+        // Text is kept in its own world layer above fog and particles.
+        continue;
+      }
       if (fx.kind === "windup") {
-        const t = (now - fx.start) / 260;
+        const t = (now - fx.start) / (260 / playbackSpeed);
         if (t >= 1) {
           fxs.splice(i, 1);
           continue;
@@ -1206,7 +1695,7 @@ export function createFieldRenderer(): FieldRenderer {
         g.circle(fx.x, fx.y, 19 + t * 7).stroke({ width: 2, color, alpha: 0.75 * (1 - t) });
         g.circle(fx.x, fx.y, 24 + t * 9).stroke({ width: 1, color, alpha: 0.4 * (1 - t) });
       } else if (fx.kind === "bolt") {
-        const t = (now - fx.start) / fx.dur;
+        const t = (now - fx.start) / (fx.dur / playbackSpeed);
         if (t >= 1) {
           fxs.splice(i, 1);
           continue;
@@ -1215,6 +1704,17 @@ export function createFieldRenderer(): FieldRenderer {
         const bx = fx.x0 + (fx.x1 - fx.x0) * e;
         const by = fx.y0 + (fx.y1 - fx.y0) * e - Math.sin(Math.PI * t) * 13;
         const color = fx.warm ? 0xffd268 : 0xa8e063;
+        // Сужающаяся лента вместо набора разрозненных точек: хвост плавно
+        // следует за дугой снаряда и остаётся дешёвым одним полигоном.
+        const tailT = Math.max(0, e - 0.18);
+        const tailX = fx.x0 + (fx.x1 - fx.x0) * tailT;
+        const tailY = fx.y0 + (fx.y1 - fx.y0) * tailT - Math.sin(Math.PI * tailT) * 13;
+        const nx = -(by - tailY);
+        const ny = bx - tailX;
+        const nlen = Math.max(1, Math.hypot(nx, ny));
+        const width = 5.2 * (1 - t * 0.55);
+        g.poly([bx, by, tailX + (nx / nlen) * width, tailY + (ny / nlen) * width, tailX - (nx / nlen) * width, tailY - (ny / nlen) * width])
+          .fill({ color, alpha: 0.28 * (1 - t * 0.4) });
         for (let trail = 1; trail <= 3; trail += 1) {
           const tt = Math.max(0, e - trail * 0.03);
           const tx = fx.x0 + (fx.x1 - fx.x0) * tt;
@@ -1224,7 +1724,7 @@ export function createFieldRenderer(): FieldRenderer {
         g.circle(bx, by, 4.4).fill({ color, alpha: 0.85 });
         g.circle(bx, by, 1.9).fill(0xfffbe8);
       } else if (fx.kind === "flash") {
-        const t = (now - fx.start) / 340;
+        const t = (now - fx.start) / (340 / playbackSpeed);
         if (t >= 1) {
           fxs.splice(i, 1);
           continue;
@@ -1233,7 +1733,7 @@ export function createFieldRenderer(): FieldRenderer {
           g.circle(fx.x, fx.y, 8 + t * 16).stroke({ width: 2, color: 0xb9b4a4, alpha: 0.65 * (1 - t) });
           continue;
         }
-        const color = fx.crit ? 0xffd268 : 0xf3ecdc;
+        const color = fx.crit ? 0xffd268 : COLORS.ui.white;
         g.circle(fx.x, fx.y, 5 + t * (fx.crit ? 26 : 18)).stroke({ width: 2.6, color, alpha: 0.85 * (1 - t) });
         if (fx.crit) {
           g.circle(fx.x, fx.y, 3 + t * 14).stroke({ width: 1.4, color: 0xff9e4a, alpha: 0.8 * (1 - t) });
@@ -1248,7 +1748,7 @@ export function createFieldRenderer(): FieldRenderer {
             .stroke({ width: 1.6, color, alpha: (1 - t) * 0.9 });
         }
       } else if (fx.kind === "skill") {
-        const t = (now - fx.start) / fx.dur;
+        const t = (now - fx.start) / (fx.dur / playbackSpeed);
         if (t >= 1) {
           fxs.splice(i, 1);
           continue;
@@ -1257,18 +1757,18 @@ export function createFieldRenderer(): FieldRenderer {
           heal: [0x74e071, 0xd8ffd0],
           cleanse: [0x55ddea, 0xe5ffff],
           summon_forest_beast: [0x73b64b, 0xd8ef8a],
-          aimed_eye: [0xe8b64c, 0xfff0a0],
+          aimed_eye: [COLORS.ui.amberBright, 0xfff0a0],
           roots: [0x769845, 0x49351f],
           poison_needles: [0x83d632, 0xd6ff62],
           raise_skeleton: [0x66b849, 0xd9d5b5],
           panic: [0xa648e8, 0xff5e7a],
           create_illusion: [0x55d5e8, 0xc4f8ff],
           teleport_ally: [0x5398ff, 0x9ee9ff],
-          circular_sweep: [0xe8b64c, 0xffe8a0],
+          circular_sweep: [COLORS.ui.amberBright, 0xffe8a0],
           breach: [0xd47b39, 0xf1d19a],
           shield_bash: [0x78aee8, 0xe8f5ff],
         };
-        let [primary, secondary] = palette[fx.style] ?? [0x9ad27a, 0xf3ecdc];
+        let [primary, secondary] = palette[fx.style] ?? [0x9ad27a, COLORS.ui.white];
         if (!fx.success) [primary, secondary] = [0x6f7470, 0xb0b4ae];
         const px = fx.x0 + (fx.x1 - fx.x0) * easeOut(Math.min(1, t * 1.35));
         const py = fx.y0 + (fx.y1 - fx.y0) * easeOut(Math.min(1, t * 1.35));
@@ -1309,16 +1809,16 @@ export function createFieldRenderer(): FieldRenderer {
           }
         }
       } else if (fx.kind === "status") {
-        const t = (now - fx.start) / 520;
+        const t = (now - fx.start) / (520 / playbackSpeed);
         if (t >= 1) {
           fxs.splice(i, 1);
           continue;
         }
         const colors: Record<string, number> = {
-          POISON: 0x78d83d, PANIC: 0xb94cff, IMMOBILE: 0x709343, HIDDEN: 0x78c9b2,
-          FLYING: 0x9edfff, TIMED: 0x5fd6e8, DEFENDING: 0x68aee8, OVERWATCH: 0xe8b64c,
+          POISON: COLORS.status.poison, PANIC: COLORS.status.panic, IMMOBILE: 0x709343, HIDDEN: 0x78c9b2,
+          FLYING: 0x9edfff, TIMED: COLORS.status.timed, DEFENDING: COLORS.status.defending, OVERWATCH: COLORS.ui.amberBright,
         };
-        const color = colors[fx.status] ?? 0xf3ecdc;
+        const color = colors[fx.status] ?? COLORS.ui.white;
         const radius = 12 + t * 20;
         g.circle(fx.x, fx.y, radius).stroke({ width: fx.applied ? 2.5 : 1.5, color, alpha: 1 - t });
         for (let p = 0; p < 8; p += 1) {
@@ -1327,8 +1827,58 @@ export function createFieldRenderer(): FieldRenderer {
           g.circle(fx.x + Math.cos(angle) * radius, fx.y + Math.sin(angle) * radius, 2)
             .fill({ color, alpha: (1 - t) * 0.8 * direction * direction });
         }
+      } else if (fx.kind === "death") {
+        const t = (now - fx.start) / (700 / playbackSpeed);
+        if (t >= 1) {
+          fxs.splice(i, 1);
+          continue;
+        }
+        if (fx.pit) {
+          // Падение в яму: воронка и уход вниз, без разлетающихся осколков.
+          g.ellipse(fx.x, fx.y + 4 + t * 18, 18 * (1 - t * 0.45), 7 * (1 - t * 0.35))
+            .stroke({ width: 2, color: 0x25231e, alpha: 0.75 * (1 - t) });
+          g.circle(fx.x, fx.y + t * 15, 12 * (1 - t * 0.65)).fill({ color: 0x11140f, alpha: 0.3 * (1 - t) });
+          for (let p = 0; p < 5; p += 1) {
+            const angle = hashCell(fx.entityId, p, 187) * Math.PI * 2;
+            const radius = 17 - t * 13;
+            g.circle(fx.x + Math.cos(angle) * radius, fx.y + Math.sin(angle) * radius + t * 5, 1.2 + (p % 2))
+              .fill({ color: 0x746448, alpha: 0.8 * (1 - t) });
+          }
+        } else {
+          const debris = 11;
+          const fade = 1 - t;
+          for (let p = 0; p < debris; p += 1) {
+            const angle = hashCell(fx.entityId, p, 173) * Math.PI * 2;
+            const distance = (10 + hashCell(fx.entityId, p, 179) * 24) * easeOut(t);
+            const size = 1.4 + hashCell(fx.entityId, p, 181) * 2.2;
+            const dx = fx.x + Math.cos(angle) * distance;
+            const dy = fx.y + Math.sin(angle) * distance - t * 13;
+            if (p % 3 === 0) {
+              g.poly([dx, dy - size, dx + size, dy + size, dx - size, dy + size])
+                .fill({ color: p % 2 ? 0x5e5543 : 0x9c8452, alpha: fade });
+            } else {
+              g.rect(dx - size / 2, dy - size / 2, size, size)
+                .fill({ color: p % 2 ? 0x5e5543 : 0x9c8452, alpha: fade });
+            }
+          }
+          g.circle(fx.x, fx.y - t * 8, 8 + t * 12).stroke({ width: 2, color: 0xbca26d, alpha: 0.65 * fade });
+        }
+      } else if (fx.kind === "coverBreak") {
+        const t = (now - fx.start) / (420 / playbackSpeed);
+        if (t >= 1) {
+          fxs.splice(i, 1);
+          continue;
+        }
+        for (let p = 0; p < 6; p += 1) {
+          const angle = hashCell(fx.seed, p, 191) * Math.PI * 2;
+          const distance = (5 + hashCell(fx.seed, p, 197) * 22) * easeOut(t);
+          const size = 1.2 + hashCell(fx.seed, p, 199) * 2;
+          g.rect(fx.x + Math.cos(angle) * distance - size / 2, fx.y + Math.sin(angle) * distance - size / 2 + t * t * 14, size, size)
+            .fill({ color: p % 2 ? 0xa08050 : 0xd4ad6b, alpha: 0.9 * (1 - t) });
+        }
+        g.circle(fx.x, fx.y, 18 + t * 8).stroke({ width: 1.8, color: COLORS.ui.amberBright, alpha: 0.7 * (1 - t) });
       } else if (fx.kind === "poof") {
-        const t = (now - fx.start) / 430;
+        const t = (now - fx.start) / (430 / playbackSpeed);
         if (t >= 1) {
           fxs.splice(i, 1);
           continue;
@@ -1337,7 +1887,7 @@ export function createFieldRenderer(): FieldRenderer {
         g.circle(fx.x - 4, fx.y - t * 8, 4 + t * 8).fill({ color: 0x3a3a34, alpha: 0.35 * (1 - t) });
       } else if (fx.kind === "extract") {
         // Эвакуация: световой столб, поднимающиеся искры и растворяющийся силуэт (0.13.0).
-        const t = (now - fx.start) / 700;
+        const t = (now - fx.start) / (700 / playbackSpeed);
         if (t >= 1) {
           fxs.splice(i, 1);
           continue;
@@ -1359,14 +1909,30 @@ export function createFieldRenderer(): FieldRenderer {
     if (!view || destroyed || !mounted) return;
     const g = fxLayer;
     g.clear();
+    magicLayer.clear();
     const now = performance.now();
+    const motionNow = reducedMotion ? 0 : now;
+    if (reducedMotion) fxs.length = 0;
+
+    // Областной прицел показывает ровно геометрию ядра: горизонтальный
+    // радиус и не более одного яруса перепада. Клетки не зависят от FPS.
+    if (view.areaPreview) {
+      const { center, radius } = view.areaPreview;
+      for (const tile of view.snapshot.grid.tiles) {
+        if (distH(center.x, center.y, tile.x, tile.y) > radius || Math.abs(center.z - tile.z) > 1) continue;
+        if (view.visibleCells && !view.visibleCells.has(`${tile.x},${tile.y}`)) continue;
+        const { fx, fy } = faceOf(tile.x, tile.y, visualLevel(tile));
+        g.rect(fx + 2, fy + 2, CELL_SIZE - 4, CELL_SIZE - 4).fill({ color: COLORS.ui.amberBright, alpha: 0.18 });
+        g.rect(fx + 3, fy + 3, CELL_SIZE - 6, CELL_SIZE - 6).stroke({ width: 2, color: COLORS.ui.amberBright, alpha: 0.82 });
+      }
+    }
 
     // Movement and route previews are dynamic UI, not part of cached terrain.
     for (const cell of view.reachable) {
       const tile = tileAt(view.snapshot.grid, cell.x, cell.y);
       if (!tile) continue;
       const { fx, fy } = faceOf(tile.x, tile.y, visualLevel(tile));
-      const tint = cell.apCost === 1 ? 0x388cdc : 0xe0b34a;
+      const tint = cell.apCost === 1 ? COLORS.ui.movement : COLORS.ui.amber;
       g.rect(fx + 1, fy + 1, CELL_SIZE - 2, CELL_SIZE - 2).fill({ color: tint, alpha: 0.32 });
       g.rect(fx + 2.5, fy + 2.5, CELL_SIZE - 5, CELL_SIZE - 5).stroke({ width: 1.8, color: tint, alpha: 0.9 });
     }
@@ -1380,13 +1946,23 @@ export function createFieldRenderer(): FieldRenderer {
 
     // Зона эвакуации: пульсирующее свечение у края поля (0.13.0).
     // Рисуется под туманом войны: разведанные клетки зоны читаются как «выход».
-    const extractPulse = 0.5 + Math.sin(now * 0.0022) * 0.25;
+    const extractPulse = 0.5 + Math.sin(motionNow * 0.0022) * 0.25;
     for (const tile of view.snapshot.grid.tiles) {
       if (!tile.extract) continue;
       const z = visualLevel(tile);
       const { fx, fy } = faceOf(tile.x, tile.y, z);
       g.rect(fx + 2, fy + 2, CELL_SIZE - 4, CELL_SIZE - 4).fill({ color: 0xe8c96a, alpha: 0.08 + extractPulse * 0.07 });
       g.rect(fx + 2, fy + 2, CELL_SIZE - 4, CELL_SIZE - 4).stroke({ width: 1.4, color: 0xe8c96a, alpha: 0.3 + extractPulse * 0.35 });
+      // Портальный контур поверх подсветки: арка и порог явно обозначают
+      // выход, не конкурируя с иконкой щита и не меняя семантику ядра.
+      const portalColor = COLORS.ui.success;
+      g.moveTo(fx + 11, fy + C - 7)
+        .lineTo(fx + 11, fy + 23)
+        .quadraticCurveTo(fx + C / 2, fy + 8, fx + C - 11, fy + 23)
+        .lineTo(fx + C - 11, fy + C - 7)
+        .stroke({ width: 2.4, color: portalColor, alpha: 0.72 + extractPulse * 0.22 });
+      g.moveTo(fx + 7, fy + C - 6).lineTo(fx + C - 7, fy + C - 6)
+        .stroke({ width: 2, color: portalColor, alpha: 0.58 + extractPulse * 0.18 });
       // Стрелка выхода по центру клетки.
       g.poly([
         fx + CELL_SIZE / 2, fy + 5,
@@ -1401,8 +1977,8 @@ export function createFieldRenderer(): FieldRenderer {
       if (tile.homeOwner === undefined) continue;
       const z = visualLevel(tile);
       const { fx, fy } = faceOf(tile.x, tile.y, z);
-      const color = tile.homeOwner === 1 ? 0xe0b34a : 0x6aa9d9;
-      const pulse = 0.5 + Math.sin(now * 0.0028 + tile.homeOwner) * 0.25;
+      const color = tile.homeOwner === 1 ? COLORS.ui.amber : 0x6aa9d9;
+      const pulse = 0.5 + Math.sin(motionNow * 0.0028 + tile.homeOwner) * 0.25;
       g.rect(fx + 2, fy + 2, CELL_SIZE - 4, CELL_SIZE - 4).stroke({
         width: 1.6,
         color,
@@ -1424,11 +2000,11 @@ export function createFieldRenderer(): FieldRenderer {
         const { fx, fy } = faceOf(tile.x, tile.y, z);
         const cx = fx + CELL_SIZE / 2;
         const cy = fy + CELL_SIZE / 2 + 4;
-        const pulse = 0.5 + Math.sin(now * 0.003) * 0.25;
+        const pulse = 0.5 + Math.sin(motionNow * 0.003) * 0.25;
         // Свечение.
         g.circle(cx, cy, 10 + pulse * 3).fill({ color: 0xe06a4a, alpha: 0.18 + pulse * 0.1 });
         // Тень.
-        g.ellipse(cx, cy + 7, 5, 2).fill({ color: 0x000000, alpha: 0.35 });
+        g.ellipse(cx, cy + 7, 5, 2).fill({ color: COLORS.overlay.shadow, alpha: 0.35 });
         // Яблоко.
         g.circle(cx, cy, 5.5 + pulse * 0.5).fill(0xd94a3a).stroke({ width: 1, color: 0x8a2a1e });
         // Блик.
@@ -1439,47 +2015,13 @@ export function createFieldRenderer(): FieldRenderer {
       }
     }
 
-    // Туман войны: анимированный оверлей поверх рельефа.
-    if (view.visibleCells) {
-      const C = CELL_SIZE;
-      const slowT = now * 0.0003; // медленная анимация
-      for (const tile of view.snapshot.grid.tiles) {
-        const key = `${tile.x},${tile.y}`;
-        const isVisible = view.visibleCells.has(key);
-        const isExplored = view.exploredCells?.has(key) ?? false;
-        const z = visualLevel(tile);
-        const { fx, fy } = faceOf(tile.x, tile.y, z);
-
-        if (!isVisible && !isExplored) {
-          // Неразведанная клетка: полностью скрыта.
-          g.rect(fx, fy, C, C).fill({ color: 0x080a0c, alpha: 1.0 });
-        } else if (!isVisible && isExplored) {
-          // Ранее виденная: затемнение + анимированный туман.
-          g.rect(fx, fy, C, C).fill({ color: 0x0c1218, alpha: 0.6 });
-          const fogSeed = tile.x * 7919 + tile.y * 6271;
-          for (let i = 0; i < 3; i += 1) {
-            const h1 = ((fogSeed * (i + 1) * 2654435761) >>> 0) / 4294967296;
-            const h2 = (((fogSeed + 31) * (i + 7) * 2246822519) >>> 0) / 4294967296;
-            const phase = slowT + h1 * 6.28;
-            const drift = Math.sin(phase) * 4;
-            const driftY = Math.cos(phase * 0.7 + i) * 3;
-            const cx = fx + h1 * C + drift;
-            const cy = fy + h2 * C + driftY;
-            const fr = 10 + h1 * 16;
-            const alpha = 0.05 + 0.03 * Math.sin(phase * 1.3 + i * 2.1);
-            g.circle(cx, cy, fr).fill({ color: 0x8a9aaa, alpha });
-          }
-        }
-      }
-    }
-
     // Линия прицеливания: всегда прямая от A к Ц, меняет стиль в точке препятствия.
     const selected = view.snapshot.entities.find((entity) => entity.id === view?.selectedId);
     const aimed = view.snapshot.entities.find((entity) => entity.id === view?.aimId);
     if (selected && aimed && !selected.dead && !aimed.dead) {
       const a = entityPixel(selected);
       const b = entityPixel(aimed);
-      const color = view.aimOk ? 0xe8b64c : 0xc45c5c;
+      const color = view.aimOk ? COLORS.ui.amberBright : COLORS.ui.danger;
 
       // Вычислить точку разрыва на прямой A→Ц.
       let breakRatio = 1; // 1 = нет разрыва
@@ -1498,18 +2040,21 @@ export function createFieldRenderer(): FieldRenderer {
         }
       }
 
+      // Тёмная подложка держит луч читаемым на любом ярусе; поверх неё
+      // идёт контрастная янтарная/красная линия.
+      g.moveTo(a.cx, a.cy).lineTo(b.cx, b.cy).stroke({ width: 7, color: COLORS.overlay.shadow, alpha: 0.72 });
       if (breakRatio < 1) {
         // Точка разрыва на прямой.
         const bx = a.cx + (b.cx - a.cx) * breakRatio;
         const by = a.cy + (b.cy - a.cy) * breakRatio;
         // Сплошная часть.
-        g.moveTo(a.cx, a.cy).lineTo(bx, by).stroke({ width: 2, color, alpha: 0.85 });
+        g.moveTo(a.cx, a.cy).lineTo(bx, by).stroke({ width: 3, color, alpha: 0.95 });
         // Пунктирная часть от разрыва до цели.
         const dx = b.cx - bx;
         const dy = b.cy - by;
         const len = Math.hypot(dx, dy);
-        const dashLen = 6;
-        const gapLen = 5;
+        const dashLen = 10;
+        const gapLen = 4;
         const steps = Math.max(1, Math.floor(len / (dashLen + gapLen)));
         const ux = dx / (len || 1);
         const uy = dy / (len || 1);
@@ -1520,13 +2065,16 @@ export function createFieldRenderer(): FieldRenderer {
           pos += dashLen;
           const x1 = bx + ux * Math.min(pos, len);
           const y1 = by + uy * Math.min(pos, len);
-          g.moveTo(x0, y0).lineTo(x1, y1).stroke({ width: 1.5, color, alpha: 0.4 });
+          g.moveTo(x0, y0).lineTo(x1, y1).stroke({ width: 2.5, color, alpha: 0.68 });
           pos += gapLen;
           if (pos >= len) break;
         }
       } else {
-        g.moveTo(a.cx, a.cy).lineTo(b.cx, b.cy).stroke({ width: 2, color, alpha: 0.85 });
+        g.moveTo(a.cx, a.cy).lineTo(b.cx, b.cy).stroke({ width: 3, color, alpha: 0.95 });
       }
+      // Точка-якорь повторяет цвет луча прямо на цели.
+      g.circle(b.cx, b.cy, 4.2).fill({ color, alpha: 0.92 });
+      g.circle(b.cx, b.cy, 7.5).stroke({ width: 1.2, color, alpha: 0.55 });
 
       if (view.heightMod !== 0) {
         const mx = (a.cx + b.cx) / 2;
@@ -1567,6 +2115,28 @@ export function createFieldRenderer(): FieldRenderer {
       drawToken(g, entity);
     }
 
+    // Фланг читается отдельно от кольца цели: четыре пульсирующих красных
+    // уголка и короткий маячок предупреждают об угрозе, не меняя семантику
+    // синего кольца Нави.
+    if (view.flanked && view.aimId !== null) {
+      const flanked = view!.snapshot.entities.find((entity) => entity.id === view!.aimId);
+      if (flanked && !flanked.dead) {
+        const { cx, cy } = entityPixel(flanked);
+        const pulse = 0.55 + Math.sin(motionNow * 0.007) * 0.35;
+        const arm = 9 + pulse * 3;
+        const inset = 17;
+        const red = COLORS.ui.danger;
+        for (const [dx, dy] of [[-1, -1], [1, -1], [-1, 1], [1, 1]] as const) {
+          const x = cx + dx * inset;
+          const y = cy + dy * inset;
+          g.moveTo(x, y + dy * arm).lineTo(x, y).lineTo(x + dx * arm, y)
+            .stroke({ width: 3, color: red, alpha: pulse });
+        }
+        g.poly([cx, cy - 31 - pulse * 4, cx - 5, cy - 21, cx + 5, cy - 21])
+          .fill({ color: red, alpha: 0.75 + pulse * 0.2 });
+      }
+    }
+
     // Подсветка защищённых граней выбранного персонажа.
     if (view.selectedId !== null) {
       const sel = view.snapshot.entities.find((e) => e.id === view!.selectedId);
@@ -1587,7 +2157,7 @@ export function createFieldRenderer(): FieldRenderer {
 
     // Подсветка обучающего указания (0.19.0; строгий сценарий 0.20.13):
     // поле приглушается «прожектором», а цель указания — единственный яркий
-    // элемент. Прежде золотистая рамка (0xe0b34a) была почти неотличима на
+    // элемент. Прежде золотистая рамка (COLORS.ui.amber) была почти неотличима на
     // жёлтой подсветке клеток рывка — теперь ледяной голубо-белый маркер
     // контрастен и янтарным, и синим клеткам достижимости.
     if (view.trainingFocus || view.trainingHighlight) {
@@ -1625,7 +2195,7 @@ export function createFieldRenderer(): FieldRenderer {
         g.rect(inside.x1, inside.y0, outside.x1 - inside.x1, inside.y1 - inside.y0).fill({ color: dimColor, alpha });
       };
       if (view.trainingFocus) {
-        const pulseDim = 0.5 + Math.sin(now * 0.0021) * 0.04;
+        const pulseDim = 0.5 + Math.sin(motionNow * 0.0021) * 0.04;
         if (cellRect) {
           const clamp = (r: { x0: number; y0: number; x1: number; y1: number }) => ({
             x0: Math.max(r.x0, outer.x0),
@@ -1638,17 +2208,19 @@ export function createFieldRenderer(): FieldRenderer {
           const hole2 = clamp({ x0: cellRect.fx - m2, y0: cellRect.fy - m2, x1: cellRect.fx + CELL_SIZE + m2, y1: cellRect.fy + CELL_SIZE + m2 });
           const hole1 = clamp({ x0: cellRect.fx - m1, y0: cellRect.fy - m1, x1: cellRect.fx + CELL_SIZE + m1, y1: cellRect.fy + CELL_SIZE + m1 });
           const hole = clamp({ x0: cellRect.fx - 2, y0: cellRect.fy - 2, x1: cellRect.fx + CELL_SIZE + 2, y1: cellRect.fy + CELL_SIZE + 2 });
-          frameRects(outer, hole2, 0.5 * pulseDim + 0.14);
-          frameRects(hole2, hole1, 0.28 * pulseDim + 0.06);
-          frameRects(hole1, hole, 0.12 * pulseDim);
+          // Около трети непрозрачности: поле сохраняет контекст, а цель
+          // всё ещё доминирует без прежней тяжёлой пелены.
+          frameRects(outer, hole2, 0.34 * pulseDim + 0.12);
+          frameRects(hole2, hole1, 0.16 * pulseDim + 0.04);
+          frameRects(hole1, hole, 0.08 * pulseDim);
         } else {
-          frameRects(outer, { x0: outer.x1, y0: outer.y1, x1: outer.x0, y1: outer.y0 }, 0.38 * pulseDim + 0.06);
+          frameRects(outer, { x0: outer.x1, y0: outer.y1, x1: outer.x0, y1: outer.y0 }, 0.24 * pulseDim + 0.08);
         }
       }
 
       // Яркий маркер цели указания: рамка со свечением, уголки и маяк.
       if (highlight && cellRect && tile) {
-        const pulse = 0.5 + Math.sin(now * 0.005) * 0.5;
+        const pulse = 0.5 + Math.sin(motionNow * 0.005) * 0.5;
         const fx = cellRect.fx;
         const fy = cellRect.fy;
         const C = CELL_SIZE;
@@ -1671,7 +2243,7 @@ export function createFieldRenderer(): FieldRenderer {
         corner(fx + inset, fy + C - inset, 1, -1);
         corner(fx + C - inset, fy + C - inset, -1, -1);
         // Маяк над клеткой: покачивающийся треугольник «сюда».
-        const bob = Math.sin(now * 0.006) * 3;
+        const bob = Math.sin(motionNow * 0.006) * 3;
         const bx = fx + C / 2;
         const by = fy - 12 - bob;
         g.poly([bx, by + 8, bx - 6, by - 2, bx + 6, by - 2]).fill({ color: 0xf4feff, alpha: 0.65 + pulse * 0.35 });
@@ -1690,7 +2262,23 @@ export function createFieldRenderer(): FieldRenderer {
       drawAimIntersections(g, view);
     }
 
-    drawFxList(g, now);
+    if (!reducedMotion) {
+      drawFxList(magicLayer, now, "magic");
+      drawFxList(g, now, "physical");
+    }
+    for (let i = floatingTextLayer.children.length - 1; i >= 0; i -= 1) {
+      const child = floatingTextLayer.children[i];
+      if (!child) continue;
+      const fx = fxs.find((item) => item.kind === "combatText" && item.label === child);
+      if (!fx || fx.kind !== "combatText") {
+        floatingTextLayer.removeChild(child);
+        child.destroy();
+      } else {
+        const t = Math.min(1, (now - fx.start) / 760);
+        fx.label.position.set(fx.x, fx.y - t * 24);
+        fx.label.alpha = 1 - t;
+      }
+    }
   };
 
   const paint = (): void => {
@@ -1699,20 +2287,27 @@ export function createFieldRenderer(): FieldRenderer {
 
   /* ---------- хореография событий ---------- */
 
-  const wait = (ms: number): Promise<void> =>
-    new Promise((resolve) => {
-      window.setTimeout(resolve, ms);
+  const wait = (ms: number): Promise<void> => {
+    if (reducedMotion || ms <= 0) return Promise.resolve();
+    return new Promise((resolve) => {
+      window.setTimeout(resolve, ms / playbackSpeed);
     });
+  };
 
   const tween = (ms: number, step: (t: number) => void): Promise<void> =>
     new Promise((resolve) => {
+      if (reducedMotion || ms <= 0) {
+        step(1);
+        resolve();
+        return;
+      }
       const started = performance.now();
       const frame = (): void => {
         if (destroyed) {
           resolve();
           return;
         }
-        const t = Math.min(1, (performance.now() - started) / ms);
+        const t = Math.min(1, (performance.now() - started) / (ms / playbackSpeed));
         step(t);
         if (t >= 1) resolve();
         else requestAnimationFrame(frame);
@@ -1814,14 +2409,15 @@ export function createFieldRenderer(): FieldRenderer {
   };
 
   const shake = async (strength: number): Promise<void> => {
+    if (reducedMotion) return;
     const ox = world.x;
     const oy = world.y;
     await tween(150, (t) => {
       const amp = (1 - t) * strength;
-      // Локальный шум тряски камеры: визуальный эффект не использует
-      // стандартный генератор среды и не влияет на тактическое состояние.
-      world.x = ox + (shakeNoise() * 2 - 1) * amp;
-      world.y = oy + (shakeNoise() * 2 - 1) * amp;
+      // Периодический профиль тряски детерминирован временем анимации:
+      // источник случайности партии и системный генератор не используются.
+      world.x = ox + Math.sin(t * Math.PI * 12) * amp;
+      world.y = oy + Math.cos(t * Math.PI * 10 + 0.7) * amp;
     });
     world.x = ox;
     world.y = oy;
@@ -1829,6 +2425,31 @@ export function createFieldRenderer(): FieldRenderer {
 
   const entityById = (id: number): EntityState | undefined => view?.snapshot.entities.find((e) => e.id === id);
 
+  const addCombatText = (x: number, y: number, text: string, color: number, critical = false): void => {
+    // Ограниченный пул не даёт массовому бою разрастись в сотни Pixi Text.
+    while (fxs.filter((item) => item.kind === "combatText").length >= 18) {
+      const oldest = fxs.find((item) => item.kind === "combatText");
+      if (!oldest || oldest.kind !== "combatText") break;
+      floatingTextLayer.removeChild(oldest.label);
+      oldest.label.destroy();
+      fxs.splice(fxs.indexOf(oldest), 1);
+    }
+    const label = new Text({
+      text,
+      style: {
+        fontFamily: "system-ui, sans-serif",
+        fontSize: critical ? 25 : 18,
+        fontWeight: critical ? "900" : "800",
+        fill: color,
+        stroke: { color: 0x090b0a, width: critical ? 5 : 3 },
+      },
+    });
+    label.anchor.set(0.5, 0.5);
+    label.position.set(x, y);
+    floatingTextLayer.addChild(label);
+    fxs.push({ kind: "combatText", x, y, start: performance.now(), text, color, critical, label });
+  };
+
   const playCombat = async (event: Extract<GameEvent, { type: "COMBAT_RESOLVED" }>): Promise<void> => {
     const source = entityById(event.sourceId);
     const target = entityById(event.targetId);
@@ -1881,19 +2502,18 @@ export function createFieldRenderer(): FieldRenderer {
       if (miss) {
         fxs.push({ kind: "flash", x: to.cx, y: to.cy, start: performance.now(), crit: false, miss: true, angle });
         bumps.set(event.targetId, { dx: Math.cos(angle + Math.PI / 2) * 6, dy: Math.sin(angle + Math.PI / 2) * 6 });
-        window.setTimeout(() => bumps.delete(event.targetId), 180);
+        window.setTimeout(() => bumps.delete(event.targetId), 180 / playbackSpeed);
         return;
       }
       fxs.push({ kind: "flash", x: to.cx, y: to.cy, start: performance.now(), crit, miss: false, angle });
       flashes.set(event.targetId, 1);
-      window.setTimeout(() => flashes.delete(event.targetId), 260);
+      window.setTimeout(() => flashes.delete(event.targetId), 260 / playbackSpeed);
       // Попадание отбрасывает цель от нападающего; здоровье падает именно в момент удара.
       bumps.set(event.targetId, { dx: Math.cos(angle) * (crit ? 10 : 7), dy: Math.sin(angle) * (crit ? 10 : 7) });
-      window.setTimeout(() => bumps.delete(event.targetId), 200);
+      window.setTimeout(() => bumps.delete(event.targetId), 200 / playbackSpeed);
       const shown = display.get(event.targetId);
       if (shown) shown.hp = Math.max(0, shown.hp - event.damageDealt);
-      if (crit) void shake(4.5);
-      else void shake(2.6);
+      if (event.shake && event.shake > 0) void shake(event.shake);
     }
   };
 
@@ -1948,6 +2568,25 @@ export function createFieldRenderer(): FieldRenderer {
       display.delete(event.entityId);
       return;
     }
+    if (event.type === "COVER_DESTROYED") {
+      const at = centerOf(event.gridPos.x, event.gridPos.y, event.gridPos.z);
+      fxs.push({ kind: "coverBreak", x: at.cx, y: at.cy, start: performance.now(), seed: event.gridPos.x * 977 + event.gridPos.y * 613 });
+      if (event.shake && event.shake > 0) void shake(event.shake);
+      await wait(180);
+      return;
+    }
+    if (event.type === "OVERWATCH_FIRED") {
+      const at = centerOf(event.at.x, event.at.y, event.at.z);
+      fxs.push({ kind: "flash", x: at.cx, y: at.cy, start: performance.now(), crit: false, miss: false, angle: 0 });
+      await wait(120);
+      return;
+    }
+    if (event.type === "OBJECTIVE_CHANGED") {
+      const at = centerOf(event.pos.x, event.pos.y, event.pos.z);
+      fxs.push({ kind: "skill", x0: at.cx, y0: at.cy, x1: at.cx, y1: at.cy, start: performance.now(), dur: 430, style: "objective", success: true });
+      await wait(150);
+      return;
+    }
     if (event.type === "ENTITY_MOVED") {
       const moved = event.path;
       if (moved.length === 0) return;
@@ -1996,17 +2635,34 @@ export function createFieldRenderer(): FieldRenderer {
           shown.y = event.from.y + (event.to.y - event.from.y) * t;
           shown.z = event.from.z + (event.to.z - event.from.z) * t;
         });
+        if (event.shake && event.shake > 0) void shake(event.shake);
       }
       return;
     }
     if (event.type === "COMBAT_RESOLVED") {
       await playCombat(event);
+      const at = displayPixel(event.targetId);
+      if (at) {
+        const text = event.result === "MISS"
+          ? (view?.combatLabels?.miss ?? "MISS")
+          : `−${event.damageDealt}`;
+        const color = event.result === "MISS"
+          ? 0xc8c8bf
+          : event.result === "CRIT"
+            ? COLORS.ui.amberBright
+            : COLORS.ui.white;
+        addCombatText(at.cx, at.cy - 26, text, color, event.result === "CRIT");
+      }
       return;
     }
     if (event.type === "STAT_CHANGED") {
       const shown = display.get(event.entityId);
       if (shown && event.stat === "HP") {
         shown.hp = Math.min(shown.maxHp, Math.max(0, event.newValue));
+        if (event.delta > 0) {
+          const at = displayPixel(event.entityId);
+          if (at) addCombatText(at.cx, at.cy - 26, `+${event.delta}`, COLORS.ui.success);
+        }
       }
       return;
     }
@@ -2016,10 +2672,13 @@ export function createFieldRenderer(): FieldRenderer {
       const entity = entityById(event.entityId);
       if (entity) {
         const { cx, cy } = entityPixel(entity);
-        fxs.push({ kind: "poof", x: cx, y: cy, start: performance.now() });
-        dying.set(event.entityId, performance.now());
-        await wait(430);
+        const deathStart = performance.now();
+        fxs.push({ kind: "death", x: cx, y: cy, start: deathStart, entityId: event.entityId, pit: event.causeOfDeath === "FALL_INTO_PIT" });
+        dying.set(event.entityId, deathStart);
+        deathPit.set(event.entityId, event.causeOfDeath === "FALL_INTO_PIT");
+        await wait(700);
         dying.delete(event.entityId);
+        deathPit.delete(event.entityId);
       }
       if (shown) shown.dead = true;
       return;
@@ -2089,9 +2748,9 @@ export function createFieldRenderer(): FieldRenderer {
       const b = pts[1];
       if (!a || !b) return;
       const dist = Math.hypot(a.x - b.x, a.y - b.y);
-      world.scale.set(Math.min(1.8, Math.max(0.55, world.scale.x * (dist / pinch))));
+      const midpoint = { x: (a.x + b.x) / 2, y: (a.y + b.y) / 2 };
+      zoomAt(midpoint.x, midpoint.y, dist / pinch);
       pinch = dist;
-      userMoved = true;
       return;
     }
     if (!drag) {
@@ -2134,9 +2793,11 @@ export function createFieldRenderer(): FieldRenderer {
 
   const onWheel = (event: WheelEvent): void => {
     event.preventDefault();
-    world.x -= event.deltaX;
-    world.y -= event.deltaY;
-    userMoved = true;
+    // Колесо масштабирует вокруг курсора: точка поля под указателем не
+    // «убегает» в сторону. Диапазон оставлен прежним (pinch: 0.55–1.8).
+    if (event.deltaY === 0) return;
+    const factor = Math.pow(1.0018, -event.deltaY);
+    zoomAt(event.offsetX, event.offsetY, factor);
   };
 
   const onContext = (event: Event): void => {
@@ -2146,6 +2807,8 @@ export function createFieldRenderer(): FieldRenderer {
   /** Холст сменил размер (поворот устройства, окно): цель шага обучения
    * перепланируется к подводке по новым границам экрана. */
   const onCanvasResize = (): void => {
+    atmosphereSignature = null;
+    paintAtmosphere();
     if (!view?.trainingFocus || !view.trainingHighlight) return;
     const point = trainingHighlightPoint(view.trainingHighlight);
     if (point) pendingTrainingFocus = point;
@@ -2153,7 +2816,13 @@ export function createFieldRenderer(): FieldRenderer {
 
   const animLoop = (): void => {
     if (destroyed) return;
-    if (!playing && view?.visibleCells) {
+    const now = performance.now();
+    // Туман имеет собственный редкий ритм, независимый от частоты кадров
+    // основной сцены. На слабом устройстве динамический слой ограничен 30 Гц:
+    // поле остаётся отзывчивым, а статичные слои не перерисовываются.
+    paintFogDriftIfDue(now);
+    if (!playing && (!lowPowerDevice || now - lastDynamicPaint >= 33)) {
+      lastDynamicPaint = now;
       paintFx();
     }
     driveTrainingFocus();
@@ -2164,9 +2833,11 @@ export function createFieldRenderer(): FieldRenderer {
     async mount(element) {
       if (destroyed) return;
       const common = {
-        background: 0x101410,
+        background: COLORS.ui.fieldBackground,
         antialias: true,
-        resolution: Math.min(window.devicePixelRatio || 1, 2),
+        // Плотность пикселей ограничена на слабых устройствах: пиксельный
+        // размер не меняет правила или геометрию, но заметно снижает fill-rate.
+        resolution: Math.min(window.devicePixelRatio || 1, lowPowerDevice ? 1.5 : 2),
         autoDensity: true,
         resizeTo: element,
         preferWebGLVersion: 2 as const,
@@ -2186,7 +2857,7 @@ export function createFieldRenderer(): FieldRenderer {
       canvas.style.height = "100%";
       canvas.style.touchAction = "none";
       element.appendChild(canvas);
-      app.stage.addChild(world);
+      app.stage.addChild(world, atmosphereLayer);
       // Поворот экрана или изменение окна: цель урока может оказаться за
       // пределами новой «зоны комфорта» — перепланировать подводку.
       app.renderer.on("resize", onCanvasResize);
@@ -2200,11 +2871,14 @@ export function createFieldRenderer(): FieldRenderer {
       mounted = true;
       fit();
       paintStatic();
+      if (view) paintFog(view);
       paintFx();
+      paintAtmosphere();
       animFrame = requestAnimationFrame(animLoop);
     },
     update(next) {
       view = next;
+      reducedMotion = Boolean(next.reducedMotion);
       // Подводка камеры (0.20.14): новая цель указания планируется здесь;
       // исполняется в простое между анимациями событий (driveTrainingFocus).
       armTrainingFocus(next);
@@ -2225,9 +2899,12 @@ export function createFieldRenderer(): FieldRenderer {
       }
       // The map seed changes only when a battlefield is generated. State updates
       // redraw overlays/tokens, never the cached terrain graphics.
-      if (terrainSeed !== next.matchSeed) paintStatic();
+      const nextTerrainSeed = `${next.matchSeed}:${next.biome ?? DEFAULT_BIOME}`;
+      if (terrainSeed !== nextTerrainSeed) paintStatic();
+      paintFog(next);
       paintDebug();
       fit();
+      paintAtmosphere();
       paint();
     },
     play(events) {
@@ -2242,6 +2919,29 @@ export function createFieldRenderer(): FieldRenderer {
       world.y += dy;
       userMoved = true;
     },
+    screenPosition(x, y, z) {
+      if (!mounted) return null;
+      const point = centerOf(x, y, z);
+      return {
+        x: world.x + point.cx * world.scale.x,
+        y: world.y + point.cy * world.scale.y,
+      };
+    },
+    centerOn(x, y, z) {
+      if (!mounted || !view) return;
+      const point = centerOf(x, y, z);
+      const screen = { width: app.renderer.width, height: app.renderer.height };
+      const map = mapPlane();
+      const next = centerCameraOn(
+        { x: point.cx, y: point.cy },
+        { scale: world.scale.x, offset: { x: world.x, y: world.y } },
+        screen,
+        map,
+      );
+      world.x = next.offset.x;
+      world.y = next.offset.y;
+      userMoved = true;
+    },
     destroy() {
       destroyed = true;
       cancelAnimationFrame(animFrame);
@@ -2273,5 +2973,8 @@ export function createFieldRenderer(): FieldRenderer {
     setOnHover(handler) {
       onHover = handler;
     },
+    setPlaybackSpeed(multiplier) {
+      playbackSpeed = multiplier === 2 ? 2 : 1;
+    },
   };
 }
diff --git a/app/packages/render/src/index.ts b/app/packages/render/src/index.ts
index 0a5f766..a632522 100644
--- a/app/packages/render/src/index.ts
+++ b/app/packages/render/src/index.ts
@@ -3,10 +3,15 @@ export type { FieldRenderer, FieldView } from "./field-renderer.js";
 export {
   TRAINING_COMFORT,
   needsTrainingFocus,
+  centerCameraOn,
   trainingGlideOffset,
   worldToScreen,
+  zoomAroundPoint,
   type CameraPlane,
   type MapPlane,
   type Point,
   type ScreenSize,
 } from "./camera.js";
+
+export { BIOME_PALETTES, RENDER_COLORS, RENDER_CSS_VARIABLES, applyRenderColorVariables } from "./colors.js";
+export type { RenderColors } from "./colors.js";
diff --git a/app/packages/render/tests/camera.test.ts b/app/packages/render/tests/camera.test.ts
index 0521baa..100f274 100644
--- a/app/packages/render/tests/camera.test.ts
+++ b/app/packages/render/tests/camera.test.ts
@@ -1,8 +1,10 @@
 import { describe, expect, it } from "vitest";
 import {
   TRAINING_COMFORT,
+  centerCameraOn,
   needsTrainingFocus,
   trainingGlideOffset,
+  zoomAroundPoint,
   worldToScreen,
   type CameraPlane,
 } from "../src/camera.js";
@@ -18,6 +20,27 @@ const MAP = { width: 572, height: 516 };
 
 const plane = (scale: number, x: number, y: number): CameraPlane => ({ scale, offset: { x, y } });
 
+describe("camera anchors (stage 1)", () => {
+  it("keeps the world point under the cursor during zoom", () => {
+    const before = plane(1, 40, -20);
+    const cursor = { x: 260, y: 180 };
+    const after = zoomAroundPoint(cursor, before, 1.5);
+    expect(after.scale).toBe(1.5);
+    expect(worldToScreen({ x: (cursor.x - before.offset.x) / before.scale, y: (cursor.y - before.offset.y) / before.scale }, after)).toEqual(cursor);
+  });
+
+  it("clamps zoom to the existing range", () => {
+    expect(zoomAroundPoint({ x: 0, y: 0 }, plane(1, 0, 0), 0.01).scale).toBe(0.55);
+    expect(zoomAroundPoint({ x: 0, y: 0 }, plane(1, 0, 0), 100).scale).toBe(1.8);
+  });
+
+  it("centers a fighter without exposing space beyond the map", () => {
+    const centered = centerCameraOn({ x: 300, y: 260 }, plane(1.2, -100, -20), { width: 390, height: 844 }, MAP);
+    expect(worldToScreen({ x: 300, y: 260 }, centered).x).toBeCloseTo(195, 5);
+    expect(centered.offset.x).toBeLessThanOrEqual(0);
+  });
+});
+
 describe("needsTrainingFocus (0.20.14)", () => {
   it("comfort zone is a proper band widened for HUD at top and bottom", () => {
     expect(TRAINING_COMFORT.x0).toBeLessThan(TRAINING_COMFORT.x1);
diff --git a/app/packages/render/tests/render.test.ts b/app/packages/render/tests/render.test.ts
index 39c1b08..ea7dde2 100644
--- a/app/packages/render/tests/render.test.ts
+++ b/app/packages/render/tests/render.test.ts
@@ -1,8 +1,24 @@
 import { describe, expect, it } from "vitest";
-import { RENDER_STATUS } from "../src/index.js";
+import { BIOME_PALETTES, RENDER_COLORS, RENDER_CSS_VARIABLES, RENDER_STATUS } from "../src/index.js";
 
 describe("render package", () => {
   it("declares PixiJS as the field backend", () => {
     expect(RENDER_STATUS).toBe("pixi");
   });
+
+  it("publishes one semantic palette for PixiJS and CSS", () => {
+    expect(RENDER_COLORS.targeting.ready).toBe(RENDER_COLORS.ui.amber);
+    expect(RENDER_COLORS.targeting.blocked).toBe(RENDER_COLORS.ui.danger);
+    expect(RENDER_CSS_VARIABLES["--amber"]).toBe("#e0b34a");
+    expect(RENDER_CSS_VARIABLES["--mist-dim"]).toBe("#b8b3a5");
+  });
+
+  it("publishes four deterministic biome palettes with three shades per tier", () => {
+    expect(Object.keys(BIOME_PALETTES).sort()).toEqual(["meadow", "scorched", "swamp", "thicket"]);
+    for (const palette of Object.values(BIOME_PALETTES)) {
+      expect(palette.face).toHaveLength(3);
+      expect(palette.face.every((tier) => tier.length === 3)).toBe(true);
+      expect(palette.riser).toHaveLength(3);
+    }
+  });
 });
diff --git a/app/packages/replay/package.json b/app/packages/replay/package.json
index afbcff9..ecfa115 100644
--- a/app/packages/replay/package.json
+++ b/app/packages/replay/package.json
@@ -1,6 +1,6 @@
 {
   "name": "@bylina/replay",
-  "version": "0.20.19",
+  "version": "0.21.2",
   "type": "module",
   "exports": {
     ".": "./src/index.ts"
diff --git a/app/packages/replay/src/index.ts b/app/packages/replay/src/index.ts
index 75c638b..e3ec96d 100644
--- a/app/packages/replay/src/index.ts
+++ b/app/packages/replay/src/index.ts
@@ -9,7 +9,7 @@ import type { Command, PvpMatchOptions } from "@bylina/core";
  * либо обрыве соединения.
  */
 
-export const REPLAY_VERSION = "0.20.19";
+export const REPLAY_VERSION = "0.21.2";
 
 export interface ReplayJournal {
   version: string;
diff --git a/app/packages/replay/tests/replay.test.ts b/app/packages/replay/tests/replay.test.ts
index 8d1d28e..1e95244 100644
--- a/app/packages/replay/tests/replay.test.ts
+++ b/app/packages/replay/tests/replay.test.ts
@@ -13,9 +13,9 @@ const OPTIONS: PvpMatchOptions = {
   seed: 99,
 };
 
-describe("replay journal (0.20.19)", () => {
+describe("replay journal (0.21.2)", () => {
   it("uses the current replay format version", () => {
-    expect(REPLAY_VERSION).toBe("0.20.19");
+    expect(REPLAY_VERSION).toBe("0.21.2");
   });
 
   it("records commands and serializes to a plain object", () => {
diff --git a/app/packages/session/package.json b/app/packages/session/package.json
index f2689f1..824720e 100644
--- a/app/packages/session/package.json
+++ b/app/packages/session/package.json
@@ -1,6 +1,6 @@
 {
   "name": "@bylina/session",
-  "version": "0.20.19",
+  "version": "0.21.2",
   "type": "module",
   "exports": {
     ".": "./src/index.ts"
diff --git a/app/packages/session/src/index.ts b/app/packages/session/src/index.ts
index 354048d..fb180f0 100644
--- a/app/packages/session/src/index.ts
+++ b/app/packages/session/src/index.ts
@@ -17,7 +17,7 @@ import { eventsVisibleTo } from "@bylina/core";
 import type { Command as ReplayCommand } from "@bylina/core";
 import type { ReplayJournal } from "@bylina/replay";
 
-export const APP_VERSION = "0.20.19";
+export const APP_VERSION = "0.21.2";
 
 export type AppScreen =
   | "boot"
diff --git a/app/packages/session/tests/session.test.ts b/app/packages/session/tests/session.test.ts
index b42ede0..dd89b77 100644
--- a/app/packages/session/tests/session.test.ts
+++ b/app/packages/session/tests/session.test.ts
@@ -74,8 +74,8 @@ describe("createSession", () => {
     expect(createSession().get().screen).toBe("boot");
   });
 
-  it("reports version 0.20.19", () => {
-    expect(APP_VERSION).toBe("0.20.19");
+  it("reports version 0.21.2", () => {
+    expect(APP_VERSION).toBe("0.21.2");
   });
 
   it("moves between menu and settings", () => {
@@ -415,7 +415,7 @@ describe("createSession campaign hints (0.20.0)", () => {
   });
 });
 
-describe("continueCampaign (0.20.19)", () => {
+describe("continueCampaign (0.21.2)", () => {
   const bindCampaignAutomaton = (session: ReturnType<typeof createSession>): void => {
     session.bindCampaign(createCampaign(CAMPAIGN_CONFIG));
   };
@@ -470,7 +470,7 @@ describe("continueCampaign (0.20.19)", () => {
   });
 });
 
-describe("suspend/resume campaign battle (0.20.17–0.20.19)", () => {
+describe("suspend/resume campaign battle (0.20.17–0.21.2)", () => {
   const makeBattleSession = (): ReturnType<typeof createSession> => {
     const session = createSession("menu");
     const campaign = createCampaign(CAMPAIGN_CONFIG);
@@ -493,7 +493,7 @@ describe("suspend/resume campaign battle (0.20.17–0.20.19)", () => {
     const suspended = session.get();
     expect(suspended.screen).toBe("menu");
     expect(suspended.paused).toBe(false);
-    // Контекст миссии — в слоте; навигационные поля чистые (0.20.19).
+    // Контекст миссии — в слоте; навигационные поля чистые (0.21.2).
     expect(suspended.suspendedCampaign?.activeMissionId).toBe("clearing_1");
     expect(suspended.suspendedCampaign?.restoredMatch).toBeDefined();
     expect(suspended.battleKind).toBeNull();
@@ -504,7 +504,7 @@ describe("suspend/resume campaign battle (0.20.17–0.20.19)", () => {
     expect(session.get().suspendedCampaign).toBeNull();
   });
 
-  it("detours through other modes from the menu do not lose the mission (0.20.19)", () => {
+  it("detours through other modes from the menu do not lose the mission (0.21.2)", () => {
     // Регрессия: вход из меню в обучение/быстрый матч/настройки клал {...idle}
     // и стирал контекст приостановленной миссии — «Продолжить» вело на карту.
     const session = makeBattleSession();
diff --git a/app/packages/settings/package.json b/app/packages/settings/package.json
index 9abdef6..53f00c5 100644
--- a/app/packages/settings/package.json
+++ b/app/packages/settings/package.json
@@ -1,6 +1,6 @@
 {
   "name": "@bylina/settings",
-  "version": "0.20.19",
+  "version": "0.21.2",
   "type": "module",
   "exports": {
     ".": "./src/index.ts"
diff --git a/app/packages/settings/src/index.ts b/app/packages/settings/src/index.ts
index 0b34de4..3e3ea29 100644
--- a/app/packages/settings/src/index.ts
+++ b/app/packages/settings/src/index.ts
@@ -9,6 +9,8 @@ export interface SettingsState {
   highContrast: boolean;
   /** Показывать подсказки и туториалы «первого раза» (0.20.0); по умолчанию включено. */
   showHints: boolean;
+  /** Автоматически передавать ход, когда у живых бойцов не осталось ОД. */
+  autoEndTurn: boolean;
   /**
    * Отладочный режим (0.20.1, doc/debug-mode.md): включает средства QA
    * (оверлей стоимости, автопобеда) в боевом интерфейсе. Помимо адреса с
@@ -26,6 +28,8 @@ export const defaultSettings: SettingsState = {
   fontScale: 1,
   highContrast: false,
   showHints: true,
+  // Сохраняет прежнее поведение автоматического перехода хода.
+  autoEndTurn: true,
   debugMode: false,
 };
 
@@ -57,6 +61,7 @@ export function sanitizeSettings(
     fontScale: clamp(Number(raw?.fontScale ?? defaultSettings.fontScale), 0.85, 1.4),
     highContrast: Boolean(raw?.highContrast),
     showHints: raw?.showHints !== false,
+    autoEndTurn: raw?.autoEndTurn !== false,
     debugMode: Boolean(raw?.debugMode),
   };
 }
diff --git a/app/packages/settings/tests/settings.test.ts b/app/packages/settings/tests/settings.test.ts
index bff11c6..8867f3b 100644
--- a/app/packages/settings/tests/settings.test.ts
+++ b/app/packages/settings/tests/settings.test.ts
@@ -27,6 +27,11 @@ describe("sanitizeSettings", () => {
     expect(sanitizeSettings({ showHints: false }, ["ru"]).showHints).toBe(false);
     expect(sanitizeSettings({ showHints: true }, ["ru"]).showHints).toBe(true);
   });
+
+  it("keeps auto-end-turn enabled by default and allows opting out (stage 1)", () => {
+    expect(sanitizeSettings({}, ["ru"]).autoEndTurn).toBe(true);
+    expect(sanitizeSettings({ autoEndTurn: false }, ["ru"]).autoEndTurn).toBe(false);
+  });
 });
 
 describe("createSettings", () => {
diff --git a/app/packages/signaling/package.json b/app/packages/signaling/package.json
index 8edf519..e4e6d12 100644
--- a/app/packages/signaling/package.json
+++ b/app/packages/signaling/package.json
@@ -1,6 +1,6 @@
 {
   "name": "@bylina/signaling",
-  "version": "0.20.19",
+  "version": "0.21.2",
   "type": "module",
   "exports": {
     ".": "./src/index.ts"
diff --git a/app/packages/storage/package.json b/app/packages/storage/package.json
index eeeaf35..3a7f144 100644
--- a/app/packages/storage/package.json
+++ b/app/packages/storage/package.json
@@ -1,6 +1,6 @@
 {
   "name": "@bylina/storage",
-  "version": "0.20.19",
+  "version": "0.21.2",
   "type": "module",
   "exports": {
     ".": "./src/index.ts"
diff --git a/app/packages/ui/package.json b/app/packages/ui/package.json
index bde55cf..a5a9e1e 100644
--- a/app/packages/ui/package.json
+++ b/app/packages/ui/package.json
@@ -1,6 +1,6 @@
 {
   "name": "@bylina/ui",
-  "version": "0.20.19",
+  "version": "0.21.2",
   "type": "module",
   "exports": {
     ".": "./src/index.ts"
diff --git a/app/packages/ui/src/BattleScreenView.tsx b/app/packages/ui/src/BattleScreenView.tsx
index 8d48fc5..49f7354 100644
--- a/app/packages/ui/src/BattleScreenView.tsx
+++ b/app/packages/ui/src/BattleScreenView.tsx
@@ -24,7 +24,7 @@ import {
 } from "@bylina/core";
 import type { TrainingMissionConfig } from "@bylina/content";
 import { createFieldRenderer, type FieldRenderer } from "@bylina/render";
-import { useEffect, useMemo, useRef, useState } from "react";
+import { useEffect, useMemo, useRef, useState, type PointerEvent as ReactPointerEvent } from "react";
 import { ACTION_SHORTCUTS, selectableActions, shortcutForAction } from "./action-shortcuts.js";
 import { interactiveEntityAt, primaryAttackForEnemy } from "./cell-interaction.js";
 import { shouldAutoEndTurn, trainingHintsSorted } from "./training-progress.js";
@@ -39,7 +39,7 @@ import {
   type TrainingDirectiveView,
 } from "./training-scenario.js";
 import { useServices, useT } from "./context.js";
-import { useI18nTick, useSessionState, useSettingsState } from "./hooks.js";
+import { useI18nTick, usePrefersReducedMotion, useSessionState, useSettingsState } from "./hooks.js";
 import { CampaignHint } from "./CampaignHint.js";
 import { pendingCampaignHints, type CampaignHintId } from "./campaign-hints.js";
 import { unitPortrait } from "./portraits.js";
@@ -62,6 +62,12 @@ function unitNameKey(configId: string): string {
   return `unit.${configId}.name`;
 }
 
+type TrainingEdgeHint = {
+  left: number;
+  top: number;
+  angle: number;
+};
+
 /** Иконка автопобеды: молния как знак мгновенного разрешения. */
 function AutoWinIcon() {
   return (
@@ -101,11 +107,18 @@ export function BattleScreenView() {
   const { session, content, debug } = useServices();
   const { paused, difficulty, battleKind, activeMissionId, deployment, matchSeed, trainingDone: trainingDoneMissions } = useSessionState();
   const hostRef = useRef<HTMLDivElement>(null);
+  const aimCardRef = useRef<HTMLDivElement>(null);
   const rendererRef = useRef<FieldRenderer | null>(null);
   const hoverRef = useRef<string | null>(null);
+  const portraitTapRef = useRef<{ id: number; at: number } | null>(null);
   const inputRef = useBattleInput();
+  const reducedMotion = usePrefersReducedMotion();
 
   const [debugMovement, setDebugMovement] = useState(false);
+  const [fastMode, setFastMode] = useState(false);
+  const playbackSpeed: 1 | 2 = fastMode ? 2 : 1;
+  const playbackSpeedRef = useRef<1 | 2>(playbackSpeed);
+  playbackSpeedRef.current = playbackSpeed;
 
   const weapons = useMemo(() => {
     const base: Record<string, WeaponStats> = defaultTrainingWeapons();
@@ -259,6 +272,8 @@ export function BattleScreenView() {
   const [selectedId, setSelectedId] = useState<number | null>(null);
   const [action, setAction] = useState<{ type: "weapon" | "skill"; id: string } | null>(null);
   const [aimId, setAimId] = useState<number | null>(null);
+  const [aimCardPosition, setAimCardPosition] = useState<{ left: number; top: number } | null>(null);
+  const [trainingEdgeHint, setTrainingEdgeHint] = useState<TrainingEdgeHint | null>(null);
   const [skillTargetPos, setSkillTargetPos] = useState<CellPos | null>(null);
   const [preview, setPreview] = useState<string | null>(null);
   const [log, setLog] = useState<string | null>(null);
@@ -338,12 +353,15 @@ export function BattleScreenView() {
         return;
       }
       const command = commands[index];
-      if (command) kernel.apply(command);
+      if (command) {
+        const applied = kernel.apply(command);
+        if (applied.ok) void rendererRef.current?.play(applied.events);
+      }
       setReplayIndex(index + 1);
-    }, 480);
+    }, 480 / playbackSpeed);
     return () => window.clearInterval(timer);
     // eslint-disable-next-line react-hooks/exhaustive-deps
-  }, [isReplay, replayJournal, kernel, replayIndex, replayDone]);
+  }, [isReplay, replayJournal, kernel, replayIndex, replayDone, playbackSpeed]);
 
 
   // Обрыв канала состязательного боя (0.17.0): отсчёт 30 секунд.
@@ -379,7 +397,6 @@ export function BattleScreenView() {
   const snapshot = usesNetSnapshot
     ? (session.getNetSnapshot() ?? EMPTY_SNAPSHOT)
     : session.getBattleSnapshot(viewOwner);
-
   // Завершение миссии обучения: итоговая плашка вместо мгновенного возврата
   // (ui-design §3: «…→ итог → экран обучения»). Пройденной считается только
   // победа (0.19.1).
@@ -468,6 +485,13 @@ export function BattleScreenView() {
   const isOwn = (entity: EntityState): boolean =>
     !isSpectator && !isReplay && !entity.dead && entity.coverType === 0 && entity.owner === viewOwner && entity.maxAp > 0;
 
+  const noActionPoints = useMemo(() => {
+    const fighters = snapshot.entities.filter(
+      (entity) => !entity.dead && entity.coverType === 0 && entity.owner === viewOwner && entity.maxAp > 0,
+    );
+    return fighters.length > 0 && fighters.every((entity) => entity.ap <= 0);
+  }, [snapshot.entities, viewOwner]);
+
   // События поочерёдного боя приходят через транспорт (0.14.0/0.15.0):
   // локальный — на одном устройстве, сетевой — ведомому от ведущего.
   useEffect(() => {
@@ -496,6 +520,19 @@ export function BattleScreenView() {
   const mission = battleKind === "campaign" && activeMissionId
     ? session.getCampaign().getMission(activeMissionId)
     : undefined;
+  // Тьма — только презентационная метаинформация кампании: в MatchState и
+  // правила боя она не попадает. В быстрых/PvP/обучающих боях слой отсутствует.
+  const campaignAtmosphere = battleKind === "campaign"
+    ? (() => {
+        const state = session.getCampaign().getState();
+        return { current: state.darkness, max: state.darknessMax };
+      })()
+    : undefined;
+  const presentationBiome = mission?.map.biome
+    ?? trainingMission?.map.biome
+    ?? replayJournal?.options.map.biome
+    ?? content.pvp.map?.biome
+    ?? content.quickMatch.map.biome;
 
   // Боевые туториалы кампании (0.20.0/0.20.1): «первый бой», «первый леший»,
   // «первая кикимора», «появление генерала». Показываются один раз, отключаются
@@ -632,6 +669,17 @@ export function BattleScreenView() {
     };
   }, [kernel, selectedId, aimId, skillTargetPos, action, selected?.x, selected?.y, selected?.ap, aimed?.x, aimed?.y, aimed?.hp]);
 
+  const areaPreview = useMemo(() => {
+    if (action?.type !== "skill" || selectedId === null) return null;
+    const skill = skills[action.id];
+    const hasAreaTarget = (skill?.radius ?? 0) > 0 || Boolean(skill?.effects.some((effect) => effect.type === "spawn" || effect.type === "displace"));
+    if (!skill || !hasAreaTarget) return null;
+    const center = skill.category === "self" ? selected : skillTargetPos;
+    return center
+      ? { center: { x: center.x, y: center.y, z: center.z }, radius: skill.radius ?? 0 }
+      : null;
+  }, [action, selectedId, selected, skillTargetPos, skills]);
+
   const announce = (events: GameEvent[]): void => {
     const combat = events.find((event) => event.type === "COMBAT_RESOLVED");
     if (combat && combat.type === "COMBAT_RESOLVED") {
@@ -874,7 +922,7 @@ export function BattleScreenView() {
         finishFromEvents([]);
         return;
       }
-      await sleep(430);
+      await sleep(430 / playbackSpeedRef.current);
       for (let guard = 0; guard < 96; guard += 1) {
         const snap = session.getBattleSnapshot(PLAYER_OWNER);
         if (snap.activeOwner !== ENEMY_OWNER) break;
@@ -901,7 +949,7 @@ export function BattleScreenView() {
         finishFromEvents(applied.events);
         if (!command) break;
         if (session.getBattleOutcome() !== "ongoing") break;
-        await sleep(190);
+        await sleep(190 / playbackSpeedRef.current);
       }
     } finally {
       setEnemyPhase(false);
@@ -976,6 +1024,7 @@ export function BattleScreenView() {
       isSpectator,
       isTraining,
       activeHint,
+      autoEndTurn: hintSettings.autoEndTurn,
       activeOwner: snapshot.activeOwner,
       viewOwner,
       ownUnits,
@@ -984,7 +1033,7 @@ export function BattleScreenView() {
     })) return;
     endTurn();
     // eslint-disable-next-line react-hooks/exhaustive-deps
-  }, [snapshot.turnNumber, snapshot.entities, viewOwner, paused, busy, enemyPhase, isReplay, isSpectator, isNetGuest, isTraining, activeHint]);
+  }, [snapshot.turnNumber, snapshot.entities, viewOwner, paused, busy, enemyPhase, isReplay, isSpectator, isNetGuest, isTraining, activeHint, hintSettings.autoEndTurn]);
 
   const onCell = (x: number, y: number): void => {
     if (paused || busy || snapshot.activeOwner !== viewOwner) return;
@@ -1001,6 +1050,11 @@ export function BattleScreenView() {
     const selectedSkill = action?.type === "skill" ? skills[action.id] : undefined;
     const positionOnlySkill = selectedSkill?.effects.some((effect) => effect.type === "spawn");
     const allyTargeting = Boolean(selectedSkill && !positionOnlySkill && (selectedSkill.filter === "allies" || selectedSkill.filter === "all"));
+    const selfAreaTargeting = Boolean(selectedSkill?.category === "self" && (selectedSkill.radius ?? 0) > 0);
+    if (selfAreaTargeting && action?.type === "skill") {
+      useSelfSkill(action.id);
+      return;
+    }
     const entity = interactiveEntityAt(snapshot.entities, x, y, Boolean(reach) && !targeting);
     if (entity?.owner === viewOwner && entity.coverType === 0 && entity.maxAp > 0 && !allyTargeting) {
       // Обучение: выбор иного бойца запрещён — действует только исполнитель
@@ -1131,6 +1185,10 @@ export function BattleScreenView() {
     };
   }, []);
 
+  useEffect(() => {
+    rendererRef.current?.setPlaybackSpeed(playbackSpeed);
+  }, [playbackSpeed]);
+
   const aimBreakCell = useMemo(() => {
     if (!hit || !selected || !aimed) return null;
     // breakCell теперь вычисляется ядром в previewAttack (§7, §9.3).
@@ -1148,16 +1206,140 @@ export function BattleScreenView() {
     return { x, y, z: tile?.z ?? 0 };
   }, [preview, skillTargetPos, snapshot.grid]);
 
+  const aimStatus = aimId === null
+    ? undefined
+    : hit === null
+      ? "selected" as const
+      : hit.available
+        ? "ready" as const
+        : "blocked" as const;
+  const aimTarget = aimId === null ? undefined : snapshot.entities.find((entity) => entity.id === aimId);
+
+  // Карточка шанса живёт в DOM-слое, а цель — на Pixi-холсте. Позиция
+  // пересчитывается коротким, ограниченным ритмом прицеливания: она остаётся
+  // рядом с целью и после панорамирования/поворота экрана, но не создаёт
+  // бесконечный поток рендеров.
+  useEffect(() => {
+    if (aimId === null || !aimTarget) {
+      setAimCardPosition(null);
+      return;
+    }
+    const updatePosition = (): void => {
+      const host = hostRef.current;
+      const card = aimCardRef.current;
+      const renderer = rendererRef.current;
+      const root = host?.closest(".battle-screen");
+      if (!host || !card || !renderer?.screenPosition || !root) return;
+      const point = renderer.screenPosition(aimTarget.x, aimTarget.y, aimTarget.z);
+      if (!point) return;
+      const rootRect = root.getBoundingClientRect();
+      const stageRect = host.getBoundingClientRect();
+      const targetX = stageRect.left - rootRect.left + point.x;
+      const targetY = stageRect.top - rootRect.top + point.y;
+      const padding = 8;
+      const targetRadius = 30;
+      const gap = 12;
+      const cardWidth = card.offsetWidth;
+      const cardHeight = card.offsetHeight;
+      // left — это левый край карточки: цель остаётся её горизонтальным
+      // центром, но сама карточка не выходит за границы root.
+      const left = Math.max(
+        padding,
+        Math.min(rootRect.width - padding - cardWidth, targetX - cardWidth / 2),
+      );
+      const above = targetY - targetRadius - gap - cardHeight;
+      const below = targetY + targetRadius + gap;
+      const top = above >= padding || below + cardHeight > rootRect.height - padding
+        ? Math.max(padding, Math.min(rootRect.height - padding - cardHeight, above))
+        : below;
+      setAimCardPosition((previous) =>
+        previous && Math.abs(previous.left - left) < 0.5 && Math.abs(previous.top - top) < 0.5
+          ? previous
+          : { left, top },
+      );
+    };
+    updatePosition();
+    const timer = window.setInterval(updatePosition, 100);
+    window.addEventListener("resize", updatePosition);
+    return () => {
+      window.clearInterval(timer);
+      window.removeEventListener("resize", updatePosition);
+    };
+  }, [aimId, aimTarget?.x, aimTarget?.y, aimTarget?.z, hit?.available, hit?.chance, hit?.dmgMin, hit?.dmgMax, hit?.reason]);
+
+  // Подсказка на краю поля для обучения: если цель всё ещё вне видимой
+  // рабочей области, стрелка показывает направление, не подменяя подводку
+  // камеры и не требуя клика/наведения.
+  useEffect(() => {
+    const updateEdgeHint = (): void => {
+      if (!trainingFocus || !trainingHighlight) {
+        setTrainingEdgeHint(null);
+        return;
+      }
+      const host = hostRef.current;
+      const renderer = rendererRef.current;
+      const root = host?.closest(".battle-screen");
+      if (!host || !renderer?.screenPosition || !root) return;
+      const tile = snapshot.grid.tiles.find((candidate) =>
+        candidate.x === trainingHighlight.x && candidate.y === trainingHighlight.y,
+      );
+      const entity = trainingHighlight.kind === "entity"
+        ? snapshot.entities.find((candidate) => candidate.x === trainingHighlight.x && candidate.y === trainingHighlight.y)
+        : undefined;
+      const point = renderer.screenPosition(
+        trainingHighlight.x,
+        trainingHighlight.y,
+        entity?.z ?? tile?.z ?? 0,
+      );
+      if (!point) return;
+      const rootRect = root.getBoundingClientRect();
+      const stageRect = host.getBoundingClientRect();
+      const targetX = stageRect.left - rootRect.left + point.x;
+      const targetY = stageRect.top - rootRect.top + point.y;
+      const edgeX = 28;
+      const edgeY = Math.min(82, Math.max(28, rootRect.height * 0.18));
+      const bottomEdge = Math.min(112, Math.max(28, rootRect.height * 0.2));
+      const minX = edgeX;
+      const maxX = Math.max(minX, rootRect.width - edgeX);
+      const minY = Math.min(edgeY, rootRect.height / 2 - 12);
+      const maxY = Math.max(minY, rootRect.height - bottomEdge);
+      const outside = targetX < minX || targetX > maxX || targetY < minY || targetY > maxY;
+      if (!outside) {
+        setTrainingEdgeHint(null);
+        return;
+      }
+      const left = Math.max(minX, Math.min(maxX, targetX));
+      const top = Math.max(minY, Math.min(maxY, targetY));
+      const angle = Math.atan2(targetY - top, targetX - left) * (180 / Math.PI);
+      setTrainingEdgeHint((previous) =>
+        previous && Math.abs(previous.left - left) < 0.5 && Math.abs(previous.top - top) < 0.5 && Math.abs(previous.angle - angle) < 0.5
+          ? previous
+          : { left, top, angle },
+      );
+    };
+    updateEdgeHint();
+    const timer = window.setInterval(updateEdgeHint, 120);
+    window.addEventListener("resize", updateEdgeHint);
+    return () => {
+      window.clearInterval(timer);
+      window.removeEventListener("resize", updateEdgeHint);
+    };
+  }, [trainingFocus, trainingHighlight?.kind, trainingHighlight?.x, trainingHighlight?.y, snapshot.grid, snapshot.entities]);
+
   useEffect(() => {
     rendererRef.current?.update({
       matchSeed,
+      biome: presentationBiome,
+      darkness: campaignAtmosphere,
       snapshot,
       selectedId,
       aimId,
       reachable,
       path: previewPath,
       aimOk: Boolean(hit?.available),
+      aimStatus,
       heightMod: hit?.heightMod ?? 0,
+      reducedMotion,
       debugMovement,
       visibleCells,
       exploredCells,
@@ -1165,8 +1347,27 @@ export function BattleScreenView() {
       hoverCell,
       trainingHighlight,
       trainingFocus,
+      areaPreview,
+      flanked: Boolean(hit?.flanked),
+      combatLabels: { miss: t("combat.miss") },
     });
-  }, [matchSeed, snapshot, selectedId, aimId, reachable, previewPath, hit?.available, hit?.heightMod, paused, debugMovement, visibleCells, exploredCells, aimBreakCell, hoverCell, trainingHighlight, trainingFocus]);
+  }, [matchSeed, presentationBiome, campaignAtmosphere?.current, campaignAtmosphere?.max, snapshot, selectedId, aimId, reachable, previewPath, hit?.available, hit?.heightMod, hit?.flanked, aimStatus, reducedMotion, paused, debugMovement, visibleCells, exploredCells, aimBreakCell, hoverCell, trainingHighlight, trainingFocus, areaPreview, t]);
+
+  const centerOnEntity = (entity: EntityState): void => {
+    rendererRef.current?.centerOn(entity.x, entity.y, entity.z);
+  };
+
+  const handlePortraitPointerUp = (event: ReactPointerEvent, entity: EntityState): void => {
+    if (event.pointerType !== "touch" || entity.dead) return;
+    const now = performance.now();
+    const previous = portraitTapRef.current;
+    if (previous && previous.id === entity.id && now - previous.at < 360) {
+      portraitTapRef.current = null;
+      centerOnEntity(entity);
+    } else {
+      portraitTapRef.current = { id: entity.id, at: now };
+    }
+  };
 
   useEffect(() => {
     const onKey = (event: KeyboardEvent): void => {
@@ -1175,6 +1376,10 @@ export function BattleScreenView() {
         return;
       }
       if (paused || busy) return;
+      if (event.key === "f" || event.key === "F") {
+        setFastMode((value) => !value);
+        return;
+      }
       if (event.key === "Tab") {
         event.preventDefault();
         // Обучение: перебор бойцов запрещён — действует исполнитель указания.
@@ -1229,7 +1434,7 @@ export function BattleScreenView() {
           const uses = selected.skillUses?.[chosen.id] ?? 0;
           if (cooldown > 0 || (skill?.maxUsesPerBattle !== undefined && uses >= skill.maxUsesPerBattle)) return;
         }
-        if (chosen.type === "skill" && skills[chosen.id]?.category === "self") {
+        if (chosen.type === "skill" && skills[chosen.id]?.category === "self" && (skills[chosen.id]?.radius ?? 0) <= 0) {
           useSelfSkill(chosen.id);
         } else {
           const active = action?.type === chosen.type && action.id === chosen.id;
@@ -1333,6 +1538,20 @@ export function BattleScreenView() {
             ) : null}
           </div>
         ) : null}
+        {trainingEdgeHint ? (
+          <div
+            className="training-edge-hint"
+            role="img"
+            aria-label={t("training.offscreenHint")}
+            style={{
+              left: trainingEdgeHint.left,
+              top: trainingEdgeHint.top,
+              transform: `translate(-50%, -50%) rotate(${trainingEdgeHint.angle}deg)`,
+            }}
+          >
+            <span aria-hidden="true">➤</span>
+          </div>
+        ) : null}
         {trainingNote ? (
           // Реактивные плашки (яд, воскрешение, призыв) — у нижнего края,
           // над панелью действий, чтобы не перекрывать центр поля.
@@ -1380,6 +1599,17 @@ export function BattleScreenView() {
             <button type="button" className="hud-btn" onClick={() => session.setPaused(true)}>
               {t("battle.pause")}
             </button>
+            <button
+              type="button"
+              className={`hud-btn speed-toggle${fastMode ? " is-on" : ""}`}
+              onClick={() => setFastMode((value) => !value)}
+              aria-pressed={fastMode}
+              aria-keyshortcuts="F"
+              aria-label={t(fastMode ? "battle.speedFast" : "battle.speedNormal")}
+              title={t("battle.speedHint")}
+            >
+              <span aria-hidden="true">»</span> {fastMode ? "2×" : "1×"}
+            </button>
             {isTraining ? (
               <button
                 type="button"
@@ -1516,6 +1746,12 @@ export function BattleScreenView() {
                   key={entity.id}
                   type="button"
                   className={`roster-card${entity.id === selectedId ? " is-on" : ""}${entity.dead ? " is-dead" : ""}`}
+                  aria-label={t(unitNameKey(entity.configId))}
+                  title={t(unitNameKey(entity.configId))}
+                  onDoubleClick={() => {
+                    if (!entity.dead) centerOnEntity(entity);
+                  }}
+                  onPointerUp={(event) => handlePortraitPointerUp(event, entity)}
                   onClick={() => {
                     if (entity.dead) return;
                     // Обучение: выбор иного бойца запрещён — действует только
@@ -1558,7 +1794,11 @@ export function BattleScreenView() {
             </p>
           ) : null}
           {hit ? (
-            <div className="aim-card">
+            <div
+              ref={aimCardRef}
+              className="aim-card"
+              style={aimCardPosition ? { left: aimCardPosition.left, top: aimCardPosition.top, transform: "none" } : undefined}
+            >
               <div className="aim-header">
                 <span className={`aim-chance${hit.available ? "" : " blocked"}`}>
                   {hit.available
@@ -1676,7 +1916,14 @@ export function BattleScreenView() {
             {selected ? (
               <div className="sel-row">
                 {unitPortrait(selected.configId) ? (
-                  <img className="sel-face" src={unitPortrait(selected.configId)} alt="" draggable={false} />
+                  <img
+                    className="sel-face"
+                    src={unitPortrait(selected.configId)}
+                    alt=""
+                    draggable={false}
+                    onDoubleClick={() => centerOnEntity(selected)}
+                    onPointerUp={(event) => handlePortraitPointerUp(event, selected)}
+                  />
                 ) : null}
                 <div className="sel-info">
                   <p className="eyebrow">{t(unitNameKey(selected.configId))}</p>
@@ -1707,7 +1954,8 @@ export function BattleScreenView() {
               <p>{t("battle.empty")}</p>
             )}
           </div>
-          <div className="skill-row">
+          <div className="skill-row-wrap">
+            <div className="skill-row">
             {(selected?.weaponIds ?? (selected?.weaponId ? [selected.weaponId] : [])).map((weaponId, index) => (
               <button
                 key={`weapon-${weaponId}`}
@@ -1746,7 +1994,7 @@ export function BattleScreenView() {
                   title={cooldown > 0 ? t("battle.cooldownHint", { turns: cooldown }) : exhausted ? t("battle.noUsesHint") : undefined}
                   disabled={!selected || selected.ap < (skill?.apCost ?? 1) || cooldown > 0 || exhausted || busy || snapshot.activeOwner !== viewOwner || !trainingSkillAllowed(skillId)}
                   onClick={() => {
-                    if (skill?.category === "self") useSelfSkill(skillId);
+                    if (skill?.category === "self" && (skill.radius ?? 0) <= 0) useSelfSkill(skillId);
                     else {
                       setAction(active ? null : { type: "skill", id: skillId });
                       setSkillTargetPos(null);
@@ -1806,10 +2054,14 @@ export function BattleScreenView() {
               <kbd>0</kbd>
               {t("battle.overwatch")}
             </button>
+            </div>
+            {((selected?.weaponIds?.length ?? (selected?.weaponId ? 1 : 0)) + (selected?.skillIds?.length ?? 0) + 2) > 4 ? (
+              <span className="skill-row-hint" aria-hidden="true">{t("battle.skillScrollHint")}</span>
+            ) : null}
           </div>
           <button
             type="button"
-            className={`hud-btn hud-btn-primary${hintPanelKey === "end_turn" ? " hint-pulse" : ""}`}
+            className={`hud-btn hud-btn-primary end-turn-btn${noActionPoints && snapshot.activeOwner === viewOwner ? " is-ready" : ""}${hintPanelKey === "end_turn" ? " hint-pulse" : ""}`}
             disabled={busy || snapshot.activeOwner !== viewOwner || !trainingAllows("endTurn")}
             onClick={() => endTurn()}
           >
diff --git a/app/packages/ui/src/CampaignScreen.tsx b/app/packages/ui/src/CampaignScreen.tsx
index 2c99be5..4645b0b 100644
--- a/app/packages/ui/src/CampaignScreen.tsx
+++ b/app/packages/ui/src/CampaignScreen.tsx
@@ -1,7 +1,7 @@
-import { useEffect, useMemo, useState, type CSSProperties } from "react";
+import { useEffect, useMemo, useRef, useState, type CSSProperties } from "react";
 import type { ItemConfig, MissionConfig } from "@bylina/content";
 import { useServices, useT } from "./context.js";
-import { useI18nTick, useSessionState, useSettingsState } from "./hooks.js";
+import { useI18nTick, usePrefersReducedMotion, useSessionState, useSettingsState } from "./hooks.js";
 import { unitPortrait } from "./portraits.js";
 import { CampaignHint } from "./CampaignHint.js";
 import { pendingCampaignHints, type CampaignHintId } from "./campaign-hints.js";
@@ -11,8 +11,68 @@ const CLASS_IDS: readonly string[] = ["bogatyr", "strelets", "znaharka", "volkhv
 
 type CampTab = "map" | "roster" | "chamber" | "forge";
 
-/** Геометрические знаки-руны отметок: плоские формы вместо орнамента (ui-design §1). */
-const RUNES: readonly string[] = ["✦", "▲", "◆", "⬢", "✶", "✵"];
+type ShipFlight = {
+  id: number;
+  from: { x: number; y: number };
+  to: { x: number; y: number };
+};
+
+/**
+ * Путь для дороги строится только по открытым точкам. Отдельные серии
+ * позволяют не соединять видимые миссии с закрытыми участками карты.
+ */
+function roadPath(missions: readonly MissionConfig[]): string {
+  return missions.map((mission) => `${mission.x},${mission.y}`).join(" ");
+}
+
+function openRoadSegments(
+  missions: readonly MissionConfig[],
+  statuses: ReadonlyMap<string, string>,
+): MissionConfig[][] {
+  const segments: MissionConfig[][] = [];
+  let current: MissionConfig[] = [];
+  for (const mission of missions) {
+    if (statuses.get(mission.id) !== "locked") {
+      current.push(mission);
+      continue;
+    }
+    if (current.length > 1) segments.push(current);
+    current = [];
+  }
+  if (current.length > 1) segments.push(current);
+  return segments;
+}
+
+function flightArc(flight: ShipFlight): { midX: number; midY: number; arc: number; path: string } {
+  const dx = flight.to.x - flight.from.x;
+  const dy = flight.to.y - flight.from.y;
+  const distance = Math.hypot(dx, dy);
+  const arc = Math.min(12, Math.max(5, distance * 0.18));
+  const midX = (flight.from.x + flight.to.x) / 2;
+  const midY = (flight.from.y + flight.to.y) / 2;
+  const controlX = midX - (dy / Math.max(distance, 1)) * arc;
+  const controlY = midY + (dx / Math.max(distance, 1)) * arc;
+  return {
+    midX,
+    midY,
+    arc,
+    path: `M ${flight.from.x} ${flight.from.y} Q ${controlX} ${controlY} ${flight.to.x} ${flight.to.y}`,
+  };
+}
+
+function missionIcon(type: MissionConfig["type"]) {
+  switch (type) {
+    case "destroy":
+      return <IdolIcon />;
+    case "rescue":
+      return <ShieldIcon />;
+    case "recon":
+    case "needle":
+      return <ReconIcon />;
+    default:
+      return <SwordsIcon />;
+  }
+}
 
 function unitName(unitId: string): string {
   return `unit.${unitId}.name`;
@@ -22,10 +82,6 @@ function itemName(itemId: string): string {
   return `item.${itemId}.name`;
 }
 
-function roadPath(missions: readonly MissionConfig[]): string {
-  return missions.map((mission) => `${mission.x},${mission.y}`).join(" ");
-}
-
 /** Строка эффекта предмета для карточки Кузни и снаряжения. */
 function itemEffectParts(item: ItemConfig, t: (key: string, vars?: Record<string, string | number>) => string): string[] {
   const parts: string[] = [];
@@ -218,8 +274,12 @@ export function CampaignScreen() {
   const items = campaign.getItems();
   const settings = useSettingsState();
   const { campaignHintsDone } = useSessionState();
+  const reducedMotion = usePrefersReducedMotion();
   const [tab, setTab] = useState<CampTab>("map");
   const [selectedId, setSelectedId] = useState<string | null>(null);
+  const initialFlight = campaign.getLastFlight();
+  const [shipFlight, setShipFlight] = useState<ShipFlight | null>(initialFlight);
+  const flightSeenRef = useRef<number | null>(initialFlight?.id ?? null);
   const [trainingId, setTrainingId] = useState<number | null>(null);
   const [scanKey, setScanKey] = useState<number>(0);
   const [justOpened, setJustOpened] = useState<string[]>([]);
@@ -237,6 +297,20 @@ export function CampaignScreen() {
     [campaign],
   );
 
+  const lastFlight = campaign.getLastFlight();
+  const lastFlightId = lastFlight?.id ?? null;
+  useEffect(() => {
+    if (!lastFlight || lastFlight.id === flightSeenRef.current) return;
+    flightSeenRef.current = lastFlight.id;
+    setShipFlight(lastFlight);
+  }, [lastFlightId]);
+
+  useEffect(() => {
+    if (!shipFlight) return;
+    const timer = window.setTimeout(() => setShipFlight(null), 1_050);
+    return () => window.clearTimeout(timer);
+  }, [shipFlight?.id]);
+
   const byId = useMemo(() => {
     const map = new Map<string, MissionConfig>();
     for (const mission of missions) map.set(mission.id, mission);
@@ -256,6 +330,15 @@ export function CampaignScreen() {
   const lockedCount = state.missions.filter((point) => point.status === "locked").length;
 
   const shipPosition = state.shipPosition;
+  const activeVisualFlight = shipFlight && !reducedMotion ? shipFlight : null;
+  const missionStatuses = useMemo(
+    () => new Map(state.missions.map((point) => [point.id, point.status] as const)),
+    [state.missions],
+  );
+  const roadSegments = useMemo(
+    () => openRoadSegments(missions, missionStatuses),
+    [missions, missionStatuses],
+  );
   const woundedFighters = state.fighters.filter((fighter) => fighter.alive && fighter.wounded);
   const training = trainingId !== null ? state.fighters.find((fighter) => fighter.id === trainingId) : undefined;
 
@@ -331,6 +414,23 @@ export function CampaignScreen() {
     );
   }
 
+  const flightShape = activeVisualFlight ? flightArc(activeVisualFlight) : null;
+  const mapStyle = {
+    "--ship-x": `${shipPosition.x}%`,
+    "--ship-y": `${shipPosition.y}%`,
+    ...(flightShape && activeVisualFlight
+      ? {
+          "--ship-from-x": `${activeVisualFlight.from.x}%`,
+          "--ship-from-y": `${activeVisualFlight.from.y}%`,
+          "--ship-mid-x": `${flightShape.midX}%`,
+          "--ship-mid-y": `${flightShape.midY}%`,
+          "--ship-arc": `${flightShape.arc}%`,
+          "--ship-to-x": `${activeVisualFlight.to.x}%`,
+          "--ship-to-y": `${activeVisualFlight.to.y}%`,
+        }
+      : {}),
+  } as CSSProperties;
+
   return (
     <div className="screen campaign-screen">
       <header className="campaign-top">
@@ -348,7 +448,7 @@ export function CampaignScreen() {
         </button>
         <div className="campaign-title-block">
           <p className="eyebrow">{t("campaign.kingdom")}</p>
-          <h1>{t("menu.campaign")}</h1>
+          <h1 className="display-title">{t("menu.campaign")}</h1>
         </div>
         <div className="campaign-darkness" aria-label={t("campaign.darknessLabel")}>
           <span className="campaign-darkness-name">{t("campaign.darkness")}</span>
@@ -401,12 +501,12 @@ export function CampaignScreen() {
           </div>
 
           <div
-            className={`campaign-map${scanKey > 0 ? " is-scanning" : ""}`}
+            className={`campaign-map${scanKey > 0 ? " is-scanning" : ""}${activeVisualFlight ? " is-flying" : ""}`}
             role="region"
             aria-label={t("campaign.mapLabel")}
-            style={{ "--ship-x": `${shipPosition.x}%`, "--ship-y": `${shipPosition.y}%` } as CSSProperties}
+            style={mapStyle}
           >
-            {/* Декоративный рельеф: река, горы, леса, дорога между точками */}
+            {/* Декоративный рельеф: река, горы, леса и открытые участки дороги */}
             <svg className="map-terrain" viewBox="0 0 100 100" preserveAspectRatio="none" aria-hidden="true">
               <path
                 d="M-4 58 C 14 46 26 62 40 54 C 54 46 58 28 78 24 C 88 22 98 30 106 22"
@@ -430,30 +530,37 @@ export function CampaignScreen() {
               <path d="M13 74 19 86 7 86Z" fill="#223027" />
               <path d="M20 80 26 92 14 92Z" fill="#1e2a22" />
               <path d="M27 76 33 87 21 87Z" fill="#223027" />
-              <polyline
-                points={roadPath(missions)}
-                fill="none"
-                stroke="rgba(224, 179, 74, 0.16)"
-                strokeWidth="0.5"
-                strokeDasharray="1.6 1.8"
-                strokeLinecap="round"
-              />
+              {roadSegments.map((segment, index) => (
+                <polyline
+                  key={`${segment[0]?.id}-${segment[segment.length - 1]?.id}`}
+                  className="map-road-segment"
+                  points={roadPath(segment)}
+                  fill="none"
+                  pathLength="100"
+                  style={{ animationDelay: `${index * 90}ms` }}
+                />
+              ))}
             </svg>
+            {flightShape && activeVisualFlight ? (
+              <svg className="ship-flight-layer" viewBox="0 0 100 100" preserveAspectRatio="none" aria-hidden="true">
+                <path className="ship-flight-trail-glow" d={flightShape.path} pathLength="100" />
+                <path className="ship-flight-trail" d={flightShape.path} pathLength="100" />
+              </svg>
+            ) : null}
             <div className="map-fog" aria-hidden="true" />
             {/* Волна сканирования от корабля */}
             {scanKey > 0 ? <div key={scanKey} className="scan-wave" aria-hidden="true" /> : null}
             {scanMissed ? <p className="scan-toast" role="status">{t("scan.nothing")}</p> : null}
 
-            {missions.map((mission, index) => {
+            {missions.map((mission) => {
               const point = state.missions.find((candidate) => candidate.id === mission.id);
               const status = point?.status ?? "locked";
-              const rune = RUNES[index % RUNES.length] ?? "✦";
               const isNewlyOpen = justOpened.includes(mission.id);
               return (
                 <button
                   key={mission.id}
                   type="button"
-                  className={`map-marker is-${status}${selectedId === mission.id ? " is-selected" : ""}${isNewlyOpen ? " is-revealed" : ""}`}
+                  className={`map-marker is-${status} is-mission-${mission.type}${selectedId === mission.id ? " is-selected" : ""}${isNewlyOpen ? " is-revealed" : ""}`}
                   style={{ left: `${mission.x}%`, top: `${mission.y}%` }}
                   aria-label={t(`campaign.marker.${status}`, { mission: mission.id })}
                   disabled={status === "locked"}
@@ -462,7 +569,8 @@ export function CampaignScreen() {
                   }}
                 >
                   <span className="marker-medallion" aria-hidden="true">
-                    {status === "done" ? "✓" : status === "locked" ? "?" : rune}
+                    {status === "locked" ? <span className="marker-locked">?</span> : missionIcon(mission.type)}
+                    {status === "done" ? <span className="marker-complete">✓</span> : null}
                   </span>
                   <span className="marker-label" aria-hidden="true">
                     {mission.id.replace("clearing_", "C")}
@@ -472,10 +580,13 @@ export function CampaignScreen() {
             })}
 
             <div
-              className="ship-marker"
+              className={`ship-marker${activeVisualFlight ? " is-flying" : ""}`}
               aria-hidden="true"
               title={t("campaign.ship")}
-              style={{ left: `${shipPosition.x}%`, top: `${shipPosition.y}%` }}
+              style={{
+                left: `${activeVisualFlight?.from.x ?? shipPosition.x}%`,
+                top: `${activeVisualFlight?.from.y ?? shipPosition.y}%`,
+              }}
             >
               <span className="ship-glyph">
                 <ShipIcon />
@@ -689,7 +800,8 @@ export function CampaignScreen() {
       ) : null}
 
       {tab === "chamber" ? (
-        <div className="roster-panel" aria-label={t("campaign.tabChamber")}>
+        <div className="roster-panel chamber-panel" aria-label={t("campaign.tabChamber")}>
+
           <div className="panel-head">
             <h2>{t("campaign.tabChamber")}</h2>
             <p className="muted">
diff --git a/app/packages/ui/src/SettingsScreen.tsx b/app/packages/ui/src/SettingsScreen.tsx
index 1773b4d..7d2b99c 100644
--- a/app/packages/ui/src/SettingsScreen.tsx
+++ b/app/packages/ui/src/SettingsScreen.tsx
@@ -73,6 +73,23 @@ export function SettingsScreen() {
         </label>
       </section>
 
+      <section className="panel">
+        <label className="toggle-row" htmlFor="auto-end-turn">
+          <span className="toggle-meta">
+            <span>{t("settings.autoEndTurn")}</span>
+            <span className="muted">{t("settings.autoEndTurnHint")}</span>
+          </span>
+          <input
+            id="auto-end-turn"
+            type="checkbox"
+            role="switch"
+            aria-checked={state.autoEndTurn}
+            checked={state.autoEndTurn}
+            onChange={(event) => settings.set({ autoEndTurn: event.target.checked })}
+          />
+        </label>
+      </section>
+
       <section className="panel">
         <label className="toggle-row" htmlFor="debug-mode">
           <span className="toggle-meta">
diff --git a/app/packages/ui/src/battle.css b/app/packages/ui/src/battle.css
index 2e8ac6b..4b41f8a 100644
--- a/app/packages/ui/src/battle.css
+++ b/app/packages/ui/src/battle.css
@@ -2,8 +2,8 @@
   position: relative;
   height: 100dvh;
   overflow: hidden;
-  background: #101410;
-  color: #d5cfc0;
+  background: var(--field-background);
+  color: var(--mist);
   touch-action: none;
 }
 
@@ -63,23 +63,59 @@
 
 .hud-btn {
   appearance: none;
-  border: 1px solid #3a4550;
-  background: #1c2320;
-  color: #d5cfc0;
+  border: 1px solid var(--line);
+  background: var(--button-surface);
+  color: var(--mist);
   padding: 10px 12px;
   min-height: 44px;
   cursor: pointer;
 }
 
-.hud-btn:hover,
-.hud-btn:focus-visible {
-  border-color: #e0b34a;
+.hud-btn:hover:not(:disabled),
+.hud-btn:focus-visible:not(:disabled) {
+  border-color: var(--amber);
   outline: none;
+  /* Общее мягкое янтарное свечение для основных боевых команд. */
+  box-shadow: 0 0 14px rgba(224, 179, 74, 0.22);
+}
+
+.hud-btn:disabled {
+  cursor: not-allowed;
+  opacity: 0.52;
+}
+
+/* Переключатель темпа доступен мышью, клавиатурой и скринридером. */
+.speed-toggle {
+  min-width: 48px;
+  padding-inline: 9px;
+  font-weight: 800;
+  letter-spacing: 0.02em;
+}
+
+.speed-toggle.is-on {
+  border-color: var(--warning);
+  color: var(--warning);
+  box-shadow: 0 0 14px rgba(255, 210, 104, 0.24);
 }
 
 .hud-btn-primary {
-  background: linear-gradient(180deg, #222a32, #1a2026);
-  color: #f3ecdc;
+  background: linear-gradient(180deg, var(--primary-surface-top), var(--primary-surface-bottom));
+  color: var(--white);
+}
+
+/* «Завершить ход» — главная команда, когда все ОД потрачены. */
+.end-turn-btn.is-ready {
+  border-color: var(--amber);
+  color: var(--ink);
+  font-weight: 700;
+  background: linear-gradient(180deg, rgba(224, 179, 74, 0.98), rgba(224, 179, 74, 0.78));
+  box-shadow: 0 0 18px rgba(224, 179, 74, 0.28);
+}
+
+.end-turn-btn.is-ready:hover:not(:disabled),
+.end-turn-btn.is-ready:focus-visible:not(:disabled) {
+  background: linear-gradient(180deg, rgba(246, 210, 117, 0.98), rgba(224, 179, 74, 0.86));
+  box-shadow: 0 0 24px rgba(224, 179, 74, 0.45);
 }
 
 .roster {
@@ -97,17 +133,17 @@
   gap: 8px;
   min-width: 108px;
   min-height: 48px;
-  border: 1px solid #3a4550;
-  background: #161c18;
-  color: #d5cfc0;
+  border: 1px solid var(--line);
+  background: var(--roster-surface);
+  color: var(--mist);
   padding: 5px 9px 5px 5px;
   text-align: left;
   cursor: pointer;
 }
 
 .roster-card.is-on {
-  border-color: #e0b34a;
-  background: #1d231b;
+  border-color: var(--amber);
+  background: var(--roster-selected);
 }
 
 .roster-card.is-dead {
@@ -124,7 +160,7 @@
 }
 
 .roster-card.is-on .roster-face {
-  border-color: #e0b34a;
+  border-color: var(--amber);
 }
 
 .roster-card.is-dead .roster-face {
@@ -156,13 +192,13 @@
   height: 30px;
   border-radius: 50%;
   object-fit: cover;
-  border: 2px solid #6d9a3a;
-  background: #131b10;
+  border: 2px solid var(--nav);
+  background: var(--nav-surface);
 }
 
 .enemy-face.is-dead {
   filter: grayscale(1) brightness(0.5);
-  border-color: #3a4550;
+  border-color: var(--line);
 }
 
 .diamonds {
@@ -175,12 +211,12 @@
   width: 10px;
   height: 10px;
   transform: rotate(45deg);
-  border: 2px solid #e0b34a;
+  border: 2px solid var(--amber);
   background: transparent;
 }
 
 .diamond.is-on {
-  background: #e0b34a;
+  background: var(--amber);
 }
 
 .battle-selected {
@@ -199,7 +235,7 @@
   height: 52px;
   border-radius: 8px;
   object-fit: cover;
-  border: 1px solid #e0b34a;
+  border: 1px solid var(--amber);
   flex: 0 0 auto;
 }
 
@@ -218,7 +254,7 @@
   transform: translateX(-50%);
   padding: 8px 26px;
   background: linear-gradient(180deg, rgba(26, 36, 19, 0.96), rgba(15, 21, 10, 0.96));
-  border: 1px solid #8bc34a;
+  border: 1px solid var(--nav);
   border-radius: 4px;
   color: #cfe8a0;
   font-family: Palatino, "Palatino Linotype", "Iowan Old Style", serif;
@@ -304,6 +340,13 @@
   transition: border-color 120ms ease, background 120ms ease, color 120ms ease, box-shadow 120ms ease;
 }
 
+.skill-slot:hover:not(:disabled),
+.skill-slot:focus-visible:not(:disabled) {
+  border-color: var(--amber);
+  outline: none;
+  box-shadow: 0 0 12px rgba(224, 179, 74, 0.2);
+}
+
 .skill-slot::before {
   content: "";
   position: absolute;
@@ -320,9 +363,9 @@
 .skill-slot.is-active,
 .skill-slot[aria-pressed="true"],
 .skill-slot:active:not(:disabled) {
-  border-color: #e0b34a;
+  border-color: var(--amber);
   background: linear-gradient(180deg, #4a3b1e, #2d2819);
-  color: #fff2c4;
+  color: var(--bright-text);
   box-shadow: 0 0 0 1px rgba(224, 179, 74, 0.25), 0 0 14px rgba(224, 179, 74, 0.2);
 }
 
@@ -330,7 +373,7 @@
 .skill-slot[aria-pressed="true"]::before,
 .skill-slot:active:not(:disabled)::before {
   border-color: #ffe49a;
-  background: #e0b34a;
+  background: var(--amber);
   box-shadow: 0 0 7px rgba(224, 179, 74, 0.8);
 }
 
@@ -369,7 +412,7 @@
 .skill-slot kbd {
   display: block;
   font-size: 0.68rem;
-  color: #9a9384;
+  color: var(--mist-dim);
   margin-bottom: 2px;
 }
 
@@ -384,7 +427,7 @@
   font-size: 0.82rem;
   font-variant-numeric: tabular-nums;
   background: rgba(28, 35, 32, 0.94);
-  border: 1px solid #3a4550;
+  border: 1px solid var(--line);
   pointer-events: auto;
   max-width: 360px;
 }
@@ -399,7 +442,7 @@
 .aim-chance {
   font-size: 1.1rem;
   font-weight: 700;
-  color: #f3ecdc;
+  color: var(--white);
 }
 
 .aim-chance.blocked {
@@ -409,17 +452,17 @@
 
 .aim-dmg {
   font-size: 0.78rem;
-  color: #9a9384;
+  color: var(--mist-dim);
 }
 
 .aim-copy-btn {
   margin-left: auto;
   background: rgba(58, 69, 80, 0.4);
-  border: 1px solid #3a4550;
+  border: 1px solid var(--line);
   border-radius: 4px;
   padding: 4px 6px;
   cursor: pointer;
-  color: #9a9384;
+  color: var(--mist-dim);
   display: flex;
   align-items: center;
   justify-content: center;
@@ -427,8 +470,8 @@
 }
 
 .aim-copy-btn:hover {
-  border-color: #e0b34a;
-  color: #e0b34a;
+  border-color: var(--amber);
+  color: var(--amber);
   background: rgba(224, 179, 74, 0.1);
 }
 
@@ -444,7 +487,7 @@
 }
 
 .bd-item {
-  color: #9a9384;
+  color: var(--mist-dim);
 }
 
 .bd-item.pos {
@@ -465,7 +508,7 @@
 .bd-details-title {
   display: block;
   font-size: 0.68rem;
-  color: #9a9384;
+  color: var(--mist-dim);
   margin-bottom: 2px;
   font-weight: 600;
 }
@@ -476,7 +519,7 @@
   color: #c0b898;
   line-height: 1.3;
   padding-left: 6px;
-  border-left: 2px solid #3a4550;
+  border-left: 2px solid var(--line);
   margin-bottom: 2px;
 }
 
@@ -486,9 +529,9 @@
   font-size: 0.88rem;
   font-variant-numeric: tabular-nums;
   background: rgba(28, 35, 32, 0.92);
-  border: 1px solid #3a4550;
+  border: 1px solid var(--line);
   pointer-events: none;
-  color: #e0b34a;
+  color: var(--amber);
 }
 
 .battle-mid {
@@ -513,8 +556,8 @@
 
 .pause-card {
   width: min(100%, 360px);
-  background: #1c232a;
-  border: 1px solid #3a4550;
+  background: var(--ink-2);
+  border: 1px solid var(--line);
   padding: 22px 20px 18px;
 }
 
@@ -566,7 +609,7 @@
   background: rgba(16, 22, 16, 0.94);
   border: 1px solid rgba(224, 179, 74, 0.55);
   border-radius: 8px;
-  color: #f3ecdc;
+  color: var(--white);
   box-shadow: 0 6px 24px rgba(0, 0, 0, 0.5);
   z-index: 72;
   animation: campaign-hint-in-banner 320ms ease-out both;
@@ -623,7 +666,7 @@
 .pause-card h2 {
   margin: 0 0 14px;
   font-family: Palatino, "Palatino Linotype", "Iowan Old Style", serif;
-  color: #f3ecdc;
+  color: var(--white);
 }
 
 .pause-card .hud-btn {
@@ -864,9 +907,9 @@
   display: inline-block;
   margin-right: 8px;
   padding: 1px 8px;
-  border: 1px solid var(--amber-dim, #8a6a24);
+  border: 1px solid var(--amber-dim);
   border-radius: 2px;
-  color: var(--amber, #e0b34a);
+  color: var(--amber);
   font-size: 0.68rem;
   letter-spacing: 0.14em;
   text-transform: uppercase;
@@ -875,14 +918,14 @@
 
 /* Отладочная автопобеда: янтарная молния, отличная от жука-переключателя */
 .debug-win {
-  color: var(--amber, #e0b34a);
-  border-color: var(--amber-dim, #8a6a24);
+  color: var(--amber);
+  border-color: var(--amber-dim);
 }
 
 .debug-win:hover,
 .debug-win:focus-visible {
-  border-color: var(--amber, #e0b34a);
-  color: #f3ecdc;
+  border-color: var(--amber);
+  color: var(--white);
   box-shadow: 0 0 10px rgba(224, 179, 74, 0.35);
 }
 
@@ -924,7 +967,7 @@
 
 .objective-name {
   font-size: 0.72rem;
-  color: var(--amber, #e0b34a);
+  color: var(--amber);
   letter-spacing: 0.04em;
 }
 
@@ -1041,11 +1084,11 @@
 
 .pvp-side-emblem.is-active {
   transform: scale(1.18);
-  color: #f3ecdc;
+  color: var(--white);
 }
 
 .pvp-side-emblem.is-side1.is-active {
-  border-color: var(--amber, #e0b34a);
+  border-color: var(--amber);
   box-shadow: 0 0 14px rgba(224, 179, 74, 0.55);
   animation: emblem-pulse 1.8s ease-in-out infinite;
 }
@@ -1086,7 +1129,7 @@
   width: 26px;
   height: 26px;
   border: 2px solid rgba(224, 179, 74, 0.25);
-  border-top-color: var(--amber, #e0b34a);
+  border-top-color: var(--amber);
   border-radius: 50%;
   animation: net-spin 0.9s linear infinite;
 }
@@ -1193,7 +1236,7 @@
   border: 1px solid rgba(224, 179, 74, 0.45);
   border-bottom: 2px solid rgba(224, 179, 74, 0.55);
   border-radius: 10px;
-  color: #f3ecdc;
+  color: var(--white);
   box-shadow: 0 8px 28px rgba(0, 0, 0, 0.55);
   z-index: 69;
   pointer-events: none;
@@ -1225,7 +1268,7 @@
 }
 
 .training-coach-name {
-  color: var(--amber, #e0b34a);
+  color: var(--amber);
   font-size: 0.72rem;
   letter-spacing: 0.08em;
   text-transform: uppercase;
@@ -1329,7 +1372,7 @@
   padding: 2px 10px;
   border-radius: 10px;
   background: rgba(224, 179, 74, 0.15);
-  color: var(--amber, #e0b34a);
+  color: var(--amber);
   font-family: monospace;
   font-size: 0.75rem;
   letter-spacing: 0.06em;
@@ -1346,7 +1389,7 @@
 
 .controls-help summary {
   cursor: pointer;
-  color: var(--amber, #e0b34a);
+  color: var(--amber);
   font-size: 0.85rem;
   letter-spacing: 0.05em;
   text-transform: uppercase;
@@ -1367,16 +1410,47 @@
   min-width: 22px;
   padding: 1px 6px;
   margin-right: 8px;
-  border: 1px solid #3a4550;
+  border: 1px solid var(--line);
   border-bottom-width: 2px;
   border-radius: 4px;
   background: rgba(28, 35, 32, 0.9);
-  color: #f3ecdc;
+  color: var(--white);
   font-family: ui-monospace, "Cascadia Mono", Consolas, monospace;
   font-size: 0.75rem;
   text-align: center;
 }
 
+/* Целевая клетка обучения за пределами видимой области: стрелка на кромке
+   остаётся доступной без наведения и направлена к цели. */
+.training-edge-hint {
+  position: absolute;
+  z-index: 72;
+  display: grid;
+  place-items: center;
+  width: 32px;
+  height: 32px;
+  border: 1px solid rgba(191, 240, 255, 0.78);
+  border-radius: 50%;
+  background: rgba(7, 20, 25, 0.9);
+  color: #d8f8ff;
+  font-size: 1.5rem;
+  line-height: 1;
+  pointer-events: none;
+  box-shadow: 0 0 0 3px rgba(96, 200, 255, 0.16), 0 0 18px rgba(96, 200, 255, 0.48);
+  animation: training-edge-pulse 1.2s ease-in-out infinite;
+}
+
+@keyframes training-edge-pulse {
+  0%,
+  100% {
+    opacity: 0.78;
+  }
+  50% {
+    opacity: 1;
+    box-shadow: 0 0 0 5px rgba(96, 200, 255, 0.08), 0 0 24px rgba(96, 200, 255, 0.65);
+  }
+}
+
 /* Плашка реактивной подсказки обучения (0.20.1): у нижнего края, над
    панелью действий, чтобы не перекрывать центр поля. */
 .training-note {
@@ -1402,7 +1476,7 @@
 }
 
 .training-note-mark {
-  color: var(--amber, #e0b34a);
+  color: var(--amber);
   flex: none;
 }
 
@@ -1425,8 +1499,8 @@
 
 .training-skip:hover,
 .training-skip:focus-visible {
-  border-color: var(--amber, #e0b34a);
-  color: var(--amber, #e0b34a);
+  border-color: var(--amber);
+  color: var(--amber);
 }
 
 /* Кнопка «Далее» ознакомительного шага (0.20.2). */
@@ -1437,7 +1511,7 @@
   border: 1px solid rgba(224, 179, 74, 0.6);
   border-radius: 10px;
   background: rgba(224, 179, 74, 0.16);
-  color: var(--amber, #e0b34a);
+  color: var(--amber);
   font-size: 0.85rem;
   cursor: pointer;
   pointer-events: auto;
@@ -1473,6 +1547,258 @@
 }
 
 .training-step-dot.is-current {
-  background: var(--amber, #e0b34a);
+  background: var(--amber);
   transform: scale(1.3);
 }
+
+/* Уважение к системной настройке «уменьшить движение» (Этап 1.7).
+   Сама логика Pixi также получает этот флаг из BattleScreenView. */
+@media (prefers-reduced-motion: reduce) {
+  .battle-screen,
+  .battle-screen *,
+  .battle-screen *::before,
+  .battle-screen *::after {
+    animation-duration: 0.01ms !important;
+    animation-iteration-count: 1 !important;
+    transition-duration: 0.01ms !important;
+    scroll-behavior: auto !important;
+  }
+}
+
+/* ============================================================
+   Этап 4: мобильная панель и привязанная карточка шанса
+   ============================================================ */
+
+/* DOM-позиция подсказки рассчитывается от экранной позиции цели в рендерере.
+   battle-hud становится её containing block, поэтому панель не выпадает за
+   границы поля при панорамировании и на узком экране. */
+.battle-mid {
+  position: static;
+}
+
+.aim-card {
+  position: absolute;
+  z-index: 24;
+  left: 50%;
+  top: 50%;
+  max-width: min(360px, calc(100% - 16px));
+  margin: 0;
+  transform: translate(-50%, -50%);
+  box-shadow: 0 8px 24px rgba(0, 0, 0, 0.42);
+  animation: aim-card-in var(--ui-motion-duration) var(--ui-motion-ease) both;
+}
+
+@keyframes aim-card-in {
+  from {
+    opacity: 0;
+    transform: translateY(5px) scale(0.98);
+  }
+  to {
+    opacity: 1;
+    transform: none;
+  }
+}
+
+.skill-row-wrap {
+  min-width: 0;
+  flex: 1 1 auto;
+  display: flex;
+  flex-direction: column;
+  gap: 3px;
+}
+
+.skill-row {
+  min-width: 0;
+  flex-wrap: nowrap;
+  justify-content: flex-start;
+  overflow-x: auto;
+  overflow-y: hidden;
+  scrollbar-width: none;
+  -ms-overflow-style: none;
+  overscroll-behavior-inline: contain;
+  padding: 2px 1px;
+}
+
+.skill-row::-webkit-scrollbar,
+.roster::-webkit-scrollbar {
+  display: none;
+}
+
+.skill-row-hint {
+  display: none;
+  align-self: center;
+  color: var(--mist-dim);
+  font-size: 0.62rem;
+  letter-spacing: 0.06em;
+  opacity: 0.82;
+}
+
+/* Круглые аватары не теряют ОД: ромбики остаются отдельным слоем внутри
+   кнопки, а полное имя доступно через aria-label/title. */
+@media (max-width: 640px) {
+  .battle-bottom {
+    flex-wrap: wrap;
+    align-items: center;
+  }
+
+  .roster {
+    order: 1;
+    flex: 0 1 auto;
+    max-width: 42%;
+    flex-wrap: nowrap;
+    overflow-x: auto;
+    overflow-y: visible;
+    justify-content: flex-start;
+    padding: 3px 3px 5px;
+  }
+
+  .roster-card {
+    position: relative;
+    flex: 0 0 46px;
+    width: 46px;
+    min-width: 46px;
+    height: 46px;
+    min-height: 46px;
+    padding: 4px;
+    border-radius: 50%;
+    justify-content: center;
+    background: radial-gradient(circle at 35% 28%, #26352b, #141b18);
+  }
+
+  .roster-card.is-on {
+    box-shadow: 0 0 0 2px rgba(224, 179, 74, 0.24), 0 0 12px rgba(224, 179, 74, 0.18);
+  }
+
+  .roster-face {
+    width: 34px;
+    height: 34px;
+    border-radius: 50%;
+  }
+
+  .roster-meta {
+    position: absolute;
+    inset: auto 0 -2px;
+    align-items: center;
+    pointer-events: none;
+  }
+
+  .roster-card .name {
+    display: none;
+  }
+
+  .roster-card .diamonds {
+    gap: 3px;
+    margin: 0;
+    padding: 2px 4px;
+    border-radius: 7px;
+    background: rgba(9, 12, 10, 0.9);
+  }
+
+  .roster-card .diamond {
+    width: 7px;
+    height: 7px;
+    border-width: 1px;
+  }
+
+  .skill-row-wrap {
+    order: 2;
+    flex: 1 1 52%;
+    max-width: none;
+  }
+
+  .skill-row {
+    justify-content: flex-start;
+  }
+
+  .skill-row-hint {
+    display: block;
+  }
+
+  .skill-slot {
+    flex: 0 0 auto;
+    min-width: 76px;
+  }
+
+  .battle-selected {
+    order: 3;
+    flex: 1 1 100%;
+  }
+
+  .end-turn-btn {
+    order: 4;
+  }
+}
+
+@media (max-width: 420px) and (orientation: portrait) {
+  .roster {
+    max-width: 100%;
+    flex-basis: 100%;
+    order: 1;
+  }
+
+  .skill-row-wrap {
+    flex-basis: 100%;
+    order: 2;
+  }
+}
+
+/* 4.10: all transient panels use the same entrance rhythm as modal windows. */
+.modal-root,
+.pause-root,
+.pass-device-root {
+  animation: ui-overlay-in var(--ui-motion-duration) var(--ui-motion-ease) both;
+}
+
+.modal,
+.pause-card,
+.pass-device-card,
+.campaign-hint-card {
+  animation: ui-panel-in var(--ui-motion-duration) var(--ui-motion-ease) both;
+}
+
+.phase-banner,
+.campaign-hint-banner,
+.training-coach,
+.training-note,
+.replay-bar,
+.scan-toast,
+.loss-banner {
+  animation-duration: var(--ui-motion-duration);
+  animation-timing-function: var(--ui-motion-ease);
+}
+
+@keyframes ui-overlay-in {
+  from { opacity: 0; }
+  to { opacity: 1; }
+}
+
+@keyframes ui-panel-in {
+  from {
+    opacity: 0;
+    transform: translateY(8px) scale(0.985);
+  }
+  to {
+    opacity: 1;
+    transform: none;
+  }
+}
+
+@media (prefers-reduced-motion: reduce) {
+  .aim-card,
+  .training-edge-hint,
+  .modal-root,
+  .pause-root,
+  .pass-device-root,
+  .modal,
+  .pause-card,
+  .pass-device-card,
+  .campaign-hint-card,
+  .phase-banner,
+  .campaign-hint-banner,
+  .training-coach,
+  .training-note,
+  .replay-bar,
+  .loss-banner {
+    animation: none !important;
+  }
+}
diff --git a/app/packages/ui/src/campaign.css b/app/packages/ui/src/campaign.css
index 0ed63d7..eddc9ad 100644
--- a/app/packages/ui/src/campaign.css
+++ b/app/packages/ui/src/campaign.css
@@ -63,7 +63,7 @@
   margin: 0;
   font-family: Palatino, "Palatino Linotype", "Iowan Old Style", serif;
   font-size: clamp(1.5rem, 4vw, 2rem);
-  color: #f3ecdc;
+  color: var(--white);
   letter-spacing: 0.05em;
 }
 
@@ -89,7 +89,7 @@
 .campaign-darkness-value {
   font-family: ui-monospace, "Cascadia Mono", Consolas, monospace;
   font-size: 0.95rem;
-  color: #f3ecdc;
+  color: var(--white);
   white-space: nowrap;
 }
 
@@ -238,7 +238,7 @@
   margin: 7px auto 0;
   border-radius: 50%;
   border: 2px solid #5d6b76;
-  background: radial-gradient(circle at 32% 28%, #2a333c, #1b2229);
+  background: radial-gradient(circle at 32% 28%, var(--backdrop-highlight), #1b2229);
   color: #8fa1ad;
   font-size: 0.9rem;
   line-height: 1;
@@ -259,7 +259,7 @@
 }
 
 .map-marker.is-selected .marker-medallion {
-  border-color: #f3ecdc;
+  border-color: var(--white);
   outline: 3px solid rgba(224, 179, 74, 0.28);
   outline-offset: 3px;
 }
@@ -421,7 +421,7 @@
   margin: 0;
   font-family: Palatino, "Palatino Linotype", serif;
   font-size: 1.35rem;
-  color: #f3ecdc;
+  color: var(--white);
 }
 
 .mission-id {
@@ -533,7 +533,7 @@
 
 .campaign-abandon-btn:hover,
 .campaign-abandon-btn:focus-visible {
-  border-color: #c45c5c;
+  border-color: var(--danger);
   color: #d98080;
   outline: none;
 }
@@ -885,14 +885,14 @@
   height: 18px;
   border-radius: 50%;
   background: #a03a4e;
-  border: 2px solid #14181c;
+  border: 2px solid var(--ink);
   display: grid;
   place-items: center;
 }
 
 .wound-badge::after {
   content: "✚";
-  color: #f3ecdc;
+  color: var(--white);
   font-size: 0.7rem;
   line-height: 1;
 }
@@ -906,7 +906,7 @@
 
 .deploy-name {
   font-weight: 600;
-  color: #f3ecdc;
+  color: var(--white);
   white-space: nowrap;
   overflow: hidden;
   text-overflow: ellipsis;
@@ -1006,7 +1006,7 @@
   margin: 0;
   font-family: Palatino, "Palatino Linotype", serif;
   font-size: 1.3rem;
-  color: #f3ecdc;
+  color: var(--white);
 }
 
 .fighter-list {
@@ -1077,7 +1077,7 @@
   align-items: center;
   gap: 8px;
   font-weight: 600;
-  color: #f3ecdc;
+  color: var(--white);
 }
 
 .fallen-tag,
@@ -1174,7 +1174,7 @@
   border-radius: 50%;
   display: grid;
   place-items: center;
-  color: #14181c;
+  color: var(--ink);
   background: #5d7d44;
   font-size: 0.72rem;
   font-weight: 700;
@@ -1257,7 +1257,7 @@
   padding: 0 4px;
   border-radius: 9px;
   background: #a03a4e;
-  color: #f3ecdc;
+  color: var(--white);
   font-size: 0.68rem;
   line-height: 18px;
   text-align: center;
@@ -1325,7 +1325,7 @@
 
 .outcome-names {
   margin: 2px 0 0;
-  color: #f3ecdc;
+  color: var(--white);
   font-size: 0.95rem;
 }
 
@@ -1538,7 +1538,7 @@
   border: 1px solid rgba(224, 179, 74, 0.5);
   border-radius: 6px;
   background: rgba(16, 22, 16, 0.92);
-  color: #f3ecdc;
+  color: var(--white);
   font-size: 0.85rem;
   z-index: 6;
   pointer-events: none;
@@ -1644,7 +1644,7 @@
 
 .forge-name {
   font-weight: 600;
-  color: #f3ecdc;
+  color: var(--white);
   font-size: 0.95rem;
 }
 
@@ -1826,7 +1826,7 @@
 
 .equip-item-name {
   font-weight: 600;
-  color: #f3ecdc;
+  color: var(--white);
 }
 
 .equip-item-effects {
@@ -2087,7 +2087,7 @@
 }
 
 .pvp-side-card.is-side1 {
-  border-top: 3px solid #e0b34a;
+  border-top: 3px solid var(--amber);
   animation-delay: 60ms;
 }
 
@@ -2116,7 +2116,7 @@
 }
 
 .is-side1 .pvp-side-title {
-  color: var(--amber, #e0b34a);
+  color: var(--amber);
 }
 
 .is-side2 .pvp-side-title {
@@ -2177,7 +2177,7 @@
   padding: 2px 10px;
   border: 1px solid rgba(224, 179, 74, 0.5);
   border-radius: 10px;
-  color: var(--amber, #e0b34a);
+  color: var(--amber);
   font-family: monospace;
   font-size: 0.8rem;
 }
@@ -2288,7 +2288,7 @@
 }
 
 .pvp-tab.is-active {
-  color: var(--amber, #e0b34a);
+  color: var(--amber);
   border-color: rgba(224, 179, 74, 0.65);
   background: rgba(58, 49, 32, 0.5);
   box-shadow: inset 0 -2px 0 rgba(224, 179, 74, 0.7);
@@ -2384,7 +2384,7 @@
   border: 1px solid rgba(120, 140, 160, 0.3);
   border-radius: 4px;
   background: rgba(0, 0, 0, 0.35);
-  color: #f3ecdc;
+  color: var(--white);
   font-family: monospace;
   font-size: 0.8rem;
   transition: border-color 180ms ease;
@@ -2482,7 +2482,7 @@
 
 .pvp-radio.is-on {
   border-color: rgba(224, 179, 74, 0.7);
-  color: var(--amber, #e0b34a);
+  color: var(--amber);
   background: rgba(58, 49, 32, 0.45);
 }
 
@@ -2509,7 +2509,7 @@
 
 .pvp-n-btn.is-on {
   border-color: rgba(224, 179, 74, 0.7);
-  color: var(--amber, #e0b34a);
+  color: var(--amber);
   background: rgba(58, 49, 32, 0.45);
   box-shadow: 0 0 10px rgba(224, 179, 74, 0.25);
 }
@@ -2524,7 +2524,7 @@
 }
 
 .pvp-check input {
-  accent-color: #e0b34a;
+  accent-color: var(--amber);
 }
 
 .draft {
@@ -2552,7 +2552,7 @@
 }
 
 .draft-side.is-side1 {
-  color: var(--amber, #e0b34a);
+  color: var(--amber);
 }
 
 .draft-side.is-side2 {
@@ -2790,7 +2790,7 @@
 
 .room-id {
   font-family: monospace;
-  color: var(--amber, #e0b34a);
+  color: var(--amber);
   font-size: 0.85rem;
 }
 
@@ -2830,7 +2830,7 @@
 }
 
 .training-mentor-row b {
-  color: var(--amber, #e0b34a);
+  color: var(--amber);
   font-weight: 600;
 }
 
@@ -2894,7 +2894,7 @@
   height: 46px;
   border-radius: 50%;
   border: 1px solid rgba(224, 179, 74, 0.5);
-  color: var(--amber, #e0b34a);
+  color: var(--amber);
   background: radial-gradient(circle at 32% 28%, #3a3120, #241d12);
 }
 
@@ -2913,7 +2913,7 @@
 .training-card-title {
   font-family: Palatino, "Palatino Linotype", serif;
   font-size: 1.15rem;
-  color: #f3ecdc;
+  color: var(--white);
 }
 
 .training-card-desc {
@@ -2927,7 +2927,7 @@
 }
 
 .training-start {
-  color: var(--amber, #e0b34a);
+  color: var(--amber);
   font-size: 0.85rem;
   letter-spacing: 0.06em;
 }
@@ -2963,3 +2963,273 @@
 .training-all-done .btn {
   margin-top: 6px;
 }
+
+/* ============================================================
+   Этап 4: открытая карта, перелёт и тематические панели
+   ============================================================ */
+
+/* Дорога существует только для открытых серий, которые CampaignScreen
+   передаёт отдельными polyline. Dash-offset даёт короткое проявление без
+   перерисовки закрытых участков или дополнительной текстуры. */
+.map-road-segment {
+  stroke: var(--movement);
+  stroke-width: 0.7;
+  stroke-opacity: 0.72;
+  stroke-dasharray: 100;
+  stroke-dashoffset: 100;
+  stroke-linecap: round;
+  animation: map-road-reveal 720ms var(--ui-motion-ease) both;
+}
+
+@keyframes map-road-reveal {
+  from {
+    stroke-dashoffset: 100;
+    opacity: 0;
+  }
+  to {
+    stroke-dashoffset: 0;
+    opacity: 1;
+  }
+}
+
+.ship-flight-layer {
+  position: absolute;
+  inset: 0;
+  width: 100%;
+  height: 100%;
+  z-index: 4;
+  pointer-events: none;
+  overflow: visible;
+}
+
+.ship-flight-trail,
+.ship-flight-trail-glow {
+  fill: none;
+  stroke-linecap: round;
+  stroke-dasharray: 100;
+  stroke-dashoffset: 100;
+  animation: ship-trail-draw 820ms var(--ui-motion-ease) both;
+}
+
+.ship-flight-trail {
+  stroke: var(--movement-bright);
+  stroke-width: 0.7;
+  opacity: 0.8;
+}
+
+.ship-flight-trail-glow {
+  stroke: var(--movement);
+  stroke-width: 2.4;
+  opacity: 0.22;
+  filter: blur(1px);
+}
+
+@keyframes ship-trail-draw {
+  0% {
+    stroke-dashoffset: 100;
+    opacity: 0;
+  }
+  16% {
+    opacity: 0.9;
+  }
+  76% {
+    stroke-dashoffset: 0;
+    opacity: 0.72;
+  }
+  100% {
+    stroke-dashoffset: 0;
+    opacity: 0;
+  }
+}
+
+.ship-marker.is-flying,
+.campaign-map.is-flying .ship-marker {
+  animation: ship-flight 820ms var(--ui-motion-ease) forwards;
+}
+
+@keyframes ship-flight {
+  0% {
+    left: var(--ship-from-x);
+    top: var(--ship-from-y);
+    transform: translate(-50%, -50%) rotate(-3deg) scale(0.94);
+  }
+  42% {
+    left: var(--ship-mid-x);
+    top: calc(var(--ship-mid-y) - var(--ship-arc));
+    transform: translate(-50%, -50%) rotate(4deg) scale(1.04);
+  }
+  100% {
+    left: var(--ship-to-x);
+    top: var(--ship-to-y);
+    transform: translate(-50%, -50%) rotate(0deg) scale(1);
+  }
+}
+
+.marker-medallion svg {
+  width: 18px;
+  height: 18px;
+  display: block;
+}
+
+.marker-locked {
+  font-size: 0.88rem;
+  line-height: 1;
+}
+
+.marker-medallion {
+  position: relative;
+}
+
+.marker-complete {
+  position: absolute;
+  right: -3px;
+  bottom: -3px;
+  display: grid;
+  place-items: center;
+  width: 13px;
+  height: 13px;
+  border: 1px solid var(--ink);
+  border-radius: 50%;
+  background: var(--success);
+  color: var(--ink);
+  font-size: 0.58rem;
+  font-weight: 800;
+  line-height: 1;
+}
+
+/* 4.5: разные поверхности зон корабля, но одна цветовая семантика.
+   Медный — кузнечная работа, травяной — лечение/природа, приглушённый
+   красный остаётся только на ранении и потере. */
+.forge-panel {
+  --panel-accent: var(--forge);
+  --panel-accent-bright: var(--forge-bright);
+  border-color: rgba(194, 118, 76, 0.52);
+  background: linear-gradient(150deg, #2b211e, #1b1918 68%, #171b1c);
+  box-shadow: inset 0 2px 0 rgba(224, 161, 107, 0.18);
+}
+
+.forge-panel .panel-head h2,
+.forge-panel .forge-name {
+  color: var(--forge-bright);
+}
+
+.forge-panel .panel-head {
+  border-bottom: 1px solid rgba(194, 118, 76, 0.2);
+  padding-bottom: 10px;
+}
+
+.forge-panel .forge-card {
+  border-color: rgba(194, 118, 76, 0.34);
+  background: linear-gradient(150deg, rgba(69, 43, 34, 0.72), rgba(25, 24, 23, 0.94));
+}
+
+.forge-panel .forge-card:hover {
+  border-color: var(--forge);
+  box-shadow: 0 0 18px rgba(194, 118, 76, 0.14);
+}
+
+.forge-panel .forge-icon {
+  border-color: rgba(194, 118, 76, 0.62);
+  color: var(--forge-bright);
+  background: rgba(93, 52, 37, 0.28);
+}
+
+.forge-panel .craft-btn {
+  border-color: rgba(194, 118, 76, 0.62);
+  color: var(--forge-bright);
+}
+
+.forge-panel .craft-btn:hover:not(:disabled),
+.forge-panel .craft-btn:focus-visible:not(:disabled) {
+  border-color: var(--forge-bright);
+  background: rgba(194, 118, 76, 0.16);
+}
+
+.forge-panel .forge-note {
+  color: #d6a17c;
+}
+
+.chamber-panel {
+  --panel-accent: var(--chamber);
+  border-color: rgba(157, 187, 114, 0.48);
+  background: linear-gradient(150deg, #202b20, #171e18 70%, #151a18);
+  box-shadow: inset 0 2px 0 rgba(191, 218, 143, 0.16);
+}
+
+.chamber-panel .panel-head h2 {
+  color: var(--chamber-bright);
+}
+
+.chamber-panel .panel-head {
+  border-bottom: 1px solid rgba(157, 187, 114, 0.2);
+  padding-bottom: 10px;
+}
+
+.chamber-panel .fighter-row {
+  border-color: rgba(157, 187, 114, 0.27);
+  background: linear-gradient(150deg, rgba(39, 57, 38, 0.82), rgba(21, 28, 22, 0.96));
+}
+
+.chamber-panel .heal-btn {
+  border-color: rgba(157, 187, 114, 0.62);
+  color: var(--chamber-bright);
+  background: linear-gradient(180deg, rgba(71, 96, 49, 0.52), rgba(27, 39, 25, 0.9));
+}
+
+.chamber-panel .heal-btn:hover,
+.chamber-panel .heal-btn:focus-visible {
+  border-color: var(--chamber-bright);
+  color: #e0efbd;
+}
+
+.chamber-panel .mission-empty svg {
+  color: var(--chamber);
+}
+
+/* Визуальный знак потери не конкурирует с зелёным лечением. */
+.fighter-row.is-wounded,
+.chamber-panel .fighter-row.is-wounded {
+  border-left-color: var(--injury);
+}
+
+@media (prefers-reduced-motion: reduce) {
+  .map-road-segment {
+    animation: none !important;
+    stroke-dashoffset: 0;
+    opacity: 1;
+  }
+
+  .ship-flight-layer,
+  .ship-flight-trail,
+  .ship-flight-trail-glow,
+  .ship-marker.is-flying,
+  .campaign-map.is-flying .ship-marker {
+    animation: none !important;
+  }
+}
+
+/* 4.10: campaign-only modal surfaces follow the same motion tokens as HUD. */
+.pass-device-root {
+  animation: ui-overlay-in var(--ui-motion-duration) var(--ui-motion-ease) both;
+}
+
+.pass-device-card {
+  animation: ui-panel-in var(--ui-motion-duration) var(--ui-motion-ease) both;
+}
+
+.map-marker.is-mission-destroy .marker-medallion {
+  color: var(--danger);
+}
+
+.map-marker.is-mission-rescue .marker-medallion {
+  color: var(--success);
+}
+
+.map-marker.is-mission-recon .marker-medallion,
+.map-marker.is-mission-needle .marker-medallion {
+  color: var(--movement-bright);
+}
+
+.map-marker.is-mission-purge .marker-medallion {
+  color: var(--amber);
+}
diff --git a/app/packages/ui/src/hooks.ts b/app/packages/ui/src/hooks.ts
index 9d7b029..08dff85 100644
--- a/app/packages/ui/src/hooks.ts
+++ b/app/packages/ui/src/hooks.ts
@@ -20,3 +20,26 @@ export function useSettingsState() {
   useEffect(() => settings.subscribe(setState), [settings]);
   return state;
 }
+
+/** Системная настройка доступности: едина для DOM и Pixi-слоя боя. */
+export function usePrefersReducedMotion(): boolean {
+  const read = (): boolean =>
+    typeof window !== "undefined" && typeof window.matchMedia === "function"
+      ? window.matchMedia("(prefers-reduced-motion: reduce)").matches
+      : false;
+  const [reduced, setReduced] = useState(read);
+
+  useEffect(() => {
+    const query = window.matchMedia("(prefers-reduced-motion: reduce)");
+    const update = (): void => setReduced(query.matches);
+    update();
+    if (typeof query.addEventListener === "function") {
+      query.addEventListener("change", update);
+      return () => query.removeEventListener("change", update);
+    }
+    query.addListener(update);
+    return () => query.removeListener(update);
+  }, []);
+
+  return reduced;
+}
diff --git a/app/packages/ui/src/training-progress.ts b/app/packages/ui/src/training-progress.ts
index a76d91e..872142f 100644
--- a/app/packages/ui/src/training-progress.ts
+++ b/app/packages/ui/src/training-progress.ts
@@ -56,6 +56,8 @@ export interface AutoEndTurnConditions {
   isSpectator: boolean;
   isTraining: boolean;
   activeHint: TrainingHintConfig | null;
+  /** Настройка игрока; по умолчанию включена для обратной совместимости. */
+  autoEndTurn: boolean;
   activeOwner: number;
   viewOwner: number;
   /** Живые бойцы активной стороны (coverType 0, maxAp > 0). */
@@ -82,6 +84,7 @@ export interface AutoEndTurnConditions {
  */
 export function shouldAutoEndTurn(conditions: AutoEndTurnConditions): boolean {
   if (conditions.paused || conditions.busy || conditions.enemyPhase) return false;
+  if (!conditions.autoEndTurn) return false;
   if (conditions.isReplay || conditions.isSpectator) return false;
   if (conditions.activeOwner !== conditions.viewOwner) return false;
   // A training step is a contract with the player. The scenario prescribes
diff --git a/app/packages/ui/tests/boot-saved.test.tsx b/app/packages/ui/tests/boot-saved.test.tsx
index 5ae53d4..7c6d375 100644
--- a/app/packages/ui/tests/boot-saved.test.tsx
+++ b/app/packages/ui/tests/boot-saved.test.tsx
@@ -11,9 +11,11 @@ vi.mock("@bylina/render", () => {
     update: () => undefined,
     play: async () => undefined,
     pan: () => undefined,
+    centerOn: () => undefined,
     destroy: () => undefined,
     setOnActivate: () => undefined,
     setOnHover: () => undefined,
+    setPlaybackSpeed: () => undefined,
   };
   return { createFieldRenderer: () => stub };
 });
diff --git a/app/packages/ui/tests/training-battle-dom.test.tsx b/app/packages/ui/tests/training-battle-dom.test.tsx
index c0b18c3..2c73844 100644
--- a/app/packages/ui/tests/training-battle-dom.test.tsx
+++ b/app/packages/ui/tests/training-battle-dom.test.tsx
@@ -26,10 +26,13 @@ const rendererStub: FieldRenderer = {
   }),
   play: vi.fn(async () => undefined),
   pan: vi.fn(),
+  centerOn: vi.fn(),
+  screenPosition: vi.fn(() => ({ x: 900, y: 10 })),
   destroy: vi.fn(),
   setOnActivate: vi.fn((handler: (x: number, y: number) => void) => {
     activate = handler;
   }),
+  setPlaybackSpeed: vi.fn(),
   setOnHover: vi.fn((handler: (x: number, y: number) => void) => {
     hover = handler;
   }),
@@ -124,6 +127,7 @@ describe("training battle DOM (0.20.13)", () => {
       await act(async () => {
         await tick(60);
       });
+      expect(document.querySelector(".training-edge-hint"), "off-screen training direction hint").not.toBeNull();
 
       // Шаг 2 — переход: указание разрешает ровно одну клетку (та же, что
       // вычисляет чистый модуль на том же семени).
diff --git a/app/packages/ui/tests/training-progress.test.ts b/app/packages/ui/tests/training-progress.test.ts
index 22f7e80..631f6cf 100644
--- a/app/packages/ui/tests/training-progress.test.ts
+++ b/app/packages/ui/tests/training-progress.test.ts
@@ -59,6 +59,7 @@ describe("shouldAutoEndTurn in training (0.20.13)", () => {
       isSpectator: false,
       isTraining: true,
       activeHint: hint({ until: "move" }),
+      autoEndTurn: true,
       activeOwner: 1,
       viewOwner: 1,
       ownUnits: [{ ap: 0 }],
@@ -81,5 +82,6 @@ describe("shouldAutoEndTurn in training (0.20.13)", () => {
     expect(conditions({ activeHint: null, busy: true })).toBe(false);
     expect(conditions({ activeHint: null, enemyPhase: true })).toBe(false);
     expect(conditions({ activeHint: null, isTraining: false, outcomeOngoing: false })).toBe(false);
+    expect(conditions({ activeHint: null, autoEndTurn: false })).toBe(false);
   });
 });
diff --git a/app/packages/ui/tests/training-ui-rules.test.ts b/app/packages/ui/tests/training-ui-rules.test.ts
index a1fefc9..6f93595 100644
--- a/app/packages/ui/tests/training-ui-rules.test.ts
+++ b/app/packages/ui/tests/training-ui-rules.test.ts
@@ -84,6 +84,7 @@ describe("auto end turn stays behind the lesson (0.20.6/0.20.13)", () => {
       isSpectator: false,
       isTraining,
       activeHint: activeHint as never,
+      autoEndTurn: true,
       activeOwner: PLAYER_OWNER,
       viewOwner: PLAYER_OWNER,
       ownUnits: [{ ap: 0 }],
diff --git a/app/pnpm-lock.yaml b/app/pnpm-lock.yaml
index 3f5b436..6a7a33e 100644
--- a/app/pnpm-lock.yaml
+++ b/app/pnpm-lock.yaml
@@ -75,7 +75,7 @@ importers:
         specifier: ^5.4.11
         version: 5.4.21(@types/node@22.20.1)(terser@5.50.0)
       vite-plugin-pwa:
-        specifier: ^0.21.1
+        specifier: ^0.21.2
         version: 0.21.2(vite@5.4.21(@types/node@22.20.1)(terser@5.50.0))(workbox-build@7.4.1(@types/babel__core@7.20.5))(workbox-window@7.4.1)
 
   apps/signaling-server:
diff --git a/app/scripts/check-visual-regression.mjs b/app/scripts/check-visual-regression.mjs
new file mode 100644
index 0000000..2bd9162
--- /dev/null
+++ b/app/scripts/check-visual-regression.mjs
@@ -0,0 +1,147 @@
+#!/usr/bin/env node
+/**
+ * Проверка визуального набора состояний без зависимости от конкретного
+ * браузера. Для каждого эталона фиксируются viewport, ключевые DOM/Pixi
+ * ориентиры и digest исходников, влияющих на этот кадр. Любая правка
+ * визуального слоя требует явного обновления эталона через
+ * `pnpm visual:regression:update`, поэтому случайная «поплывшая» картинка не
+ * проходит сборку молча.
+ *
+ * Полноразмерные PNG не хранятся в репозитории: PixiJS и системные шрифты
+ * дают разные пиксели на CI, Android и локальной машине. Этот слой проверяет
+ * стабильный визуальный контракт; ручной аудит выполняется в тех же шести
+ * состояниях по рецептам из visual-baselines/states.json.
+ */
+import { createHash } from "node:crypto";
+import { readFile, writeFile } from "node:fs/promises";
+import path from "node:path";
+import process from "node:process";
+import { fileURLToPath } from "node:url";
+
+const root = path.resolve(path.dirname(fileURLToPath(import.meta.url)), "..");
+const baselinePath = path.join(root, "visual-baselines", "states.json");
+const packagePath = path.join(root, "package.json");
+const sourceRoot = root;
+
+const states = [
+  {
+    id: "battle-start",
+    viewport: { width: 1280, height: 720 },
+    route: "menu → quick match → normal",
+    selector: ".battle-screen",
+    landmarks: [".battle-top", ".battle-stage", ".battle-bottom", ".battle-objective"],
+    sources: ["packages/ui/src/BattleScreenView.tsx", "packages/ui/src/battle.css", "packages/render/src/field-renderer.ts"],
+  },
+  {
+    id: "aiming",
+    viewport: { width: 1280, height: 720 },
+    route: "battle-start → selected weapon → target preview",
+    selector: ".aim-card",
+    landmarks: [".aim-card", ".aim-chance", ".battle-stage", ".aim-dmg"],
+    sources: ["packages/ui/src/BattleScreenView.tsx", "packages/ui/src/battle.css", "packages/render/src/field-renderer.ts"],
+  },
+  {
+    id: "fog",
+    viewport: { width: 1280, height: 720 },
+    route: "battle-start → hidden cells",
+    selector: ".battle-stage canvas",
+    landmarks: [".battle-stage", "--fog", "fogBaseLayer", "paintFog"],
+    sources: ["packages/ui/src/BattleScreenView.tsx", "packages/ui/src/battle.css", "packages/render/src/field-renderer.ts"],
+  },
+  {
+    id: "training",
+    viewport: { width: 390, height: 844 },
+    route: "menu → training → first mission",
+    selector: ".training-coach",
+    landmarks: [".training-coach", ".training-step-dots", ".training-edge-hint", ".is-training-focus"],
+    sources: ["packages/ui/src/BattleScreenView.tsx", "packages/ui/src/battle.css", "packages/ui/src/training-scenario.ts"],
+  },
+  {
+    id: "campaign-map",
+    viewport: { width: 1280, height: 720 },
+    route: "menu → new tale",
+    selector: ".campaign-map",
+    landmarks: [".campaign-map", ".map-road-segment", ".map-marker", ".ship-marker"],
+    sources: ["packages/ui/src/CampaignScreen.tsx", "packages/ui/src/campaign.css", "packages/campaign/src/index.ts"],
+  },
+  {
+    id: "mission-result",
+    viewport: { width: 1280, height: 720 },
+    route: "campaign battle → victory/defeat result",
+    selector: ".mission-result-screen",
+    landmarks: [".mission-result-screen", ".roster-outcomes", ".rewards-strip", ".darkness-summary"],
+    sources: ["packages/ui/src/MissionResultScreen.tsx", "packages/ui/src/campaign.css", "apps/game-pwa/src/styles.css"],
+  },
+];
+
+function digestFor(state) {
+  const hash = createHash("sha256");
+  for (const relative of state.sources) {
+    hash.update(relative);
+    hash.update("\0");
+    // The source list is intentionally explicit so new visual surfaces cannot
+    // enter the contract without an intentional baseline review.
+    hash.update(requireSource(relative));
+    hash.update("\0");
+  }
+  return hash.digest("hex");
+}
+
+const sourceCache = new Map();
+function requireSource(relative) {
+  const cached = sourceCache.get(relative);
+  if (cached !== undefined) return cached;
+  // Filled synchronously by the top-level async pass below. Keeping the helper
+  // synchronous makes the digest algorithm easy to audit.
+  throw new Error(`Source was not loaded: ${relative}`);
+}
+
+const packageJson = JSON.parse(await readFile(packagePath, "utf8"));
+const baseline = JSON.parse(await readFile(baselinePath, "utf8"));
+const update = process.argv.includes("--update");
+
+if (baseline.format !== 1) throw new Error(`${path.relative(root, baselinePath)}: unsupported format`);
+if (!Array.isArray(baseline.states)) throw new Error(`${path.relative(root, baselinePath)}: states must be an array`);
+
+for (const state of states) {
+  for (const relative of state.sources) sourceCache.set(relative, await readFile(path.join(sourceRoot, relative), "utf8"));
+}
+
+const current = states.map((state) => ({ ...state, sourceDigest: digestFor(state) }));
+const currentById = new Map(current.map((state) => [state.id, state]));
+const failures = [];
+
+if (baseline.version !== packageJson.version) failures.push(`baseline version ${baseline.version} does not match package ${packageJson.version}`);
+if (baseline.states.length !== states.length) failures.push(`expected ${states.length} states, found ${baseline.states.length}`);
+for (const expected of baseline.states) {
+  const state = currentById.get(expected.id);
+  if (!state) {
+    failures.push(`unknown baseline state: ${expected.id}`);
+    continue;
+  }
+  for (const field of ["viewport", "route", "selector", "landmarks", "sources"]) {
+    if (JSON.stringify(expected[field]) !== JSON.stringify(state[field])) {
+      failures.push(`${expected.id}: ${field} changed`);
+    }
+  }
+  if (expected.sourceDigest !== state.sourceDigest) failures.push(`${expected.id}: source digest changed`);
+}
+
+const output = {
+  format: 1,
+  version: packageJson.version,
+  description: "Deterministic visual-state contracts; browser-independent golden baselines.",
+  states: current,
+};
+
+if (update) {
+  await writeFile(baselinePath, `${JSON.stringify(output, null, 2)}\n`);
+  console.log(`Visual regression baselines updated: ${states.length} states`);
+} else if (failures.length > 0) {
+  console.error("Visual regression contract failed:");
+  for (const failure of failures) console.error(`- ${failure}`);
+  console.error("If the visual change is intentional, run: pnpm visual:regression:update");
+  process.exitCode = 1;
+} else {
+  console.log(`Visual regression contract passed: ${states.length} states`);
+}
diff --git a/app/scripts/visual-audit.mjs b/app/scripts/visual-audit.mjs
new file mode 100644
index 0000000..a6f21c4
--- /dev/null
+++ b/app/scripts/visual-audit.mjs
@@ -0,0 +1,118 @@
+#!/usr/bin/env node
+/** Final visual/accessibility audit for the Stage 5 release gate. */
+import { readFile } from "node:fs/promises";
+import path from "node:path";
+import process from "node:process";
+import { fileURLToPath } from "node:url";
+
+const root = path.resolve(path.dirname(fileURLToPath(import.meta.url)), "..");
+const read = (relative) => readFile(path.join(root, relative), "utf8");
+const failures = [];
+
+function hexToRgb(hex) {
+  const value = Number.parseInt(hex.replace(/^#/, ""), 16);
+  return [(value >> 16) & 0xff, (value >> 8) & 0xff, value & 0xff];
+}
+
+function luminance(hex) {
+  return hexToRgb(hex).reduce((sum, channel, index) => {
+    const value = channel / 255;
+    const linear = value <= 0.03928 ? value / 12.92 : ((value + 0.055) / 1.055) ** 2.4;
+    return sum + linear * [0.2126, 0.7152, 0.0722][index];
+  }, 0);
+}
+
+function contrast(foreground, background) {
+  const light = Math.max(luminance(foreground), luminance(background));
+  const dark = Math.min(luminance(foreground), luminance(background));
+  return (light + 0.05) / (dark + 0.05);
+}
+
+function paths(value, prefix = "") {
+  if (!value || typeof value !== "object" || Array.isArray(value)) return [prefix];
+  return Object.entries(value).flatMap(([key, child]) => paths(child, prefix ? `${prefix}.${key}` : key));
+}
+
+const [colorsSource, stylesSource, battleSource, campaignSource, rendererSource, ruSource, enSource, docsSource, manifestSource] = await Promise.all([
+  read("packages/render/src/colors.ts"),
+  read("../app/apps/game-pwa/src/styles.css"),
+  read("packages/ui/src/battle.css"),
+  read("packages/ui/src/campaign.css"),
+  read("packages/render/src/field-renderer.ts"),
+  read("packages/i18n/locales/ru/ui.json"),
+  read("packages/i18n/locales/en/ui.json"),
+  read("../doc/README.md"),
+  read("package.json"),
+]);
+const packageJson = JSON.parse(manifestSource);
+const colors = {
+  ink: "#14181c",
+  mistDim: "#b8b3a5",
+  white: "#f3ecdc",
+  amber: "#e0b34a",
+  danger: "#c45c5c",
+  movement: "#388cdc",
+  success: "#74e071",
+};
+
+// Normal text target is 4.5:1; large display text is checked at 3:1.
+for (const [name, foreground, background, minimum] of [
+  ["secondary text", colors.mistDim, colors.ink, 4.5],
+  ["primary text", colors.white, colors.ink, 4.5],
+  ["action amber", colors.amber, colors.ink, 3],
+  ["danger accent", colors.danger, colors.ink, 3],
+  ["movement blue", colors.movement, colors.ink, 3],
+  ["success green", colors.success, colors.ink, 3],
+]) {
+  const ratio = contrast(foreground, background);
+  if (ratio < minimum) failures.push(`${name} contrast ${ratio.toFixed(2)}:1 < ${minimum}:1`);
+}
+
+const requiredColorTokens = [
+  "--mist-dim", "--amber", "--danger", "--movement", "--success", "--forge", "--chamber", "--injury",
+];
+const requiredMotionTokens = ["--ui-motion-duration", "--ui-motion-ease"];
+
+for (const token of requiredMotionTokens) {
+  if (!stylesSource.includes(token)) failures.push(`missing motion token ${token}`);
+}
+for (const token of requiredColorTokens) {
+  if (!colorsSource.includes(`\"${token}\"`)) failures.push(`missing central color token ${token}`);
+}
+for (const [label, source, needles] of [
+  ["battle HUD", battleSource, [".roster-card", ".skill-row", ".aim-card", ".training-edge-hint"]],
+  ["campaign HUD", campaignSource, [".map-road-segment", ".ship-flight-trail", ".forge-panel", ".chamber-panel"]],
+  ["renderer", rendererSource, ["TOKEN_STATUS_SLOTS", "drawStatusBadge", "lowPowerDevice", "lastDynamicPaint"]],
+  ["global styles", stylesSource, ["--ui-motion-duration", ".display-title"]],
+]) {
+  for (const needle of needles) if (!source.includes(needle)) failures.push(`${label} is missing ${needle}`);
+}
+
+const ru = JSON.parse(ruSource);
+const en = JSON.parse(enSource);
+const ruPaths = paths(ru).sort();
+const enPaths = paths(en).sort();
+if (JSON.stringify(ruPaths) !== JSON.stringify(enPaths)) {
+  const missingRu = enPaths.filter((key) => !ruPaths.includes(key));
+  const missingEn = ruPaths.filter((key) => !enPaths.includes(key));
+  if (missingRu.length) failures.push(`RU locale misses: ${missingRu.join(", ")}`);
+  if (missingEn.length) failures.push(`EN locale misses: ${missingEn.join(", ")}`);
+}
+
+if (!docsSource.includes(`Текущая версия комплекта: ${packageJson.version}`)) {
+  failures.push(`doc/README.md is not synchronized to ${packageJson.version}`);
+}
+if (!battleSource.includes("prefers-reduced-motion") || !campaignSource.includes("prefers-reduced-motion")) {
+  failures.push("reduced-motion coverage is incomplete for battle/campaign surfaces");
+}
+if (!stylesSource.includes("text-wrap: balance") || !stylesSource.includes("font-kerning: normal")) {
+  failures.push("display typography audit markers are missing");
+}
+
+if (failures.length) {
+  console.error("Visual audit failed:");
+  for (const failure of failures) console.error(`- ${failure}`);
+  process.exitCode = 1;
+} else {
+  console.log(`Visual audit passed: contrast, semantics, motion, performance markers and ${ruPaths.length} locale keys`);
+}
diff --git a/app/visual-baselines/states.json b/app/visual-baselines/states.json
new file mode 100644
index 0000000..84b1b26
--- /dev/null
+++ b/app/visual-baselines/states.json
@@ -0,0 +1,133 @@
+{
+  "format": 1,
+  "version": "0.21.2",
+  "description": "Deterministic visual-state contracts; browser-independent golden baselines.",
+  "states": [
+    {
+      "id": "battle-start",
+      "viewport": {
+        "width": 1280,
+        "height": 720
+      },
+      "route": "menu → quick match → normal",
+      "selector": ".battle-screen",
+      "landmarks": [
+        ".battle-top",
+        ".battle-stage",
+        ".battle-bottom",
+        ".battle-objective"
+      ],
+      "sources": [
+        "packages/ui/src/BattleScreenView.tsx",
+        "packages/ui/src/battle.css",
+        "packages/render/src/field-renderer.ts"
+      ],
+      "sourceDigest": "c41c961e9599e21cf29a04183bd0a7ab241c6a92f5c634b2fd7641650d9c5da6"
+    },
+    {
+      "id": "aiming",
+      "viewport": {
+        "width": 1280,
+        "height": 720
+      },
+      "route": "battle-start → selected weapon → target preview",
+      "selector": ".aim-card",
+      "landmarks": [
+        ".aim-card",
+        ".aim-chance",
+        ".battle-stage",
+        ".aim-dmg"
+      ],
+      "sources": [
+        "packages/ui/src/BattleScreenView.tsx",
+        "packages/ui/src/battle.css",
+        "packages/render/src/field-renderer.ts"
+      ],
+      "sourceDigest": "c41c961e9599e21cf29a04183bd0a7ab241c6a92f5c634b2fd7641650d9c5da6"
+    },
+    {
+      "id": "fog",
+      "viewport": {
+        "width": 1280,
+        "height": 720
+      },
+      "route": "battle-start → hidden cells",
+      "selector": ".battle-stage canvas",
+      "landmarks": [
+        ".battle-stage",
+        "--fog",
+        "fogBaseLayer",
+        "paintFog"
+      ],
+      "sources": [
+        "packages/ui/src/BattleScreenView.tsx",
+        "packages/ui/src/battle.css",
+        "packages/render/src/field-renderer.ts"
+      ],
+      "sourceDigest": "c41c961e9599e21cf29a04183bd0a7ab241c6a92f5c634b2fd7641650d9c5da6"
+    },
+    {
+      "id": "training",
+      "viewport": {
+        "width": 390,
+        "height": 844
+      },
+      "route": "menu → training → first mission",
+      "selector": ".training-coach",
+      "landmarks": [
+        ".training-coach",
+        ".training-step-dots",
+        ".training-edge-hint",
+        ".is-training-focus"
+      ],
+      "sources": [
+        "packages/ui/src/BattleScreenView.tsx",
+        "packages/ui/src/battle.css",
+        "packages/ui/src/training-scenario.ts"
+      ],
+      "sourceDigest": "0dc9fd646fb0e4018c2bb8ea989aa1e3974ed19b898ecc45181e2209b74034cd"
+    },
+    {
+      "id": "campaign-map",
+      "viewport": {
+        "width": 1280,
+        "height": 720
+      },
+      "route": "menu → new tale",
+      "selector": ".campaign-map",
+      "landmarks": [
+        ".campaign-map",
+        ".map-road-segment",
+        ".map-marker",
+        ".ship-marker"
+      ],
+      "sources": [
+        "packages/ui/src/CampaignScreen.tsx",
+        "packages/ui/src/campaign.css",
+        "packages/campaign/src/index.ts"
+      ],
+      "sourceDigest": "4c4caabe0482d5f353b6335fc5377492cc02623feb995d84afa75f02bb926067"
+    },
+    {
+      "id": "mission-result",
+      "viewport": {
+        "width": 1280,
+        "height": 720
+      },
+      "route": "campaign battle → victory/defeat result",
+      "selector": ".mission-result-screen",
+      "landmarks": [
+        ".mission-result-screen",
+        ".roster-outcomes",
+        ".rewards-strip",
+        ".darkness-summary"
+      ],
+      "sources": [
+        "packages/ui/src/MissionResultScreen.tsx",
+        "packages/ui/src/campaign.css",
+        "apps/game-pwa/src/styles.css"
+      ],
+      "sourceDigest": "06b1f59fc86ba43a386355a3f3f098358cc9c7b75809f0a34fe34f1c8f1dd5ae"
+    }
+  ]
+}
diff --git a/doc/README.md b/doc/README.md
index 560b31d..79058f4 100644
--- a/doc/README.md
+++ b/doc/README.md
@@ -1,6 +1,6 @@
 # Комплект проектной документации
 
-**Текущая версия комплекта: 0.20.19** (совпадает с версией приложения в `app/package.json`). Версия комплекта проверяется скриптом `app/scripts/check-version-consistency.mjs`; перед публикацией запускайте `pnpm check:versions` из каталога `app`.
+**Текущая версия комплекта: 0.21.2** (совпадает с версией приложения в `app/package.json`). Версия комплекта проверяется скриптом `app/scripts/check-version-consistency.mjs`, а `pnpm check:versions` дополнительно запускает визуальные gate-проверки; перед публикацией запускайте его из каталога `app`.
 
 Настоящий комплект — нормативное описание текущего состояния проекта. Карта документов, каталогов кода и владельцев тем — в [project-map.md](project-map.md). При расхождении формулировок применяется документ, за которым закреплена предметная область. Числовые значения баланса, не указанные здесь явно, хранятся только в JSON5 и проверяются схемами Zod.
 
@@ -28,9 +28,12 @@
 ```bash
 cd app
 pnpm check:versions
+pnpm visual:regression
+pnpm audit:visual
 pnpm typecheck
 pnpm test
 ```
 
-`check:versions` не заменяет типизацию и тесты: он только обнаруживает
-расхождение версий между приложением, пакетами и документацией.
+`check:versions` не заменяет типизацию и тесты: он обнаруживает
+расхождение версий и запускает визуальные gate-проверки; полные типизация и
+тесты выполняются отдельными командами.
diff --git a/doc/content-schema.md b/doc/content-schema.md
index b2f4d04..b41ed2c 100644
--- a/doc/content-schema.md
+++ b/doc/content-schema.md
@@ -165,6 +165,7 @@ interface MissionConfig {
 Дружина (выпуск 0.11.0): стартовый состав задаёт `initialRoster`; новые бойцы вступают как рекруты (`recruitUnitId`) после успешной миссии, пока численность меньше `rosterCap`. Высадка включает от `deployMin` до `deployMax` живых бойцов. Погибший в миссии боец исключается окончательно; выживший, завершивший миссию с запасом здоровья не выше `woundHpRatio × maxHealth`, получает ранение со штрафами `woundPenalty` до лечения в Горнице. Выжившие при успехе получают уровень; рекрут, достигший `classUnlockLevel`, получает класс по выбору игрока. Кампания проиграна также, если в дружине не осталось живых бойцов.
 
 interface MapGenConfig {
+  biome?: "meadow" | "swamp" | "thicket" | "scorched"; // визуальная тема поля; legacy default: meadow
   width: number;             // 8…64
   height: number;
   pitChance: number;         // 0…1
@@ -179,6 +180,8 @@ interface MapGenConfig {
 }
 ```
 
+Биом меняет только представление и допустимые декоративные примитивы, не правила боя и не геометрию клеток: `meadow` — открытая трава и тёплые охристые акценты, `swamp` — холодная вода и камыш, `thicket` — тёмная зелень и кустарник, `scorched` — обугленная земля и красно-янтарный жар. Все процедурные детали детерминированы координатой клетки и seed партии; старые карты без `biome` читаются как `meadow`.
+
 Генератор обязан соблюдать ограничение связности, изложенное в документе «Игровые правила», раздел 5.3.
 
 ---
diff --git a/doc/operations.md b/doc/operations.md
index 3c0e2e6..4820efe 100644
--- a/doc/operations.md
+++ b/doc/operations.md
@@ -69,8 +69,10 @@ pnpm install
 | `pnpm dev` | Сервер разработки (`http://127.0.0.1:5173/`) |
 | `pnpm test` | Автоматические проверки |
 | `pnpm typecheck` | Проверка типов |
-| `pnpm build` | Производственная сборка PWA |
+| `pnpm build` | Производственная сборка PWA с визуальным regression-gate |
 | `pnpm preview` | Просмотр сборки (`http://127.0.0.1:4173/`) |
+| `pnpm visual:regression` | Сверка шести визуальных эталонов |
+| `pnpm audit:visual` | Контрастный, семантический и performance-аудит |
 
 Остановка: `Ctrl+C`. Если порт занят — открывать адрес из вывода сборщика.
 
@@ -82,7 +84,9 @@ node --version       # должен быть >= 20
 pnpm --version       # должен соответствовать packageManager
 ```
 
-Команду `check:versions` полезно выполнять до `build` и перед публикацией.
+Команды `visual:regression` и `audit:visual` входят в prebuild/pretest и
+вызываются из `check:versions`; последний полезно выполнять до `build` и
+перед публикацией.
 
 ---
 
diff --git a/doc/roadmap.md b/doc/roadmap.md
index 1d0286e..92ab095 100644
--- a/doc/roadmap.md
+++ b/doc/roadmap.md
@@ -25,8 +25,10 @@
 | 0.18.0 | Этап 5.1: генералы (Яга, Соловей) | готово |
 | 0.19.0 | Этап 5.2: режим обучения (3 миссии) | готово |
 | 0.20.0–0.20.19 | Этап 5.3: онбординг кампании и доводки входа/обучения/продолжения былины | готово |
+| 0.21.0–0.21.1 | Этап 5.4: удобство, инструменты анализа и presentation-слой | готово |
+| 0.21.2 | Этап 5: полировка, визуальная регрессия и финальный аудит | готово |
 
-Текущее приложение: **0.20.19**. Следующий функциональный подэтап — **0.21.0**.
+Текущее приложение: **0.21.2**. Следующий функциональный подэтап — **0.22.0**.
 
 ---
 
diff --git a/doc/ui-design.md b/doc/ui-design.md
index c1620eb..b532da1 100644
--- a/doc/ui-design.md
+++ b/doc/ui-design.md
@@ -22,6 +22,15 @@
 Клетки перемещения за одно очко действия: синие. Клетки рывка: жёлтые. Клетки, которые запрос предпросмотра не включил в допустимые, не подсвечиваются.  
 Противники Нави: ядовито-зелёный. Генералы и Кощей: фиолетовый.
 
+Вторичный текст использует `mistDim` (`#b8b3a5`) вместо прежнего
+приглушённого тона: на тёмных фонах целевой контраст — не ниже примерно 6:1.
+
+Единый источник цветов представления — `app/packages/render/src/colors.ts`.
+PixiJS получает из него числовые значения, а приложение до первого рендера
+публикует те же значения как CSS custom properties (`--amber`, `--mist-dim`,
+`--movement`, `--danger` и другие) для оболочки и интерфейса. В CSS не следует
+заводить самостоятельные значения этих семантических цветов.
+
 Заголовки: гротеск с умеренным намёком на вязь, при условии сохранения читаемости. Основной текст: нейтральный гротеск (Inter, Roboto либо равноценный). Цифры вероятности и урона — моноширинные.
 
 ---
@@ -58,6 +67,11 @@
 
 Поля выпуска 0.1.0: язык (перечень из манифеста локализации, подписи — собственные названия языков), общая громкость. Позднее на этот экран добавляются раздельная громкость музыки и действий, масштаб шрифта, пониженная цветовая различимость. Возврат — в главное меню. Смена языка перерисовывает оболочку без перезагрузки.
 
+В настройках доступно «Автоматически завершать ход»: при включённой опции
+ход передаётся Нави, когда у всех живых бойцов активной стороны закончились
+очки действия. По умолчанию опция включена для сохранения прежнего поведения;
+в обучении автоматический переход не обходит активное указание.
+
 ### 4.2. Комната сбора
 
 Экран ведущего: изображение быстрого считывания, короткая строка для ручного ввода, перечень подключившихся с указанием роли, переключатель полного обзора наблюдателя, поле числа мест N, выбор условия победы.
@@ -86,17 +100,27 @@
 - верхний правый угол — сторона хода и знаки юнитов стороны с оставшимися очками действия (это не очередь инициативы, а перечень ещё способных действовать);
 - нижний левый угол — силуэт выбранного, сегменты здоровья, ромбы очков действия по числу максимума;
 - нижний центр — оружие и умения; оружие и классовые умения последовательно занимают ближайшие свободные клавиши `1`–`8`, защитная стойка — `9`, дозор — `0`; у текущего набора Богатыря «Удар щитом» получает клавишу `5`; все действия оформляются одинаковыми кнопками слотов;
-- нижний правый угол — завершение хода стороны.
+- нижний правый угол — завершение хода стороны. Когда у живых бойцов не
+  осталось очков действия, доступная кнопка получает янтарную заливку;
+  заблокированная кнопка остаётся приглушённой.
 
 Поле:
 
 - один знак персонажа занимает одну клетку;
 - зона перемещения окрашивается по ответу запроса допустимых клеток; подъём на ярус обозначается знаком направления вверх и числом очков передвижения, полученным от ядра, а не самостоятельной подписью об очках действия; в обычном режиме нажатие проходимой клетки означает перемещение, даже если в клетке находится граневое укрытие;
-- при прицеливании строится линия к цели; на цели — знак неполного либо полного укрытия, если ядро сообщило ненулевую ступень; рядом — вероятность и интервал урона из ответа запроса; если ядро вернуло ненулевую поправку высоты к дальности, рядом с линией отображается указатель направления вверх или вниз;
+- при прицеливании строится линия к цели; на цели — знак неполного либо полного укрытия, если ядро сообщило ненулевую ступень; рядом — вероятность и интервал урона из ответа запроса; если ядро вернуло ненулевую поправку высоты к дальности, рядом с линией отображается указатель направления вверх или вниз; кольцо цели белое при предварительном выборе, янтарное при готовой атаке и красное, если выстрел недоступен;
+
 - дозор: полупрозрачная передняя полуплоскость по ориентации юнита;
 - неразведанные клетки закрыты непрозрачным туманом; разведанные и сейчас не наблюдаемые затемнены и не содержат знаков противника;
 - ярус читается толщиной плитки; яма — провалом;
 - целоклеточное укрытие рисуется внутри клетки, граневое — на соответствующей северной, восточной, южной или западной границе; при прицеливании знак щита показывается только если выбранное граневое укрытие действительно пересекает направление выстрела.
+- туман войны состоит из отдельного кэшируемого слоя: непрозрачное скрытие и
+  затемнение перестраиваются только при изменении наборов видимых и
+  разведанных клеток, а дрейф пятен обновляется не чаще пятнадцати раз в
+  секунду;
+- живые фишки слегка «дышат» с детерминированной фазой от идентификатора.
+  Дружина сохраняет круглую подставку, Навь получает шестиугольную; выбранная
+  фишка визуально приподнимается, а длина и плотность тени зависят от яруса.
 
 Кнопка оружия является переключателем режима прицеливания. Первое нажатие активирует её и убирает подсветку перемещения: последующие нажатия клеток рассматриваются только как предварительный выбор цели атаки и никогда не отдают команду перемещения. Повторное нажатие той же кнопки оружия выключает режим, после чего снова действует обычный выбор клетки для перемещения. Переключение на другое оружие выключает предыдущее. Выбор другого собственного юнита и начало нового хода также возвращают обычный режим.
 
@@ -135,10 +159,11 @@
 - основная кнопка — выбор и подтверждение;
 - дополнительная кнопка — отмена;
 - клавиша `Esc` — пауза; в паузе доступна справка по управлению;
-- клавиши перемещения либо колесо — сдвиг обзора;
+- клавиши перемещения — сдвиг обзора; колесо — зум вокруг точки под курсором без сдвига якоря;
 - клавиша табуляции — следующий собственный юнит с ненулевым запасом очков действия;
 - цифровые клавиши `1`–`8` — оружие и классовые умения в порядке слотов (повтор соответствующей клавиши режима выключает прицеливание), `9` — защитная стойка, `0` — дозор;
-- наведение — запрос предпросмотра пути и вероятности.
+- наведение — запрос предпросмотра пути и вероятности;
+- двойной щелчок по портрету бойца в интерфейсе центрирует камеру на нём.
 
 Сенсорный экран:
 
@@ -146,9 +171,15 @@
 - одно нажатие по клетке — предпросмотр;
 - повторное нажатие той же клетки — подтверждение;
 - движение одним пальцем — сдвиг обзора;
-- сближение пальцев — масштаб;
+- сближение пальцев — масштаб вокруг средней точки жеста;
+- двойное касание портрета бойца — центрирование камеры на нём;
 - длительное нажатие — справка.
 
+При системной настройке `prefers-reduced-motion: reduce` боевой экран
+отключает пульсации, покачивание фишек, переходы баннеров, тряску камеры,
+анимацию событий и дрейф тумана; действие и результат остаются доступными
+без визуальной потери смысла.
+
 ---
 
 ## 6. Отклик изображения
@@ -209,3 +240,54 @@
 ## 9. Различимость
 
 Цвет дублируется формой. Очко действия — ромб: заполненный при наличии, контурный при отсутствии. Подставка союзника круглая, подставка противника Нави — шестиугольная. Ярус дублируется толщиной плитки и знаками направления по вертикали, а не только цветом.
+
+---
+
+## 10. Полировка и защита результата (0.21.2)
+
+### 10.1. Визуальные эталоны
+
+Набор `app/visual-baselines/states.json` фиксирует шесть контрольных
+состояний: начало боя, прицеливание, туман, обучение, карта кампании и итог
+миссии. Для каждого состояния заданы маршрут воспроизведения, viewport,
+обязательные DOM/Pixi-ориентиры и digest файлов представления. Проверка
+запускается командой `pnpm visual:regression`; намеренное изменение картинки
+сначала фиксируется командой `pnpm visual:regression:update`. Это
+браузеронезависимый эталон семантической геометрии: он не хранит хрупкие PNG,
+зависящие от WebGL, системного шрифта и плотности пикселей конкретного
+устройства.
+
+### 10.2. Состояния на фишке
+
+Индикаторы постоянных состояний располагаются в фиксированных слотах вокруг
+фишки. Приоритет слотов: паника, яд, обездвиживание, скрытность, полёт,
+ограниченное существование, лесная маскировка, защитная стойка и дозор.
+Каждый знак имеет собственную форму и цвет, а подпись остаётся в панели
+выбранного бойца. Поэтому одновременно активные три и более состояния не
+накладываются друг на друга и не зависят от случайной фазы кадра.
+
+### 10.3. Силуэты
+
+Силуэты усилены только в существующих процедурных функциях рендера:
+богатырь получил широкую линию плеча, у стрельца лук и тетива выходят за
+подставку, у знахарки подчёркнуты острый капюшон и коса, у лесного зверя —
+низкий корпус, голова и лапы. Новые растровые файлы не добавлялись; мелкие
+детали остаются вторичными относительно силуэта.
+
+### 10.4. Подводка обучения
+
+Если цель активного шага обучения находится за рабочей областью холста,
+`.training-edge-hint` появляется на ближайшей кромке и поворачивается к
+цели. Карточка не принимает указатель, а подводка камеры остаётся основным
+способом вернуть цель в поле. При `prefers-reduced-motion: reduce` стрелка
+становится статичной, но не исчезает.
+
+### 10.5. Производительность и аудит
+
+Кэшируемый рельеф и туман не перерисовываются без изменения входных данных.
+На устройствах с четырьмя и менее логическими ядрами либо четырьмя и менее
+гигабайтами памяти плотность холста ограничена, дыхание фишек отключено,
+динамический слой обновляется не чаще 30 Гц, а дрейф тумана выключен.
+`pnpm audit:visual` проверяет контраст семантических цветов, наличие форменных
+дублей состояний, reduced-motion, низкопроизводительный профиль и полноту
+RU/EN словарей.
-- 
2.49.0.windows.1

